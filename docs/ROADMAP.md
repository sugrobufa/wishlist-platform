# ROADMAP — Grace (техимя репозитория — wishlist-platform)

Версия 0.3. Скоуп фаз выровнен по приоритетам дизайн-пакета (P0/P1/P2 из
`design/package/handoff/README.md`, турн 19b). Фаза закрыта, когда выполнен её
DoD: код + тесты + обновлённые `.env.example`, README и, при изменении
соглашений, CLAUDE.md.

## Phase 0 — Каркас

- Next.js 16 (App Router, TS strict), Tailwind v4, ESLint + Prettier, vitest,
  Playwright (e2e), GitHub Actions: lint + typecheck + test.
- **Дизайн-система из пакета**: генерация Tailwind-токенов из
  `design/package/handoff/tokens.json`; шрифты Archivo / Onest / Instrument
  Sans через next/font; изображения комнат (`design/package/refs/`) — в репо
  и на S3/CDN без изменений.
- `docker-compose.yml`: postgres 17 (+pgvector миграцией), redis, minio;
  `.env.example` из ARCHITECTURE §14.
- Prisma: схема ARCHITECTURE §4 целиком (включая каталог/распознавание за
  флагами), первая миграция; seed: 10 пресетов комнат из `rooms.json`,
  демо-комната.
- Auth.js: magic link (dev: ссылка в консоль) + Google (Apple/Telegram — при
  «укреплении», Phase 1–2).
- next-intl (ru), базовый layout на токенах.
- Worker BullMQ (`npm run worker`) + пример job; Sentry/PostHog через env.

**DoD:** `docker compose up -d && npm run dev` — вход/выход работает; токены и
шрифты соответствуют tokens.json; CI зелёный.

## Phase 1 — MVP: комната + тихое дарение (цикл целиком) — ✅ выполнена 2026-08-05

> **Список ниже — план фазы, каким он был.** 09.08.2026 решением владельца
> (тикет 124) у вещи отменены состояния «хочу» и «люблю»; различие переехало в
> место — комната и сокровищница (PRD §2). Поэтому в пунктах 2, 3, 5, 8 и в
> DoD читать: «призраков „хочу"» и пунктира больше нет, вопроса «что это для
> тебя» на экране добавления нет, «поля WANT» — это поля вещи КОМНАТЫ,
> «зал славы» — сокровищница, а «Дошло» — переезд туда, а не смена состояния.
> Текст плана оставлен как запись: он объясняет, почему код был устроен так, а
> не иначе.

1. Онбординг (турны 11–12): почта → комната-пресет → набор зон (F/M/Все 10).
2. **Сцена** (турны 15a/17a): кадр комнаты, хотспоты из `rooms.json`, наезд
   камеры по формуле `motion.json`, кадры «открыто» кроссфейдом, сетка вещей
   со стаггером; призраки «хочу» (пунктир = состояние, тест); ambient-дрейф;
   reduced-motion; ворота hover. Телефон + десктоп из одной карты координат.
3. **Карточка добавления** (турн 8): вопрос «что это для тебя» (люблю/хочу);
   по ссылке — парсер v1 (normalize + fastPath, safeFetch/SSRF, кэш, фикстуры
   ≥6 магазинов), вручную — товар и услуга; поля WANT (цена + видимость,
   размер/цвет/желание); `image.ingest`; дедуп по canonicalUrl.
4. **Тихая бронь**: гость по `/r/:slug` без регистрации, бронь (тихая /
   подписанная / складчина-каркас), cancelToken, «занято» для гостей;
   хозяйке — только счётчик; регистрация гостя «по пути».
5. **P0-экраны (6)**: «что подарили» → зал славы (`receiveGift`, раскрытие
   имён один раз, предложение связи) · карточка вещи хозяйки · карточка вещи
   гостя · зона целиком списком · «мои брони» гостя · письмо-напоминание за
   3 дня (`mail` + `occasion.close`).
6. Профиль и настройки: набор зон, вкл/выкл зоны (исчезают с мебелью),
   спрятанные вещи, «укрепление» вторым способом перед первым шером; GDPR
   (экспорт/удаление).
7. Связи: список с историей («дарила 2 раза»), взаимно/слежу/смотрели; без
   поиска людей.
8. e2e happy path: регистрация → добавила «люблю» и «хочу» по ссылке →
   поделилась → гость тихо забронировал → хозяйка видит только счётчик →
   «что подарили» → «Дошло» → вещь в зале славы, связь появилась.

**DoD:** e2e из п.8 зелёный; инварианты §5 ARCHITECTURE покрыты тестами
(owner-DTO без брони; hidden не течёт; пунктир=WANT); Lighthouse mobile ≥90 на
комнате гостя; метрики PRD §11 считаемы SQL.

**Итог (2026-08-05):** цикл дарения работает целиком — от онбординга до зала
славы, связей и писем; 400 юнит-тестов + 6 e2e зелёные, Lighthouse mobile 90
на прод-сборке комнаты гостя (медиана прогонов, разброс 87–90), метрики §11 —
готовыми запросами `docs/metrics.sql`. Отчёт фазы —
`.scratch/phase-1-mvp/REPORT.md`.

Отклонения от исходного плана фазы (сознательные):

- **sharp не подключён** (жёсткое ограничение тикета 06 — без новых
  зависимостей): image.ingest кладёт оригинал ≤5 МБ без resize/AVIF; аватар
  грузится без «sharp-квадрата». Джоба готова принять sharp — TODO в
  `src/worker/image-ingest.ts`, перенос в Phase 2.
- **ISR комнаты гостя** стартовал отложенным (тикет 07 держал `force-dynamic`,
  пока revalidateTag не зовут все мутации) — включён полировкой 16:
  полностраничный кэш с тегом `room-{id}` + страховочное окно 300 с.
- **Складчина — только каркас в БД** (режим POOL, по спеке): UI координации —
  Phase 2; в счётчике хозяйки складчина — одна вещь.
- **«Укрепление» вторым способом входа перед первым шером не строилось**
  (поле `User.secondAuth` в схеме есть, флоу нет — шер работает без него);
  Apple/Telegram из Phase 0 также не добавлялись. Кандидат Phase 2.
- **FRIENDS-видимость цены трактуется как ALL** до градации связей (TODO в
  `src/server/dto/guest-items.ts`).
- **Экран согласия «остаться на связи» (мок 20a) не строился**: связь
  создаётся сразу при «Дошло» (решение тикета 11 по README пакета, турн 20).
- **Чистка S3-сирот при живом аккаунте** (deleteItem / смена аватара / замена
  фото) и ретрай `s3Cleaned:false` после удаления аккаунта — Phase 2: джоба
  s3.cleanup поверх готовых кирпичей `listKeysByPrefix`/`deleteObjects`.
- **Письмо «за 3 дня»** покрыто юнитами (границы окна, идемпотентность), в
  e2e не воспроизводится — нет шва времени.
- **Метрика «доля ссылок без ручной правки»** (PRD §11) считается прокси:
  правки полей после парсинга не трекаются — честная оговорка и план на
  Phase 2 в `docs/metrics.sql`.

## Phase 2 — P1-пакет + наполнение

- **P1-экраны (8) — ПЯТЬ ИЗ ВОСЬМИ УЖЕ СДЕЛАНЫ** приёмками 10–12.08.2026, и
  список ниже правится на месте, чтобы по нему не начали заново (тот же класс
  ошибки, что у `handoff/README.md`, учившего отменённой модели, — тикет 197:
  врёт всегда тот файл, который только читают):
  - ~~день рождения~~ — **сделано** (тикеты 187, 198). Поля `occasionDate`
    больше нет: в комнате хранятся день и месяц, ближайшая дата считается на
    чтении, и праздников теперь три вида — день рождения, общие даты плашкой,
    свой повод;
  - ~~пустая зона~~ — **сделано** (тикеты 99, 193): одно место с плюсом, и оно
    же ссылка;
  - ~~ошибки распознавания ссылки~~ — **сделано** (`AddItem.errParse*`);
  - ~~спрятанные вещи~~ — **сделано** (тикет 74, зона списком);
  - ~~отмена брони~~ — **сделано** («Освободить вещь» в «Моих подарках»);
  - **остаётся: складчина изнутри** — только координация договорённостей, БЕЗ
    платежей (гриллинг №2: деньги через сервис не ходят никогда);
  - **остаётся: превью ссылки в мессенджере (OG)** — имя площадки в карточке
    уже отдаётся, картинки ждут логотип от дизайна;
  - **остаётся: подсказки.**
- **Каталог v1** (`CATALOG_ENABLED`): 1–2 источника по рынку, `feed.ingest`,
  поиск при добавлении, `embeddings.build`, `/out/:token` + клики.
- **AI-скрин v1** (`RECOGNIZE_ENABLED`): фото/скрин → атрибуты → кандидаты →
  подтверждение (вход в карточке добавления уже есть).
- Playwright-fallback + 3–5 адаптеров приоритетных магазинов с фикстурами.
- PWA: manifest + `share_target` (Android) → карточка добавления.
- `price.refresh`: перепроверка цен у вещей комнаты, бейджи изменения, письмо
  о недоступности.
- **Сокровищница: 2.5D-параллакс по карте глубины** (ADR-0001, решение владельца
  04.08.2026): monocular-depth открытой моделью один раз при загрузке фото
  (шаг в image.ingest, своё железо), параллакс-слои на подиуме. Ноль
  стоимости за вещь; three.js и платная генерация 3D — отвергнуты.
- Сверка хотспотов трёх мужских комнат с кадрами (метка из дизайн-пакета).

**DoD:** ≥80% тестовых URL без ручной правки; добавление из каталога без
парсинга; распознавание зоны ≥60% на тестовом наборе; складчина-координация
проходит целиком (без платежей); share из Android создаёт карточку; подиум
сокровищницы дышит глубиной (2.5D) на вещах с фото.

## Phase 3 — P2-пакет + каналы

- **P2-экраны (8)**: спасибо дарителям · поиск по своей комнате · уведомления ·
  смена почты и передача комнаты · удаление комнаты с чужими бронями ·
  подсказка гостю · загрузка/оффлайн · партнёрские магазины (витрина).
- Стриминги для зоны музыки (Spotify / Яндекс.Музыка — по рынку).
- Браузерное расширение (MV3); Telegram-бот (если рынок РФ).
- Монетизация CPA: расширение источников, отчёты по кликам/конверсиям.
- Сокровищница: решено ADR-0001 — 2.5D-параллакс уехал в Phase 2; платная
  генерация 3D (img2threejs и т.п.) отвергнута, three.js в проект не входит.

**DoD:** ≥1 новый канал наполнения в проде; учёт кликов и первая сверка CPA.

## Phase 4+ — Бэклог без обязательств

Новые комнаты-пресеты и premium-темы; аватар и «примерка» покупок; импорт
вишлистов (Amazon/Pinterest); Secret Santa; календарь дат и напоминания;
iOS Share Extension; закрытые комнаты «только для своих».
