# 53 — Приёмка партии «Кремовая»: 13 кадров, база, маски, контракт

**What to build:** Дизайн прислал первую полную комнату раунда 8 —
`C:\Wishlist\Wishlist платформа главный экран 9\round8\cream\`: 13 кадров
«открыто» (включая принятый пробником `fashion` и исправленные `home` /
`jewelry`), папку `base/` (как требовало задание), `masters-cream.json` и
README. Принять по полному циклу и подготовить вердикт + задание дизайну.

**Blocked by:** пробник принят (`design/ANSWERS-probe-round8.md`)

**Status:** done

## Порядок приёмки — уроки всех раундов, ни один не пропускать

### 1. База — сначала

`base/v4-cream.jpg` сравнить с продуктовым `design/package/refs/v4-cream.jpg`
**байт в байт** (хэш). Совпала — глобального сдвига нет по построению.
Не совпала — измерить l1; сдвиг больше шума пересжатия (~0.01) означает, что
кроссфейд мигнёт: кадры мерить против ЕГО базы, а в вердикт — требование
пересборки от нашего файла.

### 2. Порог + кольцо по всем 13 парам

`scripts/check-frames.mjs` логика (своя ≥ 0.05, отношение ≥ 3, кольцо ≤ 0.09
как флаг). Скрипт заточен под полный пакет — партию меряй адаптером (образец:
приёмка пробника, метрики скопированы дословно из check-frames). Мерить против
той базы, что выбрана шагом 1. `events` и `money` — по НОВЫМ прямоугольникам
из `design/rects-fix.json` (447,74,46,56 у events — сверься с файлом), они
приняты дизайном («восемь да»).

### 3. Глаза — все 13 пар, четыре класса

Вырезки база↔открыто с запасом (для `home` — запас вниз 2.6 высоты: там
пропажа пряталась). Классы: честное раскрытие / подмена / пропажа / ничего.
Сверять с глаголом: для пяти зон глаголы НОВЫЕ (движение вместо света —
`home` «поднос выезжает, свечи разгораются», `money` «конверт раскрывается»
и т.д.), для `fashion` — согласованное с нами «вещи расступаются» (проверь,
что он подтвердил в README/письме; молчание = глагол не согласован, выписать).
Пары-доказательства — в `.scratch/design-round-8/cream-check/`.

### 4. Контракт — точечно, НЕ копией

README и файлы рядом могут нести `zones.json` / `items.json` / `rooms.json`.
Правило после инцидента 51: **его rooms.json целиком не принимать** — прошлая
версия стирала наши поля приёмки (`reshoot`, `identityCheck`,
`rectsNotApplied`). Принимать точечно, поверх нашего:

- пять новых глаголов + `fashion` → в наш `handoff/zones.json` (и per-room
  `openVerb` в rooms.json «Кремовой», если согласован);
- пул `grooming` (5 вещей из его ответа) → `handoff/items.json` **и** наш
  `src/config/demo-pools.ts` + `tests/items.demo.test.ts` (свеча с инжиром
  уходит из мужских комнат — это видимое улучшение, вноси);
- прямоугольники `cream/events`, `cream/money` → rooms.json «Кремовой» из
  `design/rects-fix.json` (наши же числа, дизайн подтвердил);
- прошедшие кадры: `openFrame` + файл в `design/package/refs/` (ширина 2800 —
  проверь), `frameRound: 8`. Непрошедшие — `reshoot` + `reshootReason` с
  числами.

Если контрактных файлов в партии нет — вердикты по кадрам всё равно выносим,
а в задание дизайну идёт запрос файлов (обещал «zones.json и items.json уже
несут исправления»).

### 5. Маски — если приехали

Он обещал три полигона «Кремовой» (`maskHint`: polygon 630×351, note,
feather). Проверка как у прямоугольников: нарисовать полигон на кадре,
посмотреть глазами, попадает ли в силуэт. В контракт вносить только
проверенные; рендер маски в продукте — НЕ этот тикет (уйдёт в 50-продолжение).
Нет масок — строка в задание.

### 6. Мастера

`masters-cream.json` — проверить структуру и что ссылки отвечают (HEAD,
не качать). Это раунд-8 оригиналы, которых у раунда 7 не существовало.

### 7. Вердикт + задание

Два файла: `design/ANSWERS-cream-round8.md` — таблица 13 вердиктов с
числами; и в Comments тикета — черновик короткого «задания к пересылке»
(формат как пробничный: что принято, что переснять, что прислать). Если
партия чистая — задание = «Кремовая» подключена, следующая комната + маски
остальных зон «Кремовой».

## Осторожно

- **Изображения не править.** Приёмка = отбор.
- `design/package/refs/legacy/` не трогать — там история отклонённых.
- Счёт подключённых кадров изменится (сейчас 7). Обновить осознанно:
  тесты контракта, `CLAUDE.md` если зашито.
- `accepted ⟺ openFrame` — правило под тестом, оба флага вместе.
- Наши тесты «не верить» §1: цифры без глаз ничего не доказывают.
- Параллельно работает агент тикета 52 в `src/components/**` и `src/app/**`
  — его территорию не трогать; из src тебе разрешён ТОЛЬКО
  `src/config/demo-pools.ts` (+его тест). `messages/**` не трогать вовсе.

## Критерии

- [x] База сверена хэшем; выбор базы для порога записан с числом
- [x] 13 пар: порог + кольцо таблицей, глаза по четырём классам
- [x] Пары-доказательства в `.scratch/design-round-8/cream-check/`
- [x] Прошедшие подключены (`refs/` + rooms.json + `frameRound: 8`) — подключать оказалось нечего: 0 из 13, блок «база» в Comments
- [x] Контракт принят точечно; наши поля приёмки целы — тест
- [x] Пул grooming в handoff и в `demo-pools.ts`; демо мужских комнат без свечи
- [x] Маски проверены на кадре (или запрошены) — не приехали, запрошены файлом
- [x] Мастера проверены HEAD-ом
- [x] `design/ANSWERS-cream-round8.md` + черновик задания в Comments
- [x] `npm run typecheck && npm run lint` — зелёные; `npm test` точечно (полный запрещён этим же тикетом: параллельный агент 52), список и оговорки в Comments

## Территория

`design/package/handoff/**`, `design/package/refs/*.jpg` (только новые),
`design/ANSWERS-cream-round8.md`, `src/config/demo-pools.ts`,
`tests/design-contract.test.ts`, `tests/items.demo.test.ts`,
`.scratch/design-round-8/**`, `scripts/**` (адаптер партии — можно оформить
режимом check-frames).
**Не трогать:** `src/components/**`, `src/app/**`, `messages/**`,
`design/package/refs/legacy/**`, `prisma/**`, `design/rects-fix.json`.

## Comments

### Итог (2026-08-06)

**Подключено 0 из 13.** Не потому, что кадры плохи поголовно (глазами честных
пять, и комната впервые стоит неподвижно), а потому что вся партия снята не с
нашего файла — а кроссфейдиться ей с нашим. Полный вердикт для дизайна —
`design/ANSWERS-cream-round8.md`; ниже — то же с внутренними находками.

### Решение по базе (шаг 1) — с числами

Хэши НЕ совпали: наш `v4-cream.jpg` = `06a03e87ff2d6ce7…` (2800×1563,
541 070 байт), его `base/v4-cream.jpg` = `77f7a85299fbc01f…` (**2400×1340**,
475 222 байта). l1 между ними **0.0327** — в три раза больше шума пересжатия
(~0.01), и ровно то же число, что у его базы «Кремовой» в раунде 7 (приёмка
46): он по-прежнему правит от своей давней пересборки. README партии
(«правка от байт-в-байт продуктовой базы») опровергнут файлом — «чему НЕ
верить» §2 подтверждается в пятый раз.

По тикету порог мерился **против его базы**; вдобавок — против «чистой базы»
(медиана 13 кадров, метод пробника) и против нашей. Его `base/` — не источник
правки: кадры между собой сходятся на 0.005–0.010, а от его `base/` отстоят
на 0.018–0.045 (пересохранение в 2400 шумит). Отсюда и парадокс пробника:
`fashion`, принятый там с отн 5.88, здесь даёт 2.69 против его базы и
0.0492 · 13.96 против чистой — не кадр испортился, а линейка кривая.
Против НАШЕЙ базы фон всех кадров 0.056–0.078 — на каждом раскрытии мигала
бы вся комната; в прямоугольнике money его сцена отличается от нашей на
**0.2283** (у нас банка-копилка светится, у него потушена). В вердикт ушло
требование пересборки от байт-в-байт файла.

### Таблица 13 вердиктов (порог его база · чистая · кольцо · глаза · класс)

| Кадр | Его база | Чистая | Кольцо | Класс (глаза) | Вердикт |
|---|---|---|---|---|---|
| `fashion` | 0.0513 · 2.69 | 0.0492 · 13.96 | 0.0176 | честное — вещи расступились, все пять на месте | пересборка от нашей базы |
| `jewelry` | 0.0427 · 1.72 | 0.0362 · 3.69 | 0.0302 | честное, но ящики выехали ВНИЗ за прямоугольник, в зону bags | пересъёмка: жест внутрь прямоугольника |
| `home` | 0.0141 · 0.77 | 0.0027 · 0.86 | 0.0159 | ничего — поднос не выехал, свечи горят и в базе (README врёт) | пересъёмка |
| `money` | 0.0870 · 4.88 | 0.0842 · 28.01 | 0.0214 | честное — конверт раскрылся, купюры читаются | пересборка (в нашей базе банка светится) |
| `events` | 0.0699 · 2.46 | 0.0604 · 4.10 | 0.0232 | честное — билеты веером из тех же зажимов, по НОВОМУ прямоугольнику | пересборка |
| `travel` | 0.1032 · 4.54 | 0.0942 · 11.25 | 0.0339 | честное | не нужен: рабочий кадр раунда 3 подключён (бриф §5) |
| `anything` | 0.1182 · 4.29 | 0.1193 · 8.46 | 0.0434 | подмена окружения — стёрта банка-копилка с конвертом (предмет money), дифф его money-прямоугольника 0.1392 | пересъёмка |
| `flowers` | 0.0674 · 1.48 | 0.0650 · 1.76 | 0.0511 | подмена окружения — стёрт проигрыватель (предмет music) | пересъёмка |
| `bags` | 0.0751 · 3.81 | 0.0689 · 16.12 | 0.0189 | подмена — из шести сумок четыре, в центре новая большая | пересъёмка |
| `perfume` | 0.1054 · 5.92 | 0.0992 · 26.92 | 0.0297 | подмена — поднос заменён, флаконы перетасованы | пересъёмка |
| `beauty` | 0.0231 · 1.23 | 0.0193 · 5.58 | 0.0193 | подмена — органайзер заменён набором с боксом; жест левее прямоугольника | пересъёмка |
| `books` | 0.0146 · 0.47 | 0.0062 · 0.35 | 0.0669 | подмена — гигантская книга Dior перед комодом, корешки перерисованы | пересъёмка |
| `music` | 0.0176 · 0.97 | 0.0026 · 0.92 | 0.0187 | ничего — ящики не выезжают; проигрыватель затемнён | пересъёмка |

Сводка: порог его базы — 5, чистой — 6, нашей — 1; глаза: **5 честных,
6 подмен (две — чужих предметов), 2 без действия, 0 пропаж**. Пропаж нет
впервые за все раунды; зато открылся новый класс брака — подмена предметов
СОСЕДНИХ зон (в вердикт дизайну дописан четвёртый запрет).

Пары-доказательства: `.scratch/design-round-8/cream-check/` —
`cream-<зона>-{0-ourbase,1-base,2-open,3-pair,4-trio}.jpg` (тройка: наша
база · его база · открыто), `crop-pairs.mjs` для пересборки,
`batch-report.json` / `batch-report-repobase.json` / `median-report.json`.
Инструмент партии — `scripts/check-batch.mjs` (метрики скопированы из
check-frames.mjs дословно; режимы `--base batch|repo`, `--fix rects-fix`).

### Что принято в контракт точечно (rooms.json партии не существует — и целиком не принимали бы)

1. **Пять глаголов движения** → `handoff/zones.json` (home, sport, gaming,
   money, grooming) — по письму дизайна, принятому пробником.
2. **Пул `grooming`** (5 вещей, два «люблю») → `handoff/items.json`
   (19-й ключ) и `src/config/demo-pools.ts`; свеча с инжиром из мужских
   комнат ушла. Фото не назначал: предметников мужского ухода в пакете нет,
   серая заливка честнее чужой картинки.
3. **Прямоугольники `cream/events` 454,143,112,30 и `cream/money`
   226,282,40,58** → rooms.json «Кремовой» из `design/rects-fix.json`
   (дизайн подтвердил, «восемь да»); `rectOld`/`remappedRound: 8`; `bloomAR`
   пересчитан по формуле пакета (120 и 40) — долг раундов 4–5 не повторён;
   `sharedObjectWith` у money снят. Закрылись два реестра: пара
   `cream/events×flowers` из реестра пересечений (осталось 14) и «находка
   про скопированный угол» (совпадающих начал в контракте теперь ноль).
4. **12 вердиктов раунда 8** → `reshootReason` зон «Кремовой» (класс + что
   видно + числа), сводка партии → блок `round8` рядом с `round7`.
5. **Per-room глагол `cream/fashion` «вещи расступаются»** НЕ внесён
   сознательно: правило «глагол ровно у зон с кадром» (тест) — встанет
   вместе с принятым кадром. Зафиксирован в reshootReason, round8 и вердикте.
6. Остальные девять прямоугольников «восьми да» и выключение
   `sport/gaming` / `loft/gaming` по правилу дизайна — НЕ этот тикет
   (партии их комнат ещё не приезжали); у нас обе зоны и так скрыты адресно.

`accepted ⟺ openFrame` цел, состояние зон 7 + 115 + 8 = 130 не изменилось,
`identityCheck` раунда 7 не тронут — всё под тестом (51 зелёный).

### Маски и мастера

- **Маски не приехали.** README партии ссылается на три `maskHint` «в
  rooms.json» — файла в партии нет (как и обещанных zones.json/items.json).
  В задание ушла строка: файлы, не пересказ.
- **Мастера`masters-cream.json`**: структура честная, 13 ссылок, все HEAD 200
  (image/png, 1.78–1.99 МБ). Это нативные выходы ~1376×768 — оригиналов 5504
  у `edit role:image` не существует; ссылки живут ~сутки, качать имеет смысл
  только у принятой партии (эту не качал: принятых нет).

### Находки сверх тикета

1. **`cream/travel` (подключён с раунда 3) гасит банку уже сегодня**: в его
   кадре банка-копилка не светится, в базе — светится; при каждом раскрытии
   чемодана она гаснет и зажигается обратно. Вскрылось новым прямоугольником
   money (self-режим: фон 0.0525, отн 2.07) — раньше прямоугольник стоял на
   стене, а банка была в 8 единицах за краем вырезки тикета 49. Вырезка:
   `cream-check/travel-vs-base-money-rect.jpg`. Кадр НЕ снимал — работающее
   без решения владельца не трогаю; в задание дизайну вписано «пришлите
   travel при пересборке», замена закроет брак. Решение — за владельцем.
2. **Прямоугольник `cream/beauty` стоит на зеркале**, органайзер с косметикой
   — левее и ниже. В rects-fix (11 зон) этой зоны нет; кандидат в следующую
   порцию переразметки — жест дизайна в этой партии происходил целиком мимо
   прямоугольника.
   **ЗАКРЫТО 06.08** сверкой замеров: зона стала двенадцатым пунктом
   rects-fix, прямоугольник переехал 281,125,87,52 → 256,158,60,45, bloomAR
   пересчитан 97 → 77. Письмо — `design/ANSWERS-cream-measures.md`,
   вырезки — `.scratch/design-round-8/rects-events-money/`.
3. `books`-кадр портит корешки соседних книг («Dyar», «WAB I») — виден предел
   апскейла с 1376×768: мелкий текст рассыпается. Стоит помнить при retina.

### Проверки

`npm run typecheck` · `npm run lint` — зелёные.
`tests/design-contract.test.ts` — **51 зелёный** (обновлены: 19 вместо 23 у
тикета 49, блок раунда 8, реестр пересечений 14, bloomAR-долг 45,
remappedRound 35/13/2, сверка ректов с rects-fix.json).
`tests/items.demo.test.ts` — **16 зелёных** (19 пулов, сторож grooming).
Рядом: `tests/parser/zoneHint.test.ts`, `tests/messages-tone.test.ts` —
зелёные. `scripts/check-frames.mjs --self` работает на правленом контракте.

**Не запускал намеренно** (пересечение с территорией тикета 52 —
`src/components/**`): `camera-centering`, `immersive-layout`, `scene-camera`,
`tile-appearance` и полный `npm test`. ВАЖНО лиду: `camera-centering`
держит фикстуры «худших полей» числами (61/48 и поля до десятых) — новые
прямоугольники events/money могли их сместить; перепрогнать и переснять
числа после приземления 52. В `immersive-layout` список BEYOND_PHONE_WINDOW
не задет (events остался целиком за окном: 454 > 442; money как был в окне,
так и остался).

**Вне территории, не трогал:** `design/package/refs/README.md` (счёт «7
кадров» не изменился — править нечего), `src/config/design.ts` (тип контракта
терпит `round8` через каст; комментарий у `accepted` со счётом «7» верен).

### Файлы

`design/package/handoff/{rooms,zones,items}.json` ·
`src/config/demo-pools.ts` · `tests/{design-contract,items.demo}.test.ts` ·
`scripts/check-batch.mjs` (новый) · `design/ANSWERS-cream-round8.md` (новый) ·
`.scratch/design-round-8/cream-check/**` (вырезки + отчёты + crop-pairs.mjs).
Изображения комнат не правились и не копировались (принятых нет);
`refs/legacy/` не тронут; git-команд не выполнял; зависимостей не добавлял.

### Черновик задания дизайну (к пересылке владельцем)

> **Партия «Кремовая» не подключена: 0 из 13. Причина одна — база.**
> Кадры сняты не с нашего файла: ваш `base/` — 2400×1340, sha256 не наш,
> расхождение 0.0327 (как в раунде 7). README «от байт-в-байт базы» — неправда
> по файлу. Продукт кроссфейдит с НАШЕЙ базой: ваши кадры мигали бы всей
> комнатой, а банка-копилка (у нас светится, у вас нет) гасла бы.
>
> 1. **Пересоберите «Кремовую» от байт-в-байт копии нашего `v4-cream.jpg`**
>    (2800×1563, 541 070 байт, sha256
>    `06a03e87ff2d6ce78400cf4c2da2551a55bff0083f59e7fe855beea68d6184d5`).
>    В `base/` кладите этот файл как есть, без пересохранения: совпал хэш —
>    вопрос базы закрыт навсегда.
> 2. **Пересборка техническая** (механика принята глазами): `fashion`,
>    `events`, `money`, и заодно `travel` — в нынешнем кадре раунда 3 банка
>    не светится, замените его той же механикой.
> 3. **`jewelry`** — жест внутрь прямоугольника: ящики выдвигаются в
>    плоскости комода, не вниз к сумкам. Назовите глагол зоны: в этой мебели
>    комод, а не шкатулка.
> 4. **Переснять шесть подмен и два «ничего»**: `bags` (те же шесть сумок),
>    `perfume` (тот же поднос и флаконы), `beauty` (тот же органайзер, жест в
>    прямоугольнике), `books` (книга из стопки, в масштабе), `music` (ящики
>    винила выезжают), `home` (поднос реально выезжает — в присланном кадре
>    его движения нет, README ошибся), `anything` (банку и ленту не трогать),
>    `flowers` (проигрыватель не трогать).
> 5. **Четвёртый запрет, дописали**: кадр «открыто» не трогает предметы в
>    прямоугольниках ДРУГИХ зон комнаты. В этой партии стёрты банка (кадр
>    `anything`) и проигрыватель (кадр `flowers`).
> 6. **Самопроверка перед отправкой — по вырезке зоны с запасом**, три
>    вопроса + четвёртый: «чужие предметы целы?». `home` этой проверки не
>    прошёл бы.
> 7. **Файлы, не пересказ**: `rooms.json` с тремя полигонами масок,
>    `zones.json`, `items.json`, ваш `CLAUDE.md` с правилом выключенной зоны.
>    Глаголы и пул «Уход» мы внесли по вашему письму — файлы нужны для сверки.
>    `masters.json` — с каждой партией, как сейчас (принято).
>
> Прогресс признаём числами: комната неподвижна (0.005–0.010 между кадрами
> против 0.19–0.25 в раунде 7), пропаж содержимого впервые ноль, прямоугольники
> `events`/`money` уже в контракте. База совпала хэшем, кадры прошли —
> «Кремовая» подключается целиком, дальше следующая комната и маски.
