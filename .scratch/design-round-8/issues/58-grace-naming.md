# 58 — Grace: имя продукта везде, приёмка логотипа — когда приедет

**What to build:** Владелец назвал площадку — **Grace** — и разработал с
дизайнером логотип (он на живой доске, в наших выгрузках его ещё нет).
Разнести имя по продукту; логотип принять отдельным шагом.

**Blocked by:** 56 (вход — там имя встаёт первым, агент уже предупреждён)

**Status:** todo

## Часть 1 — имя, не дожидаясь логотипа

Инвентарь мест, где сейчас живёт «Wishlist Platform», и что делать:

| Место | Сейчас | Станет |
|---|---|---|
| `src/app/layout.tsx` → `metadata.title` | «Wishlist Platform» | «Grace» (+template для внутренних страниц) |
| Вкладка/OG-метаданные | — | проверить `openGraph`/`description`, добавить по-минимуму |
| Письма (`src/worker/mail*`, шаблоны) | от кого/подписи | имя в подписи и теме, где стояло название; адрес отправителя НЕ трогать |
| Экран входа | делает тикет 56 | сверить, что ключ один (Brand.name) |
| `messages/**` | проверить вхождения имени | через брендовый ключ, не литералами |
| README/docs заголовки | «wishlist-platform» | первая строка: «Grace (рабочее техимя репозитория — wishlist-platform)» |

**Не трогать:** техимена — репозиторий, пакет, docker-имена, `WishlistBot/1.0`
(UA парсера), стоп-лист слагов в `rooms.ts` (это про URL, «grace» туда
ДОБАВИТЬ — имя продукта не должно стать чьим-то slug). PWA-манифеста пока
нет — не заводить ради имени.

## Часть 2 — логотип (отдельный шаг, когда приедет)

- В следующем письме дизайнеру уже попросить: **SVG-экспорт логотипа** (и
  правило: где живёт — вход, шапка гостя? — и минимальные отступы), плюс
  вордмарк/знак раздельно, если они разные.
- Приёмка как у всего: файл в `design/package/`, сверка с правилом иконок
  пакета (контур/сетка — если логотип рисованный), внедрение только после
  приёмки.
- До того — текстовый вордмарк Archivo, как сделал тикет 56.

## Критерии

- [ ] `metadata.title` «Grace», template для внутренних
- [ ] Брендовый ключ один, литералов имени нет
- [ ] Письма: подпись/тема с именем; адреса не тронуты
- [ ] «grace» добавлен в стоп-лист слагов
- [ ] README первая строка с именем
- [ ] Строка про SVG-логотип добавлена в следующее письмо дизайну
- [ ] `npm run typecheck && npm run lint && npm test` точечно

## Территория

`src/app/layout.tsx`, `messages/**`, `src/worker/**` (шаблоны писем),
`src/server/services/rooms.ts` (стоп-лист, одна строка), `README.md`,
`docs/**` заголовки.
**Не трогать:** `design/package/**`, auth-механику, env/docker.

## Comments
