# 65 — Телефон: постоянный нижний бар, чистый верх, без «Добавить вещь»

**What to build:** Три замечания приёмки 07.08 (пункты 1, 2, 5) — про один и
тот же слой: обвязку комнаты на телефоне.

1. Таб-бар в комнате свёрнут в планку 112×4 и выезжает шторкой только по тяге
   пальцем. Владелец просит **всегда видимый** бар — «базовая подготовка для
   будущей вёрстки приложения». На скриншоте приёмки видно цену шторки:
   вытянутая, она наезжает на «Добавить вещь», на значок «поделиться» и на
   указатель зон.
2. Кнопка «Добавить вещь» в нижней полосе — убрать с телефона. В баре уже есть
   место «＋ Добавить», ведущее туда же (`/room/add`).
3. Верхнее меню телефона («Друзья · Сокровищница · Настройки») — убрать.
   Постоянный бар содержит ровно эти три пункта: дубль полный.

**Status:** done

## Что уже есть (не переделывать)

- `src/components/tab-bar/tab-bar.tsx` — **постоянный бар уже написан**
  (`.bar`, высота 86 = `phoneImmersive.tabBar`, иконка 22). Он стоит на
  экранах-вкладках: Друзья, Сокровищница, Настройки. Новой вёрстки бара не
  нужно — нужно поставить его в комнату.
- `tab-slots.tsx` — пять мест, одна разметка для обоих состояний.
- `room-tab-drawer.tsx` — шторка: её роль в комнате заканчивается.
- Иконки — `src/components/icons.tsx`.

## Что сделать

**Бар.** В комнате хозяйки (`src/app/room/page.tsx`) и в комнате гостя
(`src/app/r/[slug]/page.tsx`) на телефоне вместо `RoomTabDrawer` показывать
постоянный `TabBar active="room"`.

Гостю пять мест хозяйки не годятся: у него нет ни «Добавить», ни «Настроек».
Состав гостевого бара — решение тикета, запиши его в Comments; нейтральный
дефолт: «Комната» и «Мои брони» (`/my-bookings`), больше ничего. Если места
меньше трёх — бар гостю не показывать вовсе, шторку ему тоже убрать.

**Шторка.** `room-tab-drawer.tsx` после этого не используется нигде — удалить
вместе с состояниями 1 и 2 в `tab-bar.module.css` (`.strip`, `.sheet`,
`.handle`, `.backdrop`, `.sheetRow`, их переменные и reduced-motion ветку) и
с их тестами. Не оставлять мёртвым кодом «на всякий случай»: жест тяги стоил
тикета 55 отдельной правкой прицела, и держать его выключенным — держать эту
цену.

**Раскладка.** Бар `fixed`, высотой 86, контента не двигает. Значит нижняя
полоса (указатель зон, 116) должна встать НАД ним, а не под ним. Считать по
`layout.phoneImmersive` из `tokens.json`, руками чисел не набивать. Жёлоб
18 px под планку (`--tb-strip-tap`), который тикет 55 выторговал у полосы,
теперь не нужен — полоса забирает свой низ обратно.

**Кнопка.** «Добавить вещь» из `imm-row` убрать на телефоне. На десктопе
она остаётся: там постоянного бара нет. Значок «поделиться» (`ShareButton`)
остаётся на обеих раскладках — второго пути к адресу комнаты нет.

**Верх.** `nav.imm-area-actions` (Друзья · Сокровищница · Настройки) — скрыть
на телефоне, оставить на десктопе. Заголовок «ТВОЯ КОМНАТА · ИРИНА» и баннер
повода остаются.

## Осторожно

- **Не трогать геометрию сцены**: `immersive-layout.ts`, `camera.ts`,
  `rooms.json`. Тест `immersive-layout` обязан остаться зелёным; если он
  падает из-за высоты полосы — правится тест вместе с раскладкой, и в
  Comments объясняется, какое число изменилось и почему.
- Не ломать тикеты 50/55/57/59/60/64: свет по приближению, панорама пальцем,
  положение кадра, лист вещей, отклик зоны.
- Числа — из `tokens.json`, не из головы.
- Скрытие верхнего меню — по медиа-запросу раскладки, а не по `user-agent`.
- Тексты — через `next-intl`, `messages/**` править точечным Edit с
  перечитыванием (общая территория).

## Критерии

- [x] На телефоне в комнате бар виден всегда, тяги пальцем больше нет
- [x] Указатель зон стоит над баром, цели пунктов не срезаны (≥44 px)
- [x] «Добавить вещь» на телефоне из полосы ушла, в баре работает
- [x] Верхних пунктов меню на телефоне нет; на десктопе всё как было
- [x] Гостевой бар описан и обоснован в Comments
- [x] `room-tab-drawer.tsx` и мёртвый CSS удалены, тестов на них не осталось
- [x] `npm test`, `lint`, `typecheck` зелёные; замеры в Comments

## Территория

`src/components/tab-bar/**`, `src/app/room/page.tsx`,
`src/app/r/[slug]/page.tsx`, `src/app/globals.css`, `messages/**` (точечно),
свои тесты.
**Не трогать:** `src/components/scene/**` кроме вызова полосы,
`immersive-layout.ts`, `camera.ts`, `design/package/**`, `src/server/**`.

## Comments

### Что изменено

- `src/components/tab-bar/tab-bar.tsx` — проп `phoneOnly`: бар исчезает
  на десктопе. Ставит его только комната;
- `src/components/tab-bar/tab-slots.tsx` — пропы `iconSize`/`addIconSize`/
  `onNavigate` убраны. Они существовали ради разницы в единицу между баром и
  шторкой (22/21 и 19/18) и ради закрытия шторки по переходу; шторки нет —
  остались две константы;
- `src/components/tab-bar/tab-bar.module.css` — состояния 1 и 2 удалены
  целиком (`.strip`, `.stripHidden`, `.sheet`, `.sheetOpen`, `.sheetDragging`,
  `.handle`, `.backdrop`, `.sheetRow` и девять переменных). Добавлен
  `.barPhoneOnly`;
- `src/components/tab-bar/room-tab-drawer.tsx` — **удалён** (198 строк);
- `src/app/room/page.tsx` — `TabBar active="room" phoneOnly`; класс
  `imm-desktop-only` на строке ссылок шапки и на «Добавить вещь»;
- `src/app/globals.css` — ветка `@media not all and (min-width: 1024px)`:
  полоса встаёт на `bottom: var(--imm-tab-bar)`, десктопные элементы гаснут;
- `src/components/scene/zone-index.module.css` — `margin-bottom: 18px` снят;
- `tests/tab-bar.test.ts` — два случая переписаны, один добавлен.

`immersive-layout.ts`, `camera.ts`, `design/package/**`, `src/server/**`
не тронуты.

### Почему гостевой бар не появился

Тикет просил решить его состав. Решение: **гостю бара нет.** Из пяти мест
хозяйки четыре ему не принадлежат — «Добавить», «Настройки», «Сокровищница»
и сама «Комната» ведут в ЕГО собственную комнату, которой у гостя может не
быть вовсе (регистрация «по пути», тикет 08). Осталась бы одна ссылка «Мои
брони» — ради неё бар в 86 px не заводят, тем более что она уже стоит в
нижней полосе гостя (`MyBookingsLink`). Шторки у гостя и не было: `/r/[slug]`
её никогда не подключал — там нижняя полоса с призывом и указателем зон, и
она осталась ровно как была.

### Замеры на стенде (телефон 375×812, комната cream)

| | |
|---|---|
| бар | `position: fixed`, 86 px, 726…812 |
| нижняя полоса | 116 px, 610…726, `bottom: 86px` |
| пересечение полосы и бара | **0** |
| целей в полосе | 14, минимальная высота **44** |
| нижний край пунктов указателя | 726 = верхняя кромка бара |
| мест в баре | 5, каждое 44 |
| верхняя строка ссылок | `display: none` |
| «Добавить вещь» в полосе | `display: none` |

Прежде пункты указателя кончались на 18 px от низа экрана: этот жёлоб полоса
отдавала планке таб-бара (тикет 55). Планки нет — жёлоб вернулся указателю.

Десктоп 1280×800 той же страницы: бар `display: none`, полоса `bottom: 0px`
(684…800), строка ссылок `flex` с тремя пунктами, «Добавить вещь» `flex`.
Ошибок в консоли ноль.

### Проверки

`npx tsc --noEmit` чисто, `npm run lint` чисто. Локально `npm test` даёт
270 зелёных на всех файлах без БД (раскладка, сцена, камера, контракт
пакета, таб-бар); 27 файлов с Postgres в этой среде не идут — Docker не
поднимается. Их прогнал CI на своей БД: прогон `31164138491` — success,
выкат `31164323257` — success.

### Сомнения

1. **Нижний край пунктов указателя ровно на кромке бара** (726 = 726).
   Пересечения нет, но воздуха между ними тоже нет — разделяет только
   волосяная линия бара. Тикет 66 меняет это место целиком (полоса уступает
   вертикальному списку зон), поэтому подбирать отступ сейчас — работа
   в мусор.
2. **Порог 1024 для «телефона»** — тот же, что у всей раскладки комнаты, но
   он широкий: планшет в портрете (768) получает бар. Это скорее правильно,
   чем нет, но глазами на планшете не смотрели.
3. **Скриншот приложить не удалось**: снимок экрана в этой среде не
   работает (пане не отдаёт кадры), всё проверено числами — computed styles
   и прямоугольники. Глазами стоит взглянуть самому.
