# 71 — Двадцать один предметный кадр подключён (раунд 12)

**What to build:** Шаг 2 тикета 68. Дизайн закрыл задание 2 письма
`DESIGN-BRIEF-09.md`: 21 кадр на пять пулов, которые показывали подряд
четыре-пять плиток-заглушек.

**Status:** done

## Приёмка — считали сами, смотрели глазами

Правило проекта: письмам дизайна не верить, мерить самим.

**Формат — 21 из 21 сходится:** все 400×400, JPEG, 7–22 KB. Не 400×400 — ноль.

**Глаза (выборка по риску):**

| кадр | что на нём | вердикт |
|---|---|---|
| `p-philharmonic` | кремовая карточка с тиснёным лавровым венком | чисто, текста нет |
| `p-tbilisi` | конверт с сургучной печатью, открытка уголком | символ, а не пейзаж — как просили |
| `p-opera-tickets` | два гильошных билета с перфорацией | чисто, леттеринга нет |
| `p-museum-card` | терракотовая карточка на льне | самый абстрактный кадр партии |
| `p-gaming-chair` | кресло с крестовиной и роликами | база на месте (пересдача) |
| `p-trimmer` | триммер стоймя, гребёнка читается | стоит, не левитирует (пересдача) |

Ни на одном кадре нет текста, леттеринга и логотипов — проверено.

## Что изменено

- `design/package/refs/` — 21 новый `p-*.jpg` (было 16 предметных, стало 37);
- `src/config/demo-pools.ts` — фотографии проставлены пяти пулам: `grooming`
  (5), `travel` (4), `gaming` (4), `books` (4), `events` (4).

## Замер до и после

| | было | стало |
|---|---|---|
| примеров всего | 80 | 80 |
| **без фотографии** | **54** | **33** |
| пулов пустых целиком | 5 | **0** |
| разных кадров в пакете | 16 | 37 |

Ни один пул больше не показывает сплошной ряд заглушек. Оставшиеся 33 стоят
в четырнадцати пулах, где фотографии уже были, — там плитки чередуются, и
букву-заглушку (тикет 68) видно как редкое исключение, а не как правило.

Ссылок на кадры в наборе 48, разных файлов 37, отсутствующих на диске — **0**
(проверено скриптом по всему набору).

## Критерии

- [x] Все 21 кадр 400×400 и лежат в `design/package/refs/`
- [x] Пять пулов закрыты полностью
- [x] Каждая ссылка `refs/p-*.jpg` в наборе указывает на существующий файл
- [x] Кадры проходят маршрут раздачи (`SAFE_IMAGE_NAME` — имена подходят)
- [x] `typecheck`, `items.demo`, `tile-appearance`, `design-contract` зелёные

## Что осталось

33 примера без фотографии в четырнадцати пулах: `perfume`, `watches`, `music`,
`sport`, `home`, `anything`, `flowers` (по 3), `fashionF`, `fashionM`, `bags`,
`tech`, `sneakers` (по 2), `jewel`, `beauty` (по 1). Это следующая партия,
если решим её заказывать; острота снята — ряды заглушек кончились.

## Территория

`design/package/refs/**` (только добавление), `src/config/demo-pools.ts`.
**Не трогать:** кадры комнат (`v4-*`, `v5-*`, `m-*`, `c-*`, `o-*`),
`stand-seed.ts`, `rooms.json`.
