# 81 — Подключить шесть кадров раунда 14 и переразметить четыре зоны

**What to build:** Приёмка раунда 14 сделана (письмо `DESIGN-BRIEF-12.md`),
решения приняты. Осталась работа в контракте и в пакете.

**Status:** todo

## Часть 1 — подключить шесть кадров

Файлы: `Wishlist платформа главный экран 22/round14/delivery/`.
Все 2800×1563, сняты от наших баз (sha сошлись все восемь).

| кадр | отн | как принят |
|---|---|---|
| `lux/anything` | 4.03 | порогом |
| `study/anything` | 3.44 | порогом (против ректа тикета 75) |
| `cottage/anything` | 3.31 | порогом |
| `warm/travel` | 3.02 | порогом |
| `emerald/travel` | 2.96 | **глазами**, решение владельца |
| `cottage/travel` | 2.77 | **глазами**, решение владельца |
| `loft/travel` | **4.74** | порогом (раунд 15) |

**`loft/travel` приехал раундом 15** и снят против ректа, который мы приняли
тикетом 75, — 4.74 без всяких оговорок. Файл там же, в поставке раунда 15.
Глазами: клапаны верхней коробки вверх, внутри белая бумага; транспортный
лейбл переехал с крышки на переднюю грань — это переезд вещи с крышки, как
шляпа у `warm`, а не подмена. Кадров к подключению становится **семь**.

Что сделать:

- положить файлы в `design/package/refs/` под их именами `o-<комната>-<зона>.jpg`;
- в `rooms.json` у этих шести зон проставить `openFrame`, `accepted: true`,
  `frameRound: 14`, снять `reshoot`/`reshootReason`, записать `eyeNote`;
- **правило `accepted ⟺ openFrame` держится тестом `design-contract`** — оно и
  должно остаться зелёным;
- пути `openFrame` в контракте пишутся как `refs-2x/<комната>/o-…jpg`, а файлы
  лежат в `refs/` — так уже у всех десяти подключённых. Не «чинить» это в
  рамках тикета: раздача (`app/rooms/[image]`) берёт по плоскому имени.

**Числа принятых по глазам записать в `remapNote`/`eyeNote` явно** — через
месяц «2.77» без объяснения прочтётся как недосмотр.

## Часть 2 — переразметить четыре зоны

`lux/travel`, `bold/anything`, `gamer/anything`, `loft/anything`.

Жест у них честный, но происходит вне нашего прямоугольника — это видно по
числам (против наших ректов 0.52–2.28, против его предложений 1.83–3.46).
Предложения дизайна взять нельзя, все пересекают соседей:

| предложение | пересечения |
|---|---|
| `lux/travel` {262,205,125,110} | jewelry 2254 px², music 385, perfume 220, money 198 |
| `bold/anything` {75,248,180,85} | bags 1936 px², beauty 952 |
| `gamer/anything` {0,220,114,116} | sneakers 1368 px² + 1392 px² за краем окна |
| `loft/anything` {45,260,80,75} | travel 864 px² |

Делать ровно как в тикете 75: смотреть вырезки `scripts/rects-preview.mjs`,
мерить по предмету, следить за соседями и за `x ≥ 12`, пересчитывать `bloomAR`
формулой пакета, сохранять `rectOld` и `remapNote`.

**Осторожно с `loft/anything`:** он пересекается с `loft/travel`, который
только что переразмечен тикетом 75. Двигать `travel` обратно нельзя — у него
там запас под крышку коробки.

После переразметки **прогнать `scripts/check-drop.mjs` заново**: если кадр
берёт порог против нового ректа, он подключается без пересъёмки.

## Часть 3 — `emerald/anything`

Кадра нет, три попытки в брак. Посмотреть зону вырезкой: возможно, та же
болезнь прямоугольника. Если после переразметки жест найдётся — заказать
кадр; если нет — зона остаётся со светом в коде, и это не дыра.

## Осторожно

- Контракт править **точечно**: круг «разобрать → записать» с отступом 2
  воспроизводит файл байт-в-байт (проверено в тикете 75), но трогать надо
  только свои поля.
- Счётчики в тестах поедут снова (`rectOld`, `remappedRound`, раскладка,
  центровка) — каждый подписывать причиной, как в тикете 75.
- Кадры комнат неприкосновенны: `o-*.jpg` только ДОБАВЛЯЮТСЯ, базы не трогать.

## Критерии

- [ ] Шесть кадров лежат в пакете и подключены в контракте
- [ ] `accepted ⟺ openFrame` держится; `design-contract` зелёный
- [ ] Принятые по глазам подписаны числом и причиной
- [ ] Четыре зоны переразмечены без пересечений, вырезки в Comments
- [ ] `check-drop.mjs` прогнан против новых ректов, числа в Comments
- [ ] Зон с кадром стало 16 из 130 (10 + 6) — записать в `design.ts`
- [ ] `npm test`, `lint`, `typecheck`, `npm run build` зелёные

## Территория

`design/package/refs/**` (только добавление), `design/package/handoff/rooms.json`,
комментарии в `src/config/design.ts`, тесты со счётчиками.
**Не трогать:** базы комнат, `zones.json`, код сцены.
