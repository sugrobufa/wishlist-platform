# 167 — 44 значения `bloomAR` из дельты раунда 41

**Откуда:** `design/package/handoff/round41/rects-delta.json` (пакет уже в
репозитории, коммит `784019e`). Отвечает на письмо 44, пункты 1 и 2.

**Что это.** Впервые дизайн прислал ДЕЛЬТУ, а не копию контракта. Его же
формулировка: «ваш контракт — ваш: приёмка, осмотры, причины пересъёмки живут у
вас, и наша копия их затирала. Признаём: 216 полей вашей работы пропали именно
так». Прямоугольники не тронуты вовсе — прислана только пересчитанная форма
светового пятна.

## Сверка входа: всё сошлось, ничего не пришлось возвращать

| проверка | ожидали | получили |
|---|---|---|
| зон в дельте | 59 | 59 |
| прямоугольник дельты = наш | 59 из 59 побайтно | **59 из 59** |
| строк, где `bloomAR` отличается от нашего | 44 | **44** |
| пустышек (`bloomAR` уже равен нашему) | 15 | **15** |
| у пустышек `was` = наше текущее | нет (снят с round40) | **ни у одной из 15** |
| формула воспроизводит присланное значение | — | **44 из 44** |

Пятнадцать пустышек — те зоны, что мы починили сами раньше (round8, тикеты 63,
75, 81): дизайн считал `was` по снимку round40 и наших правок в нём не видел.
Править в них нечего, они пропущены.

## Формула проверена на всей карте, а не только на дельте

`bloomAR = clamp(30, 120, round(rect.w / rect.h * 58))`

- **до правки:** совпадает у 86 зон из 130; расходится ровно у 44 — и это
  ровно те 44 строки дельты, ни одной лишней;
- **у зон без `rectOld`** (то есть никогда не переразмечавшихся): **64 из 64**.
  Это и есть доказательство, что формула та самая: там, где прямоугольник не
  двигался, она воспроизводит наши числа поголовно;
- **после правки: 130 из 130.**

## Что сделано

1. **44 значения `bloomAR` в `design/package/handoff/rooms.json`.** Диффом ровно
   44 строки значений плюс одна новая — больше ничего в файле не двинулось
   (формат `JSON.stringify(obj, null, 1)` воспроизводится побайтно, проверено
   до правки). Крупнейшие: `emerald/home` 30 → 108, `bold/music` 34 → 96,
   `bold/home` 30 → 76, `sport/events` 106 → 42, `cream/books` 120 → 77,
   `gamer/watches` 67 → 120, `cream/home` 67 → 112.
2. **Формула записана РЯДОМ С ПОЛЕМ** — новый ключ `rooms.json → bloomRule`
   (сразу за `zonesNote`), плюс тип `RoomsContract.bloomRule` и доккомментарий
   у `RoomZone.bloomAR` в `src/config/design.ts`. Это и было условием тикета:
   формулу подтверждали заново трижды — раунды 8, 11 и 41, — и каждый раз она
   приезжала письмом, а в файле её не стояло. Четвёртой сверки не будет.

`bloomRot` не менялся: формулы у него нет, дельта его не трогает.

## Реестр долга закрыт, но проверка осталась

`tests/design-contract.test.ts`:

- «bloomAR выведен из w/h по формуле пакета» — **снята оговорка
  `if (zone.rectOld) continue`**: теперь правило проверяется на всех 130 без
  исключений;
- «ДОЛГ ДИЗАЙНУ: у 45 зон bloomAR остался от прежнего прямоугольника» →
  «**ДОЛГ ЗАКРЫТ: отставших не осталось ни одного**». Проверка не удалена, а
  обнулена: следующая переразметка, забывшая пятно, упадёт здесь и назовёт зону
  и оба числа. История долга (46 → 45 → 44 → 0) оставлена в комментарии —
  вместе с `emerald/home`, из-за которой он и был виден: прямоугольник из
  высокого стал широким, а свет остался лежать поперёк предмета;
- **новый тест «формула записана в самом контракте»**: числа зажима и множитель
  РАЗБИРАЮТСЯ из строки `bloomRule` и по ним пересчитываются все 130. Изменит
  дизайн формулу — тест посчитает по новой и покажет расхождение, а не
  промолчит;
- счётчик `rectOld` (66) и сверка с копией пакета раунда 40 не тронуты;
  пункт «bloomAR не взят из копии 40» переписан: он закрылся, но расхождение с
  той копией не исчезло, а выросло (61 зона) — «синхронизировать» файл целиком
  по-прежнему нельзя.

Сторож проверен подменой: `bold/music` 96 → 95 роняет три теста поимённо.

## Прогоны

- `npx vitest run tests/empty-places.test.ts tests/design-contract.test.ts
  tests/immersive-layout.test.ts` — **138 из 138**;
- `npm run lint` — 0 ошибок (2 старых предупреждения в `scripts/`, не наши);
- `npm run typecheck` — чисто.

## Глазами (стенд, `/dev-login?fresh=1` + онбординг, кремовая)

Скриншотом не проверить — панель браузера не композитит кадры; мерян DOM.
`--zone-bloom-bg` кнопок зоны кремовой отдаёт новые числа: «Книги» 77 (было
120), «Для дома» 112 (было 67), «Музыка» 83 (было 89), «Цветы» 44 (было 46).
Непочиненные пустышки на месте: «Красота и уход» 77, «Просто деньги» 40.
