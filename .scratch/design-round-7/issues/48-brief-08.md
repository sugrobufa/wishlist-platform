# 48 — Задание дизайну на раунд 8: доснять комнаты до конца

**What to build:** Владелец решил: раунд 7 возвращаем, комнаты доделываем до
конца. Нужно исчерпывающее задание — такое, после которого пересъёмка сработает
с первого раза, а не с четвёртого.

**Blocked by:** 46, 47, 49 — все закрыты. Числа аудита 49: из 30
подключённых кадров честных **7**; отключены 23 — **17 подмен** предмета или
окружения, **3 пропажи** содержимого, **3 «только свет»** при глаголе,
обещающем движение. У 100 из 118 непрошедших плывёт и кольцо вокруг зоны.

**Status:** done

## Контекст, без которого задание не написать

Прочитать целиком, прежде чем писать хоть строку:

- `.scratch/design-round-7/issues/46-accept-package.md` — весь разбор приёмки
  с числами: 12 кадров из 130, поимённый список 118 непрошедших, диагнозы;
- `design/package/handoff/reshoot-recipe.md` — **его собственный рецепт**, где
  прямо написано и про «фон плывёт», и про мягкие формулировки;
- `design/DESIGN-BRIEF-07.md` — прошлое задание, чтобы не повторять тон и не
  просить дважды;
- `design/package/CLAUDE.md` — инварианты пакета и формула промпта;
- `.scratch/STATE.md` — долги и «чему не верить».

## Главное, что надо донести

**Раунд 7 не сработал, и причина известна и записана у него же.** Из 130
кадров порог прошли 12, из них 11 — уже подключённые (8 байт в байт наши же
файлы раунда 3). Новый годный кадр один: `study/events`.

Диагноз у 110 из 118 провалов — **«фон плывёт»**: среднее расхождение по
чужим прямоугольникам 0.19…0.25 при требуемой локальности 3×. Это ровно та
болезнь, которую `reshoot-recipe.md` описывает словами «описание сцены
заставляет модель нарисовать её заново: так провалились 41 кадр раунда 3».
У восьми других диагноз обратный — **зона не изменилась вовсе**, это вторая
болезнь из того же рецепта («мягкая формулировка даёт кадр без изменений»).

Тон: без упрёка, но и без смягчения. Он потратил раунд, и повторять это
никому не выгодно. Числа говорят сами.

## Что задание обязано содержать

1. **Сколько именно кадров нужно.** Подключено 30 зон из 130. Считать самим по
   `design/package/handoff/rooms.json` на диске, а не по памяти.
2. **Поимённый список** с числами «своя / фон / отношение» и диагнозом на
   каждый — чтобы он видел не «переснимите всё», а конкретную зону и конкретную
   причину. Брать из тикета 46, не пересчитывать.
3. **Порог приёмки** дословно: своя зона ≥ 0.05 И отношение к фону ≥ 3.
   И честно: **мы теперь проверяем каждый кадр сами**, у нас есть
   `scripts/check-frames.mjs`. Предложить работать пачками по одной комнате:
   он присылает 13 кадров — мы за минуты возвращаем таблицу с вердиктами.
   Это снимает его же ограничение («считать 130 пар мне нечем»).
4. **Что делать иначе, чем в раунде 7.** Опереться на его рецепт, а не на наши
   выдумки: базовый кадр на вход, менять один предмет, запрет `Everything else
   must remain pixel-identical. Do not relight.`, жест физический
   (`the drawer is pulled fully OUT`), материалы и свет НЕ описывать.

5. **Отдельным разделом и строгим запретом — три фокуса** (числа и поимённый
   список даст тикет 49, брать оттуда):

   - **подменять предмет.** В «Изумруде» на нажатие шкатулки открывается сцена
     с косметическими палетками: ни шкатулки, ни жемчуга, ни зелёной панели.
     Это не раскрытие, это другая картинка;
   - **терять содержимое.** В «Кремовой» на нажатие шкафа исчезает половина
     одежды со штанги. Человек видит не «шкаф открылся», а «мои вещи пропали»;
   - **подменять действие.** У каждой зоны в контракте записан `openVerb` —
     что именно происходит: «створки расходятся», «шкатулка поднимает крышку»,
     «выдвигаются полки». Кадр обязан делать **это**, а не что-то другое.

   Формулировать как правило, а не как жалобу: **на кадре „открыто" тот же
   предмет, с тем же содержимым, в том состоянии, которое назвал `openVerb`.
   Всё остальное — пиксель в пиксель.**

6. **Про динамику — снять с него подозрение и вернуть ответственность.**
   Владелец говорит: «происходит просто подход к месту, а закладывалась
   динамика». Проверено: механизм у нас работает — сцена держит слой второго
   кадра и включает его на наезде, длительности из `motion.json`. Играть
   нечего: кадр есть у 30 зон из 130, и часть из этих 30 играет не то.
   Написать это прямо: движение появится ровно тогда, когда придут кадры,
   делающие обещанное.
7. **Одиннадцать прямоугольников** из `design/rects-fix.json` (тикет 47) —
   приложением, с оговоркой «место назвали вы, числа наши, как договаривались».
   Если тикет 47 для какой-то зоны честно ответил «предмета нет» — так и
   написать, и спросить, убирать ли зону из комнаты.
8. **Оригиналы.** `masters.json` не менялся с раунда 3 — ссылки ведут на старые
   кадры. Раунд 8 обязан прийти со свежим `masters.json`, иначе у нас 2400 px
   против наших 2800, и подключать нечего.
9. **Флаги.** В раунде 7 `accepted: 33` стояло при 130 снятых кадрах, а письмо
   говорило «стоит на каждой зоне». Попросить: либо флаги выводятся правилом и
   правило работает, либо флагов в пакете нет вовсе — мы всё равно считаем сами.
   Так честнее для обоих.
10. **Что НЕ переснимать.** 30 подключённых кадров трогать не надо. Перечислить
   их поимённо, иначе он снова переснимет всё и уронит то, что работает.


11. **Мужские комнаты — отдельным разделом, их четыре из десяти.** Владелец
    спросил прямо: «ты видишь, что там другой список подарков?» — и был прав,
    в задании этого не было бы.

    Набор зон у мужских комнат другой. Шесть ключей существуют **только**
    там, и кадры женского набора про них не говорят ничего:

    | зона | подпись | глагол раскрытия | комнат | с кадром |
    |---|---|---|---|---|
    | `tech` | Гаджеты | ящик выезжает | 4 | **0** |
    | `watches` | Часы | крышка поднимается | 4 | **0** |
    | `gaming` | Игры | полка подсвечивается | 4 | 1 |
    | `sport` | Спорт | стойка подсвечивается | 4 | 1 |
    | `grooming` | Уход | тюбики выходят на свет | 4 | 1 |
    | `sneakers` | Обувь и рюкзаки | витрина открывается | 4 | 3 |

    Итого у мужских комнат 14 кадров из 52, у женских 16 из 78. «Гаджеты» и
    «Часы» не раскрываются **ни в одной** комнате — восемь зон без единого
    кадра. Это надо назвать числом, иначе он снова снимет то, что заметнее.

12. **Пул «Уход» — тот самый другой список подарков.** В справочнике 18 пулов
    демо-вещей, и `grooming` среди них **нет**: зона «Уход» во всех четырёх
    мужских комнатах берёт пул `perfume`, а в нём «Свеча с инжиром» и
    «Диффузор для дома». Мужчина открывает «Уход» и видит домашнюю свечу.

    Попросить девятнадцатый пул — `grooming`, четыре-пять вещей мужского
    ухода, в том же формате, что остальные (`items.json` → `demoPools`).
    Это не косметика: демо-вещи — первое, что видит человек в пустой комнате.


13. **Порядок работы партиями — отдельным разделом, это решение владельца.**

    Причина не в объёме как таковом, а в том, что весь раунд 7 он работал
    вслепую: проверить кадр ему нечем, и системная ошибка промпта размножилась
    на 130 кадров разом. Объём определяет цену одной ошибки. Пришли он одну
    комнату — вердикт «прошло 0 из 13» вернулся бы через минуты.

    Предлагаемый порядок (формулировать как совместный, а не как надзор):

    - **пробник из трёх кадров** на одной комнате, с разными механиками:
      где что-то выдвигается, где поднимается крышка, где загорается свет.
      Три кадра проверяют формулу на всех трёх типах действия сразу;
    - пробник прошёл — **по одной комнате за раз** (13 кадров, один базовый
      на всех). Это естественная единица: и модель так работает, и наш
      привратник по композиции судит комнату целиком;
    - две-три комнаты подряд без брака — можно вести две параллельно.

    Мельче комнаты дробить не стоит: владелец посередине каждого круга, и
    пересылка съест больше, чем сэкономит.

    **Что даём взамен:** партия пришла — возвращаем таблицу с вердиктом по
    каждому кадру за минуты (`scripts/check-frames.mjs`). Это снимает его
    ограничение, а не перекладывает работу.

    **Плюс проверка, которую он делает сам, глазами, за секунды** — три
    вопроса к своей же паре кадров перед отправкой:

    > Тот же предмет? То же содержимое? Делает ли он то, что обещал `openVerb`?

    Это те же три запрета, только со стороны отправителя. На «Изумруде» и
    «Кремовой» он ответил бы «нет» сам, не отправляя.

    Оговорить честно: короткие партии не чинят метод — если формула неверна,
    десять маленьких провалятся так же, как одна большая. Они делают провал
    дешёвым и видимым сразу. Лечит метод, то есть пункты 4 и 5.


14. **Маски силуэтов — на все 130 зон, решение владельца.** Сейчас зона
    подсвечивается мягким пятном в границах прямоугольника (это чиним у себя,
    тикет 50 — к дизайну не относится). Целевое состояние — свечение по
    **силуэту предмета**: шкаф светится как шкаф, а не как овал.

    Правила, оба жёсткие:

    - **на все зоны, без деления на главные и неглавные.** Дословно от
      владельца: «это странный пользовательский путь — часть светится, а
      часть нет»;
    - **переключение без смешения**: комната переходит на силуэтное свечение
      только когда готовы все её 13 масок; до тех пор вся комната светится
      нынешним способом. Два языка меток на одном экране не встречаются.

    Формат маски согласовать с ним же (PNG-альфа поверх кадра 630×351 —
    предложить, но пусть скажет, чем ему удобнее отдавать). Маска — не
    изображение комнаты: запрет на перегенерацию кадров её не касается.
    Партиями по комнате, как и кадры.

## Осторожно

- **Не изобретать за него метод.** Всё, что мы просим, уже написано в его
  `reshoot-recipe.md` — задание должно ссылаться на рецепт, а не переписывать.
- **Не обещать того, чего нет.** Про иконки, экраны и прочие хвосты — только
  то, что действительно ждём; проверить по `docs/design-board-gaps.md`.
- Владелец — продакт, не разработчик: задание читает и он тоже. Никакого
  жаргона, числа с пояснением, что они значат.

## Критерии

- [ ] `design/DESIGN-BRIEF-08.md` написан целиком, на русском, без жаргона
- [ ] Числа взяты с диска и из тикета 46, не по памяти
- [ ] Поимённый список к пересъёмке с диагнозом на каждую зону
- [ ] Поимённый список того, что НЕ трогать
- [ ] Порог и предложение проверять пачками
- [ ] Приложение про 11 прямоугольников со ссылкой на `design/rects-fix.json`
- [ ] Требование свежего `masters.json` объяснено числами (2400 против 2800)
- [ ] Просьба про флаги
- [ ] Раздел про мужские комнаты: шесть своих зон, `tech` и `watches` без
      единого кадра, счёт 14 из 52 против 16 из 78
- [ ] Просьба про девятнадцатый пул `grooming`
- [ ] Порядок работы партиями: пробник из трёх, дальше по комнате;
      что возвращаем вердикты за минуты; три вопроса для самопроверки
- [ ] Маски силуэтов: все 130, переключение покомнатно без смешения
- [ ] Числа тикета 49 в разделе запретов (17 подмен, 3 пропажи, 3 «только свет»)
- [ ] Три запрета (подмена предмета, пропажа содержимого, подмена действия)
      отдельным разделом, с числами из тикета 49
- [ ] Про динамику сказано прямо: механизм работает, играть нечего
- [ ] В конце — короткий список «ваша очередь», как в брифе 07

## Территория

`design/DESIGN-BRIEF-08.md` (новый).
**Не трогать:** `design/package/**`, `design/rects-fix.json` (его пишет
тикет 47), `src/**`, `messages/**`.

## Comments

### Итог (2026-08-06)

`design/DESIGN-BRIEF-08.md` написан целиком: 14 разделов (0–13) плюс чек-лист
«Итого ваша очередь». Все 14 пунктов тикета вошли. По уточнению владельца,
пришедшему в работу («мы предлагаем ему сделать часть, мы проверяем и потом
постепенно всё делаем»), подача перестроена: заказ письмом — только первый шаг
(«да/нет» по 11 прямоугольникам словами · пробник из трёх кадров «Кремовой» ·
пробник формата масок на две-три маски · пул «Уход» · masters.json с каждой
партией), полный объём назван горизонтом и в заказ не входит. Проверил текст:
заказа полного объёма одним махом нигде нет.

### Что вошло, по пунктам тикета

1. Счёт кадров — с диска (7, а не 30; см. расхождения ниже).
2. Поимённый список к пересъёмке — 115 строк в десяти покомнатных таблицах,
   на каждой зоне диагноз: 17 подмен + 3 пропажи + 3 «без действия» (вердикты
   тикета 49 словами) + 89 «фон плывёт» + 3 «без действия» с числами
   «своя/фон/отношение» из таблицы тикета 46. Разбиение сверено скриптом:
   23 + 92 = 115, покрытие полное, пересечений нет.
3. Порог дословно (своя ≥ 0.05 И отношение ≥ 3), «проверяем каждый кадр сами
   за минуты», кольцо и глаза как два новых сигнала.
4. Метод — ссылкой на его же `reshoot-recipe.md` с пересказом четырёх правил,
   без выдумок; плюс два требования из приёмки (композиция базы: `emerald`
   0.0585 / `cottage` 0.0619 / `loft` 0.0719 выпали целиком; порог гоняем мы).
5. Три запрета отдельным разделом, правилом, с числами 49 (17/3/3), примерами
   «Изумруд»/«Кремовая» и контрпримерами честных кадров.
6. Динамика: механизм работает (слой второго кадра, длительности из
   `motion.json`), играть нечего — кадров 7 из 130.
7. Одиннадцать прямоугольников приложением `design/rects-fix.json`: «место
   назвали вы, числа наши», сводная таблица всех 11, две зоны «скрыты зря»
   (`warm/music` — проигрыватель, `sport/watches` — часовница, пометки
   «предмета нет» снимаем), три вопроса-оговорки с «если нет — убираем ли
   зону» (`emerald/beauty` — лампа на названной поверхности, `sport/gaming` —
   клавиатура вместо предмета, `loft/gaming` — винил в ящике). Первый шаг —
   подтвердить словами, не снимать.
8. Свежий `masters.json`: 144 ссылки раунда 3, оригиналов раунда 7 нет,
   2400 против наших 2800 — с каждой партией ссылки на оригиналы 5504.
9. Флаги: письмо «на каждой зоне» против 33 в файле, правило пакета разошлось
   с его данными на 97 зонах; просьба «либо правило исполняется, либо флагов
   нет» + его же обещание из рецепта («непроверенные молча больше не буду»)
   названо фактом, без упрёка.
10. Что НЕ трогать — семь поимённо с «что на кадре».
11. Мужские комнаты отдельным разделом: шесть своих ключей таблицей с
    глаголами из `zones.json`, `tech` и `watches` — 0 кадров за всю историю
    (восемь зон), счёт с диска 6/52 против 1/78.
12. Пул `grooming` девятнадцатым: 18 пулов на диске, `grooming` нет, все
    четыре зоны «Уход» смотрят в `perfume` («Свеча с инжиром», «Диффузор для
    дома» — проверено и в пакете, и в `src/config/demo-pools.ts`); просьба
    4–5 вещей + строка справочника `zones.json`.
13. Партии: пробник из трёх кадров с разными механиками (предложена
    «Кремовая»: `fashion` створки / `jewelry` крышка / `home` свет — три
    механики и три запрета разом, прямоугольники этих зон стабильны, комната
    владельца) → комната за раз → две параллельно; вердикты за минуты; три
    вопроса самопроверки цитатой; оговорка «партии не чинят метод».
14. Маски: на все 130 без деления (цитата владельца), переключение покомнатно
    без смешения, формат PNG-альфа 630×351 предложен вопросом («чем удобнее —
    скажите»), «маска — не изображение комнаты», первая партия — две-три маски
    как пробник формата.

### Числа, проверенные с диска (`design/package/handoff/*` + `refs/`)

- `rooms.json`: 130 зон = **7** `openFrame` (cream/travel, gamer/travel,
  sport/anything, sport/sneakers, sport/travel, study/events, study/sport)
  + **115** `reshoot` + **8** `objectAbsent`. По комнатам к пересъёмке:
  cream 12, warm 12, lux 12, emerald 12, bold 13, cottage 13, gamer 12,
  sport 8, study 9, loft 12.
- Пары `sharedObjectWith`: warm/beauty↔perfume, lux/beauty↔perfume (обе в
  пересъёмке → 115 зон = 113 съёмок), cream/money↔anything — разъезжается
  приложением (тикет 47).
- Мужские: 6 кадров из 52; женские 1 из 78. По ключам: tech 0/4, watches 0/4,
  gaming 0/4, sport 1/4 (study), grooming 0/4, sneakers 1/4 (sport).
- `items.json` → `demoPools.poolKeys`: **18** пулов, `grooming` отсутствует;
  `zones.json` ключ `grooming` сам ссылается на пул `perfume`.
- `masters.json`: **144** ссылки, шапка «оригиналы кадров раунда 3»,
  master 5504×3072, local 2400.
- Ширины кадров: `refs/v4-cream.jpg` 2800×1563, `refs/o-study-events.jpg`
  2400×1340 (единственный кадр раунда 7). В `refs/` ровно 7 файлов `o-*` —
  совпадают с семью честными.
- `rooms.json → identityCheck`: 30 = 7 + 17 + 3 + 3, `ringMax` 0.09;
  `round7`: 130 снято, 12 прошло, 1 подключён, правило разошлось на 97 зонах.

### Расхождения тикетов с диском и внутри тикетов

1. **Тикет 48 писал счёт до тикета 49**: «подключено 30 из 130», мужская
   таблица «gaming 1, sport 1, grooming 1, sneakers 3», критерий «счёт 14 из
   52 против 16 из 78». Диск после 49: подключено 7; gaming 0, grooming 0,
   sneakers 1; счёт 6/52 против 1/78. В бриф взят диск — сам тикет требует
   «считать самим по rooms.json на диске». «Восемь зон без единого кадра»
   (tech+watches) осталось верным; к ним добавились «Игры» и «Уход» — в брифе
   это названо отдельно («кадры имели, оба оказались подменами»).
2. **Проза тикетов 46 и 48 против поимённой таблицы тикета 46**: проза говорит
   «110 фон плывёт / 8 зона не изменилась», таблица 118 строк даёт **108 / 10**
   (у всех десяти «своя < 0.05» — критерию прозы удовлетворяют тоже все десять).
   В бриф взята таблица: бриф сам содержит поимённый список, и его итоги
   обязаны сходиться с его же строками. Соотношение 12 + 118 = 130 не задето.
3. **Тикет 46, вводная**: «masters.json… записей 149» — на диске 144 (и
   Comments того же тикета говорят 144). В брифе 144.
4. Мелочь: «CLAUDE.md» пакета в тикете назван `design/package/CLAUDE.md` —
   в репозитории файл лежит как `design/package/DESIGN-CLAUDE.md` (прочитан).

### Хвосты: сверено не только с design-board-gaps

`docs/design-board-gaps.md` датирован 05.08 — до прихода раунда 7. Проверил
доску пакета (`Main Screen.dc.html` в присланной папке): в раунде 7 есть
**турн 25** — 25a иконки + три состояния таб-бара, 25b карточка вещи глазами
гостя и хозяйки, 25c пустая зона и пустая комната, 25d строки зонного счётчика
и складчины, плюс `phoneFieldRemoved` (телефон убран из макетов). То есть
«ваша очередь» брифа 07 закрыта им почти целиком — просить это заново нельзя.
В брифе: «получили, читаем, вердикты отдельным письмом»; не нарисованными
названы только «зона целиком списком», «мои брони», «письмо-напоминание»
(и те — «после комнат, сейчас не в заказе»).

### Осталось спорным / за рамками тикета

- **Приёмка турна 25 не заведена**: бриф обещает «вердикты отдельным письмом» —
  нужен тикет. Строк 25d в `messages-ru.json` пакета нет («свободно» /
  «складчина» в файле отсутствуют) — строки живут только на доске, при приёмке
  их придётся переносить руками.
- **Формат масок** — предложение (PNG-альфа 630×351), решение оставлено
  дизайнеру; в брифе прямо спрошено. Как маски лягут в код (тикет 50 — наша
  сторона) — не в этом письме.
- **Ответы на три вопроса раздела 6** определят судьбу зон `emerald/beauty`,
  `sport/gaming`, `loft/gaming`: горизонт «до 123 зон с кадром» может
  уменьшиться — в брифе так и сказано («если нет — убираем ли зону»).
- **Пробник назначен на «Кремовую»** (fashion/jewelry/home) моим выбором по
  критериям тикета (три механики, стабильные прямоугольники, комната
  владельца); если владелец хочет другую комнату — правится одной строкой.
- Числа «110/8» из шапки этого тикета в бриф не попали (см. расхождение 2) —
  если где-то ещё цитируется проза 46, стоит держать в уме 108/10.
