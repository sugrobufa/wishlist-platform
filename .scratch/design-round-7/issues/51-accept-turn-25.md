# 51 — Приёмка турна 25: иконки, таб-бар, карточки, пустые состояния, строки

**What to build:** Вместе с кадрами раунда 7 пришёл турн 25 доски. Кадры мы
приняли (тикеты 46/49), а турн — нет: он лежит в папке пакета и в репозиторий
не попал. Бриф раунда 8 обещает дизайну «вердикты по турну 25 отдельным
письмом» — это письмо и есть главный выход тикета. Владелец спросил «а ты
применил иконки и таб-бары?» — ответ был «нет».

**Blocked by:** 46 (закрыт)

**Status:** done

## Что где лежит

- Новая доска: `C:\Wishlist\Wishlist платформа главный экран final\Main Screen.dc.html`
  (691 КБ против 606 КБ у нашей — турны неизменяемы, новое сверху).
- В репозитории доска старая, турна 25 в ней нет (проверено поиском).
- Содержимое турна: **25a** иконки · сетка 24, контур 1.7 · три состояния
  таб-бара («Комната», «Друзья», …); **25b** карточка вещи глазами гостя и
  хозяйки; **25c** пустая зона и пустая комната; **25d** строки — зонный
  счётчик гостя и складчина.

## Что сделать

### 1. Принять доску в репозиторий

`Main Screen.dc.html` из пакета → `design/package/`. Перед копированием
сверить, что старые турны не переписаны: правило доски — «турны неизменяемы,
новая работа — новый турн сверху» (исключение по её же правилам — правка
механизма на месте; если такая есть, выписать в Comments что изменилось).
Заодно проверить `support.js` и `github.md` — если менялись, забрать вместе.

### 2. Прочитать турн 25 целиком и вынести вердикты по блокам

На каждый блок — вердикт в трёх колонках: «берём как есть / берём с
оговоркой / против инварианта, не берём». Это сырьё для письма дизайну.

**25a, иконки и таб-бар.** Сверить с правилом пакета (сетка 24, контур 1.7,
«глифов из шрифта быть не должно»). Наши рукописные иконки (глаз в
`price-seen-badge.tsx`, стрелки, крестики по `src/**`) — сверить с набором:
где в 25a есть канонический вариант, наш подлежит замене (в этом тикете —
только выписать список замен, менять будет тикет 52).

**25b, карточки вещи.** У нас обе карточки уже построены (тикет 39 — хозяйка,
диалог брони — гость). Сверить поле за полем, расхождения — в реестр
`docs/design-board-gaps.md` тем же форматом, что тикет 36.

**25c, пустые состояния.** Сверить с тем, что рисуем сейчас (у новичка —
призраки-примеры, `items.demo`). Расхождения — в реестр.

**25d, строки.** САМОЕ ОПАСНОЕ МЕСТО. Проверить каждую строку против
инвариантов №1 и №2: зонный счётчик гостя («занято/свободно») гостю
разрешён, но любая строка, раскрывающая ИМЯ гостя другим гостям до праздника
вне складчины, — против инварианта №2 (вопрос В2 реестра, он ещё открыт).
Строки складчины сверить с ADR-0008 (копилка тиха для хозяйки). Что проходит
инварианты — добавить в `messages/ru.json` по памятке тона (`messages/**`
сейчас никто не трогает); что не проходит — в письмо, с объяснением почему.

### 3. Написать письмо дизайну

`design/ANSWERS-turn-25.md`: вердикты по 25a–25d, поимённо. Тон как в брифе
08 — числа и факты, без смягчения и без упрёка. Письмо короткое: турн в
основном хороший (наша же оценка в брифе — «закрывает почти всю вашу
очередь»), поэтому большая его часть — «берём», и это надо сказать явно.

### 4. Завести долг на внедрение

Внедрение таб-бара и замена иконок — НЕ этот тикет (новая поверхность UI с
жестом «по тяге снизу», отдельная работа). Этот тикет заканчивается приёмкой:
доска в репозитории, вердикты в письме, строки в словаре, список замен иконок
выписан.

## Осторожно

- **Доска — файл дизайна.** Мы её принимаем целиком или не принимаем; править
  внутри нельзя.
- **Инварианты выше доски** (`docs/design-board-gaps.md`, раздел 7). Никакую
  строку 25d не вносить в словарь, если она спорит с №1/№2, — даже «временно».
- `messages/**`: добавлять только свои ключи, тест тона обязан остаться
  зелёным (`tests/messages-tone.test.ts` — новые ключи внести в его список).
- Таб-бар в этом тикете не строить. Иконки не менять. Только приёмка.

## Критерии

- [x] Доска турна 25 в `design/package/`, старые турны не переписаны (проверено)
- [x] Вердикты по 25a–25d тремя колонками в Comments
- [x] Каждая строка 25d проверена против инвариантов №1/№2 поимённо
- [x] Прошедшие строки в `messages/{ru,en}.json`, тест тона зелёный
- [x] Список замен иконок выписан (наша → каноническая из 25a)
- [x] `design/ANSWERS-turn-25.md` написан
- [x] Расхождения 25b/25c в `docs/design-board-gaps.md`
- [x] `npm run typecheck && npm run lint && npm test` — зелёные

## Территория

`design/package/Main Screen.dc.html` (+ `support.js`, `github.md` если
менялись), `design/ANSWERS-turn-25.md` (новый), `messages/**`,
`tests/messages-tone.test.ts`, `docs/design-board-gaps.md`, файл тикета.
**Не трогать:** `src/**` (внедрение — тикет 52), `design/package/refs/**`,
`design/package/handoff/**`, `prisma/**`, `design/DESIGN-BRIEF-08.md`.

## Comments

### Доска принята целиком, старые турны проверены

Файл 749 709 байт (вырос с 691 КБ из шапки тикета — дизайн дописал поставку
после того, как тикет был заведён) скопирован в
`design/package/Main Screen.dc.html` без правок, md5 совпадает с источником.
`support.js` и `github.md` не менялись (md5 равны) — не копировались.

Проверка неизменности посекционно (скриптом, байт в байт):

- разделы **6, 8–22** — идентичны;
- **прежний 23 стал 24**: содержимое совпадает байт в байт, внутренние якоря
  остались `23a–23c` (наши ссылки уцелели). В середину встал **новый раздел
  23** «Телефонный главный экран… по брифу 04» — это визуал решений
  `handoff/answers-04.md`, которые у нас уже воплощены (сводка, «Подойти
  ближе», агрегаты, инвариант №8). Сверху добавлен **25**;
- правки на месте (исключение по правилам доски, выписываю):
  1) **13b** — убрано поле «Телефон — необязательно · для смс о подарках»,
     строка способов входа «Google, Telegram, телефон» → «Google, Telegram»;
  2) **19** — абзац «Что поменялось в укреплении» переписан: «поле телефона
     убрано целиком» вместо «переехало в необязательное». Обе правки — отзыв
     решения, о котором мы спрашивали (В5 реестра), в словаре поставки
     задокументирован как `phoneFieldRemoved`;
  3) **механизм прототипа** (скрипт после последней секции) — одна строка:
     фолбэк-подпись `rVerb` «наезд есть, кадра «открыто» ещё нет» → пустая
     строка (убирает технические слова из живого прототипа, по их же памятке).
- дубль якоря: `id="23a"` теперь и в разделе 24, и в новом 23 — браузер ведёт
  на первый по документу (24, прежний смысл), старые ссылки работают; дизайну
  написано убрать дубль.

### Вердикты по блокам (три колонки)

| Блок | Берём как есть | Берём с оговоркой | Против инварианта, не берём |
|---|---|---|---|
| **25a** иконки + таб-бар | 17 из 18 иконок (сетка 24, контур 1.7, скруглённые концы, глифов из шрифта нет — проверено по каждому svg); три состояния таб-бара с числами | отклонения толщины 1.6/1.8/1.9/2.1 у четырёх знаков — читаем как оптическую компенсацию, спрошено; вкладка **«Профиль»** — слово из столбца «не говорим» их же памятки, в словарь не пройдёт (тест тона), запрошена замена | — |
| **25b** карточки вещи | обе карточки: инварианты №1/№2/№3 соблюдены в самих макетах (замочек-обещание у гостя, «узнаешь после праздника» у хозяйки, «хочу» пунктиром); бирка осталась единственным действием «подарить» (правило турна 22 цело — «Скинуться» не бирка) | подпись «если её уже забрали — предупредим гостя» показывать **безусловно**, иначе она сама — оракул брони; «доставка 5–7 дней» не рисуем (данных нет); «Скинуться» — Phase 2 | — |
| **25c** пустые состояния | разделение «комната = начало, полка = выбор», тексты, три места вместо сетки, «убрать полку» из зоны | пунктир на пустых местах = язык «место готово» — с инвариантом №3 не спорит (он про «люблю» без фото), зафиксировано; порог «5 вещей до шера» — новое правило, в реестр; расхождение с демо-призраками — решать тикетом внедрения | — |
| **25d** строки | зонный счётчик гостя — все 7 строк; складчина — прогресс и исходы (7 строк) | «скинуться · {suggest}» — сумма читается как подсказка доли, не перевод; кнопок перевода не будет (ADR-0008) | «в складчине Аня» ×4 (имена гостей до праздника — вопрос В4 открыт, действует инвариант-дефолт); «не собрали — деньги вернулись» (PRD §12а: деньги мимо сервиса, возвращать нечего) |

### 25d поимённо — против инвариантов №1/№2

Зонный счётчик (`zoneCounterGuest`, только гостю — сам дизайн пишет «хозяйке
по зонам не показывается ничего»):

| Строка | №1 | №2 | Вердикт |
|---|---|---|---|
| «все {m} свободны» | ок (гостю можно занято/свободно) | ок (имён нет) | в словарь — `Scene.summaryFreeAll` |
| «{n} из {m} свободны» | ок | ок | в словарь — `Scene.summaryFreeSome` (нюанс: n=21 требует форму «свободна» — в письме) |
| «1 из {m} свободна» | ок | ок | в словарь — `Scene.summaryFreeLast` |
| «все {m} уже дарят» | ок | ок | в словарь — `Scene.summaryFreeNone` |
| «вещь одна и свободна» | ок | ок | в словарь — `Scene.summaryFreeSingle` |
| «пока пусто» | ок | ок | дубль `Scene.summaryEmpty` — не вносился |
| хозяйке «{m} вещей» | **это и есть №1** (у хозяйки то же место — количество, «7 свободно» не просочится) | ок | уже живёт (`Scene.summaryCountsGuest` / `ZoneGrid.zoneCounts`) |

Складчина (сверено с ADR-0008: копилка «ведёт себя как складчина» — прогресс
только гостям, хозяйке одна вещь в общем счётчике, имена — только на экране
«что подарили»):

| Строка | Проверка | Вердикт |
|---|---|---|
| «в складчине {a}» | имя гостя другим гостям до праздника; ADR-0008-канон: «N человек уже скинулись» **без имён**; вопрос В4 (сужен поставкой) открыт | **не взято** до решения владельца |
| «в складчине {a} и {b}» | то же | **не взято** |
| «в складчине {a}, {b} и ещё {rest}» | то же | **не взято** |
| «ты, {a} и ещё {rest}» | «ты» — законно, имена соседей — нет | **не взято** |
| «собрали {have} из {need}» | прогресс гостям разрешён (ADR-0008), №1/№2 ок | в словарь — `Booking.poolProgress` |
| «осталось {days}» | ок | `Booking.poolDaysLeft` |
| «последний день» | ок | `Booking.poolLastDay` |
| «не хватает {gap}» | ок | `Booking.poolShort` |
| «скинуться» / «скинуться · {suggest}» | не платёж (координация); сумм-кнопок перевода не будет | `Booking.poolJoin` / `poolJoinAmount` |
| «собрано, ждём праздника» | ок | `Booking.poolFull` |
| «не собрали — деньги вернулись, вещь снова свободна» | «деньги вернулись» = сервис держал деньги; PRD §12а запрещает; «вещь снова свободна» — верно, но строку целиком не режем сами (дословно или в письмо) | **не взято**, в письме предложена редакция без «денег» |
| хозяйке — «ни строки · складчина идёт за одну вещь в общем счётчике» | дословно инвариант №1 | уже так; строк не требует |

Механизм складчины — Phase 2 (`POOL_NOT_SUPPORTED`), ключи лежат готовыми.
Итог по словарю: **12 ключей внесено** (5 счётчик + 7 складчина) в
`messages/ru.json` + `messages/en.json` (каркас), оба списка — в тест тона
(`tests/messages-tone.test.ts`, список сирот + комментарий). Тест зелёный,
30 проверок.

### Список замен иконок — сырьё для тикета 52

Наши рукописные svg против канона 25a (пути сверены посимвольно):

| Наша | Где | Канон | Действие 52 |
|---|---|---|---|
| галочка `M4.5 12.5l5 5 10-11`, stroke 3–3.2 | `occasion-client.tsx:37`, `money-goal-card.tsx:266`, `onboarding-flow.tsx:235`, `settings-sections.tsx:444,544` | «Дошло» — **путь совпадает**, канон 1.7 | свести в один компонент; толщина на размерах 11–13px — решить при внедрении (канон рисован под 22–30px) |
| глаз перечёркнутый | `price-seen-badge.tsx:36` | «Скрыть» — **совпадает дословно** | уже канон, вынести в общий набор |
| глаз открытый | `price-seen-badge.tsx:16`, `onboarding-flow.tsx:130` | канона в 25a **нет** (только перечёркнутый) | оставить наш, спросить дизайн |
| замок, stroke 1.8 | `r/[slug]/page.tsx:211` | «Приватность» — те же пути, 1.7 | заменить толщину |
| шер: три узла с рёбрами | `share-button.tsx:101` | «Поделиться» — стрелка из лотка, **другая метафора** | **заменить** на канон |
| человек | `connections-list.tsx:37` | «Профиль» — **совпадает дословно** | уже канон |
| звезда одиночная, 1.7 | `occasion/page.tsx:87` | 25b «Уже моё» — совпадает; для раздела зала в 25a — **двойная** звезда 1.6 | контекст «дошло/уже моё» — наша; навигация в зал — двойная |
| камера-рамка (галерея) | `add-item-flow.tsx:767` | в 25a нет; в турне 24 (23a) есть «Снять фото» и «Из галереи» другой отрисовки | заменить на вариант доски при внедрении |

Канонические без наших аналогов (появятся с таб-баром и карточками): Комната
(арка с тёплой точкой), Друзья, Добавить, Зал славы (двойная звезда), Подарок,
Складчина (часы), Деньги (₽ контуром — не глиф, отрисован путями), Напомнить,
Дата, Перенести, Удалить, Изменить, В магазин, стрелки навигации 1.8/1.9.
Типографские `→` в строках — канон турна 12a, не трогать.

### Что ушло в реестр (`docs/design-board-gaps.md`)

- раздел 2: примечание о сдвиге нумерации (наш «турн 23» = раздел 24 новой
  доски) и трёх правках на месте;
- раздел 5: новые строки **Б24** (карточка гостя), **Б25** (карточка хозяйки,
  включая правило «подпись у удаления — безусловно»), **Б26** (пустая комната,
  порог «5 вещей до шера»), **Б27** (пустая зона);
- раздел 7: **В4 сужен** (вне складчины доска отозвала имя сама; остаток —
  имена участников складчины друг другу, решение владельца), **В5 закрыт**
  (поле телефона убрано дизайном), **В6 — предпосылка снята** («взаимные»
  вместо ручного списка в новом CLAUDE пакета);
- раздел 8: инвентарь турнов 23 (новый)/24/25 и список того, что приехало с
  доской, но этим тикетом **не принято**: `handoff/messages-ru.json` (+30
  ключей, две секции трёхуровневые — тест тона в текущем виде упадёт при их
  лобовой приёмке, там же предупреждение), `handoff/rooms.json` (863
  изменения; наши `reshoot`/`rectsNotApplied`/`identityCheck` в версии дизайна
  стёрты — при приёмке не потерять), `refs-2x/` 140 кадров + `refs-630/` 10
  (пересъёмка — только через `scripts/check-frames.mjs`, флагу `accepted`
  пакета не верим со времён раунда 3), новый `CLAUDE.md` пакета.

### Письмо

`design/ANSWERS-turn-25.md` написано: принятие целиком сказано явно, вердикты
по 25a–25d, невзятые строки с причинами (В4-остаток, PRD §12а), вопрос про
«Профиль», рассинхрон `Booking.taken` («занято» в их словаре против «уже
дарят» в 25d), дубль якоря, падежный случай n=21.

### Проверки

`npm run typecheck` · `npm run lint` · `npm test` — зелёные (см. ниже),
dev-сервер не поднимался, git-команд не было, `src/**`, `handoff/**`,
`refs/**`, `prisma/**` не тронуты.
