# 50 — Метки зон делят комнату на прямоугольники

**What to build:** Владелец: «мне не нравятся эти прямоугольники, я хочу
подсветки блока. Сейчас это выглядит топорно и делит всю комнату на
прямоугольники». Это повтор пункта 3 первой приёмки — тогда убрали рамку,
но оставили тень, которая рисует ту же коробку.

**Blocked by:** —

**Status:** done

## Диагноз — ошибка наша, не дизайна

Контракт (`tokens.json` → `zoneMarker`) описывает метку светом и прямо
оговаривает: «края растворяются в прозрачность на 72% — границы пятна не
видно, поэтому оно не читается как фигура». Пятно (`bloom`) так и сделано.

Ломает картину **второй слой**, затемнение:

```
vignette.shape = radial-gradient(ellipse 78% 78% at 50% 50%, transparent 38%, {shadow} 100%)
```

Он рисуется на элементе с `inset: 0` внутри `.hotspot`, то есть **на
прямоугольнике зоны** (проверено замером DOM: `bloom`, `vignette` и `hotspot`
имеют одну коробку до пикселя). Эллипс доходит до тени на 100%, а углы
прямоугольника лежат ЗА эллипсом и остаются залиты тенью целиком. Получается
тёмный прямоугольник с тяжёлыми углами.

### Замер на живом стенде («Кремовая», зона «Одежда»)

```
vignette: radial-gradient(78% 78%, transparent 38%, rgba(24,14,6,0.867) 100%)
          коробка 170x391 (= прямоугольник зоны), opacity 0.34, blend normal
bloom:    radial-gradient(30% 62% at 50% 42%, #E7C9A9, transparent 72%)
          та же коробка, opacity 0.0385, blend screen
```

Отсюда видно две вещи, и вторая важнее первой.

**Эллипс затемнения — 78% от коробки.** Он не достаёт даже до середины
сторон, поэтому конечным цветом залиты не только углы, а полосы вдоль ВСЕХ
четырёх краёв. Это не артефакт на углах — это тёмная рамка, обрезанная строго
по прямоугольнику.

**Хуже всего — при выбранной зоне.** Скриншот владельца снят в состоянии
«зона открыта»: остальной кадр притемнён слоем `.dim`, и на его фоне метки
СОСЕДНИХ зон проступают в полную силу — светлые и тёмные коробки на гардеробе,
свечах, комоде. Владелец: «вот отлично видны прямоугольники при выборе».
Это состояние обязано войти в проверку наравне с покоем и наведением.

**На светлой комнате свечение практически выключено.** `bloomWeight` для
«Кремовой» (`roomLightness` 0.82) даёт 0.385, и в покое остаётся альфа
**0.0385** — невидимо. То есть от метки на светлой комнате остаётся ОДИН
тёмный прямоугольник, и это ровно то, что видит владелец.

Замысел контракта при этом не нарушен: `lightnessMix` прямо говорит, что на
светлой комнате меткой работает тень, а не свет. Ломает не идея, а то, что
тень у нас имеет прямые края.

## Что сделать

### А. Свет без прямых краёв — обязательная часть

Ни один слой метки не должен обрезаться прямоугольником. Варианты
исполнения на выбор, что чище ляжет:

- растянуть слой за пределы прямоугольника (`inset: -N%`) и завершить
  градиент внутри, оставив края прозрачными;
- либо мягкая альфа-маска на группе слоёв.

Проверка простая и обязательная: **на кадре не должно быть ни одной прямой
линии, порождённой меткой** — ни в покое, ни при наведении, ни при нажатии,
ни **при открытой зоне** (активен `.dim`; на его фоне сейчас проступают метки
соседних зон — состояние со скриншота владельца, худшее из четырёх).
Смотреть глазами на светлой комнате («Кремовая», худший случай) и на тёмной
(«Геймер», там вес тени минимальный, зато пятно максимальное).

Прямоугольник зоны остаётся невидимой областью нажатия — его не трогать.

### Б. В покое — только искра, свет по приближению

Комната стоит чистой: видна лишь пульсирующая точка. Свечение разгорается
у **ближайшей** зоны, когда указатель рядом; на телефоне — у зоны под пальцем
при касании-удержании, либо у той, что ближе к центру экрана.

Правило близости описать числом (радиус в единицах кадра), а не «на глаз»,
и вынести в `zone-marker.ts` рядом с остальной арифметикой метки.

Указатель зон внизу — второй путь, он остаётся и ничего не теряет.

### Чего НЕ делать

- **Не восстанавливать рамку** ни в каком виде, кроме фокуса с клавиатуры —
  там она законна и уже есть (`focusOutline`, пунктир).
- **Не трогать координаты зон** и `rooms.json`: это показ, а не разметка.
- Не просить у дизайна маски силуэтов — это отдельное решение владельца
  (вариант «В» в разборе), 130 масок, в этот тикет не входит.

## Осторожно

- `prefers-reduced-motion` по контракту: пульс искры и разгорание обязаны
  уважать его.
- Ворота hover — `(hover:hover) and (pointer:fine)`; на тач-устройствах
  «приближение» другое, и оно обязано работать.
- Контраст: искра — единственное, что видно в покое с расстояния. Не
  утопить её ниже порога различимости на светлой комнате.
- Цель нажатия 44 px не трогать — она про прямоугольник, а не про свет.
- `tests/zone-marker.test.ts` уже существует; расширить, а не переписать.

## Критерии

- [x] Ни одной прямой линии от метки: в покое, при наведении, при нажатии,
      при открытой зоне (dim активен)
- [x] Проверено глазами на светлой и тёмной комнате, скриншоты в Comments
- [x] В покое видна только искра; свет — по приближению, правило числом
- [x] Тач: приближение работает без hover
- [x] `prefers-reduced-motion` уважается
- [x] Область нажатия и координаты зон не изменились — тест
- [x] `npm run typecheck && npm run lint` — зелёные; vitest точечно
      (`zone-marker`, `immersive-layout`, `scene-camera` — 80 тестов), полный
      `npm test` не гонялся: параллельно работает агент тикета 49 на общей БД

## Территория

`src/components/scene/scene.module.css`, `src/components/scene/zone-marker.ts`,
`src/components/scene/zone-hotspot.tsx`, `src/components/scene/SceneStage.tsx`,
`tests/zone-marker.test.ts`.
**Не трогать:** `design/package/**`, `rooms.json`, координаты зон,
`src/server/**`, `messages/**`.

## Comments

**2026-08-06, агент тикета 50 — сделано.** Правка целиком в показе:
`zone-marker.ts`, `scene.module.css`, `SceneStage.tsx`, `zone-hotspot.tsx`
(комментарий), `zone-marker.test.ts` (+9 тестов). Координаты, rooms.json,
tokens.json, цель нажатия 44 px — не тронуты.

### А. Форма слоёв: старое → новое

**Виньетка.** Было (буква контракта):
`radial-gradient(ellipse 78% 78% at 50% 50%, transparent 38%, {shadow} 100%)`
на элементе `inset: 0` — тень доходит до конца луча, всё ЗА эллипсом залито
конечным цветом, прямоугольник зоны режет заливку по своим сторонам. Стало:
`radial-gradient(ellipse 50% 50% at 50% 50%, transparent 38%, {shadow} 72%, transparent 96%)`
на элементе `inset: -18%` (слой на 18% больше зоны, `VIGNETTE_BLEED_PCT`).
Тень стала мягким КОЛЬЦОМ: прозрачный центр (38% — по-прежнему парсится из
контрактной фигуры), пик ровно на границе зоны (72% полуоси = 0.5/0.68) и
растворение в ноль на 96% — до края слоя. Конечный цвет прозрачный, обрезать
градиент больше нечем ни при каком размере зоны.

**Пятно (bloom).** Фигура контракта не изменилась, но поверх добавлена мягкая
маска `radial-gradient(ellipse 50% 50% at 50% 42%, #000 55%, transparent 96%)`
(`markerMask()`): при `bloomAR` > ~69 (у «Геймера» половина зон, AR 120)
угасание на 72% луча не умещалось в коробке — свет резался прямой вертикальной
кромкой на ~42% альфы. Маска повторяет геометрию пятна (центр 42% сверху),
не трогает его середину и гаснет до края; поворот `bloomRot` вращает её вместе
со слоем.

**Искра, подпись, dim, рамка фокуса** — без изменений.

### Б. Правило близости — числом

`zone-marker.ts` → `PROXIMITY_RADIUS = 72` единицы кадра 630×351 (~11% ширины
кадра; ≈130–220 px на десктопе). В покое у всех слоёв света альфа 0
(`--zone-near: 0`); при подходе указателя ближе радиуса у БЛИЖАЙШЕЙ зоны
(расстояние до прямоугольника, не до центра) сила растёт линейно:
`1 − d/72`, внутри прямоугольника 1. Конечные значения равны hover-альфам
контракта, поэтому в момент входа в зону шва нет — `:hover` продолжает ту же
кривую. Движок — pointermove на сцене, раз в кадр (rAF), сила пишется
инлайн-переменной прямо в DOM (без ре-рендеров React).

Тач (`(hover: hover) and (pointer: fine)` ложно): свет стоит у зоны, чей
прямоугольник ближе к центру видимого окна, с силой `TOUCH_REST_STRENGTH
= 0.6`; касание-удержание отвечает press-состоянием (`:active`, 90 мс) — оно
не за воротами hover и работало и раньше. `prefers-reduced-motion`: пульс
искры уже был выключен, разгорание — это переход opacity, который контрактно
укорачивается до 120 мс; сам движок близости — прямой ответ на ввод, не
анимация.

### Отступления от буквы контракта (замысел сохранён)

1. `vignette.shape`: эллипс 78%→50%, конец `{shadow} 100%` → пик 72% +
   `transparent 96%`, слой `inset: -18%`. Причина — прямые края, см. выше.
   Цвет тени, вес в альфе (`lightnessMix`) и граница освещённого центра (38%)
   — из пакета.
2. `states.rest` («bloom .10 · vignette .34») в покое больше не рисуется:
   покой = искра, свет приходит по приближению и упирается в hover-альфы.
   `restAlpha` контракта в CSS не используются. Это прямое требование
   владельца («комната стоит чистой»), таблице состояний пакета нужна правка
   дизайна.
3. У пятна появилась маска — механизм, которого в контракте нет; фигура пятна
   при этом контрактная дословно.

### Скриншоты (Playwright против живого стенда :3200, вход /dev-login)

Десктоп 1280×800, «Кремовая» (светлая, замер тикета — зона «Одежда») и
«Геймер» (тёмная, зона «Обувь и рюкзаки», bloomAR 120). Аудит альф по всем
13 зонам снят из DOM на каждом состоянии.

- **Покой** (`cream-1-rest`, `gamer-1-rest`): комната — чистая фотография,
  0 зон со светом из 13 (аудит), ни одного прямоугольника. Раньше здесь
  стояли 13 тёмных коробок.
- **Приближение** (`*-2-approach`): ровно одна ближайшая зона с частичным
  светом (Кремовая: v≈0.50; Геймер: b≈0.20) — мягкий овал без единой прямой.
- **Наведение** (`*-3-hover`): подпись + dim −15% + свет на hover-альфах
  (0.1155/0.62 на кремовой, 0.26/0.62 на геймере). Тень — мягкое кольцо по
  границе предмета, растворяется наружу; прямых линий нет.
- **Нажатие** (`*-4-press`): пятно на 0.42×вес — на «Обуви» (AR 120, прежний
  худший случай обрезки) свет ложится на полку и растворяется, вертикальных
  кромок нет.
- **Открытая зона + dim** (`*-5-open`, `*-6-open-dim` — состояние со
  скриншота владельца: зона открыта, наведение на строку указателя зажигает
  соседнюю): слой хотспотов спрятан, меток соседей НЕ видно вовсе, кадр чист.
- **Тач** (`touch-1/2`, эмуляция 390×844): у зоны ближе к центру окна мягкая
  подсказка на 0.6 (аудит: ровно одна зона, b=0.07/v=0.37 на кремовой),
  без краёв.
- **Reduced motion** (`cream-7/8`): разгорание и наведение работают; в кадре
  видна пунктирная рамка — это клавиатурный `focus-visible` после Esc
  (фокус вернулся на хотспот), единственная законная рамка по тикету.

Папка: `%TEMP%\claude\C--Wishlist\3f620652-…\scratchpad\t50\` (12 кадров).

### Спорное / заметки

- **Искра на светлой комнате.** Контрактная искра (3px, акцент `#E7C9A9`,
  пульс 0.18–0.5) на кремовом интерьере различима, но скромна — а теперь она
  единственное, что видно в покое. Размер/альфа — числа пакета, молча усиливать
  не стал; если владельцу «пусто», просить у дизайна правку `spark`.
- **Сила тач-подсказки 0.6** — моё число (в контракте состояния «приближение»
  нет). Легко крутится одной константой `TOUCH_REST_STRENGTH`.
- **Кольцо на мелких зонах** («Просто деньги» у кресла) в полсилы читается
  как лёгкая круглая тень возле предмета — мягко, но на приёмке стоит glance:
  вдруг владелец захочет для мелких зон только пятно без тени.
- `--zone-bloom-rest`/`--zone-vignette-rest` в tokens.css остались (файл
  генерируется из пакета, `design-contract.test` их требует), CSS их больше
  не читает.
- Для стенда добавлены `.env.local` (QUICK_LOGIN на demo@wishlist.local,
  не коммитится) и конфиг `web-verify` (порт 3200) в `.claude/launch.json`.
