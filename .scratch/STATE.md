# Состояние работ — снимок, 2026-08-05 (обновлён после развёртывания)

Читать первым при возобновлении («продолжаем»). Живая картина Phase 1 —
`PROGRESS.md`; план дизайн-раунда 2 — `.scratch/design-round-2/PLAN.md`;
замечания приёмки — `.scratch/acceptance-2026-08-05/NOTES.md`.

## Где мы

Phase 1 закрыта (отчёт `.scratch/phase-1-mvp/REPORT.md`). Прошла первая
клик-приёмка владельца → 9 замечаний. Четыре починки сделаны и закоммичены,
дизайн вернул второй пакет, **стенд развёрнут на VPS и работает**, идут
тикеты дизайн-раунда 2.

## СТЕНД ЖИВОЙ — https://155-212-143-124.sslip.io

Развёрнут 2026-08-05. HTTPS настоящий (Let's Encrypt через sslip.io), замок
без предупреждений. Проверено: главная 200, `/signin` 200, `/r/demo` 200
(12 зон, кадр на месте, ошибок в консоли нет), `http → https` 308.
`/api/health`: `status ok, db up, redis up`.

| Что | Состояние |
|---|---|
| Сервер | Beget, `155.212.143.124`, Ubuntu, 2 ГБ памяти + 2 ГБ подкачки, диск 14 ГБ (занято 55%) |
| Доступ | ключ `~/.ssh/wishlist_deploy`; пользователи `root` и `deploy` (автодеплой ходит под `deploy`) |
| Защита | ufw 22/80/443, fail2ban на ssh, вход по паролю отключён, автообновления безопасности |
| Контейнеры | шесть, все healthy; занято 756 МБ из 1967, подкачка не тронута |
| Настройки | `/opt/wishlist/.env`, права 600, пароли сгенерированы **на сервере** |
| Выпуск | автоматом после зелёного CI на main; образ собирается на раннере, уезжает в ghcr.io |
| База | миграции применены (17 таблиц), сид демо-комнаты выполнен |

**Вход владельца** (почты пока нет): ссылка-закладка
`https://155-212-143-124.sslip.io/dev-login?key=…`, ключ — в `/opt/wishlist/.env`
(`grep QUICK_LOGIN_SECRET`). Выключить, когда появится почта:
`QUICK_LOGIN_ENABLED=false` + перезапуск. Подробности — `docs/DEPLOY.md`.

Домена и SMTP нет — оба берутся у Beget вместе, это следующий шаг владельца.

### Две грабли развёртывания (не наступить снова)

1. **`bootstrap.sh` умирал на ufw.** Апостроф в комментарии к правилу
   («Let's Encrypt») → «ERROR: Invalid syntax», а `set -euo pipefail` рубил
   скрипт ровно перед fail2ban, автообновлениями и отключением пароля.
   Починено в 35975ac. Скрипт идемпотентен — повторный запуск дорешал.
2. **`/r/demo` отдавал 404 ПОСЛЕ успешного сида.** Страница комнаты гостя —
   полностраничный ISR (`revalidate = 300`), и 404 «до сида» застрял в кэше.
   Лечится перезапуском `app` или ожиданием окна. Не баг, но выглядит как баг.

## Сделано и закоммичено

| Коммит | Что |
|---|---|
| dfc9634 | тикеты 17–20 (выход из «Добавить вещь», масштаб хотспотов, вход по ссылке, центровка камеры), приём дизайн-пакета раунда 2 (тикет 21, 130 зон, ADR-0003) |
| 7f4d44a | тикет 28 деплой-пакет, 29 быстрый вход, 30 лечение флейков |
| 3fafb90 | снимок состояния |
| **35975ac** | **починка ufw в bootstrap, QUICK_LOGIN_* в образце .env и DEPLOY.md, тикеты 22 и 25** — **НЕ ЗАПУШЕН** |

CI зелёный на 7f4d44a и 3fafb90 — лечение флейков (`maxWorkers: 4` + честные
таймауты интеграционного набора) держится и на раннере с двумя ядрами.

## В работе прямо сейчас

| Тикет | Что | Территория |
|---|---|---|
| 22 | партитура «походки»: 7 фаз, вложенные слои, cameraScale формулой | `src/components/scene/**`, ADR |
| 25 | тексты: 113 строк нового тона + 11 сирот | `messages/**` |

Оба — субагентами, территории не пересекаются. Файлы:
`.scratch/design-round-2/issues/22-walk-motion.md` и `25-tone-of-voice.md`.
Status не `done` — агент не дошёл, работа на диске цела.

**Ловушка тикета 22 (записана в самом тикете):** разделение камеры на два
вложенных слоя ломает арифметику ADR-0002 — браузер больше не умножает
translate на scale, множитель придётся вернуть в расчёт руками, иначе
вернётся баг тикета 20 (зона не доезжает до центра).

## Дальше по плану

1. Принять 22 и 25, закоммитить, **запушить вместе с 35975ac** — один прогон
   CI и один выпуск на стенд вместо двух.
2. Дизайн-раунд 2, остаток: 23 метки зон → 24 сцена во весь экран;
   затем 26 сверка 28 composition-зон, 27 stateChoice.
3. Вторая приёмка владельца — **на стенде, а не локально**. Комнату для
   приёмки называет владелец (bold «Дерзкая» нравится ему меньше всех;
   дизайн рекомендует warm «Тихая роскошь»).
4. Вопросы дизайну: зона `money` (нет в `zones.json`), комплект флагмана
   «Тихая роскошь» под телефонный кроп, 14 пересечений прямоугольников.
5. Владельцу: домен + почта в Beget (берутся вместе). После домена —
   три строки в `.env` и перезапуск, инструкция в `docs/DEPLOY.md`.

## Правила конвейера (не потерять)

Тикет = отдельный субагент с чистым контекстом; субагенты не делают git-команд
и не добавляют зависимостей; коммит по путям после приёмки; параллельно — только
непересекающиеся территории; каждый тикет закрывается Status: done + Comments.
