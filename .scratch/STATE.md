# Состояние работ — снимок перед очисткой сессии, 2026-08-05

Читать первым при возобновлении («продолжаем»). Живая картина Phase 1 —
`PROGRESS.md`; план дизайн-раунда 2 — `.scratch/design-round-2/PLAN.md`;
замечания приёмки — `.scratch/acceptance-2026-08-05/NOTES.md`.

## Где мы

Phase 1 закрыта (отчёт `.scratch/phase-1-mvp/REPORT.md`). Прошла первая
клик-приёмка владельца → 9 замечаний. Четыре починки сделаны, дизайн вернул
второй пакет, начат переезд на VPS.

## Сделано и ждёт коммита (тикеты закрыты, Status: done)

| Тикет | Что | Где |
|---|---|---|
| 17 | выход из «Добавить вещь» | `.scratch/acceptance-2026-08-05/issues/17-*` |
| 18 | БАГ: хотспоты не масштабировались на десктопе | `…/18-*` |
| 19 | вход по ссылке не сгорает + редирект вошедшего | `…/19-*` |
| 20 | БАГ: камера не доводила зону до центра (ADR-0002) | `…/20-*` |
| 21 | приём дизайн-пакета раунда 2 (130 зон, ADR-0003) | `.scratch/design-round-2/issues/21-*` |
| — | документация: 84 зоны → 130/120 в CLAUDE/CONTEXT/README/ARCHITECTURE/PRD | — |

## Работает в фоне (может не доложиться после очистки — проверить по тикет-файлам)

| Тикет | Что | Территория |
|---|---|---|
| 28 | деплой на VPS: Dockerfile, prod-compose, Caddy, bootstrap, автодеплой, `docs/DEPLOY.md` | `.scratch/deploy/28-vps-setup.md` |
| 29 | быстрый вход `/dev-login` за флагом (обход письма на стенде) | `.scratch/deploy/29-quick-login.md` |
| 30 | флейк `bookings.api` (таймаут 5 с на холодном старте БД) | `.scratch/maintenance/30-flaky-bookings.md` |

Если файлы тикетов есть, но Status не done — агент не закончил; работа на диске
цела, продолжать по его тикету.

## VPS (Beget)

- IP `155.212.143.124`, root, SSH открыт. Домена нет — временно HTTPS через
  `155-212-143-124.sslip.io`; SMTP тоже позже (Beget, вместе с доменом).
- Ключ деплоя: `~/.ssh/wishlist_deploy` (приватный, у владельца) и `.pub`.
  Секреты GitHub `DEPLOY_HOST`, `DEPLOY_USER`, `DEPLOY_SSH_KEY` — **установлены**.
- Публичный ключ владелец дописывал в `~/.ssh/authorized_keys` на сервере;
  **проверить командой** `ssh -i ~/.ssh/wishlist_deploy root@155.212.143.124 "hostname"`.
  Если пускает — разворачивать (владелец разрешил: «можешь всё положить на сервер»).
- На сервере пока ничего не установлено (порт 80 закрыт).

## Дальше по плану

1. Дождаться 28/29/30 → прогнать проверки → коммит + пуш → зелёный CI.
2. Развернуть на VPS (bootstrap → .env → compose → миграции → HTTPS) и отдать
   владельцу ссылку + быстрый вход.
3. Дизайн-раунд 2, тикеты по `.scratch/design-round-2/PLAN.md`:
   22 движение («походка») → 23 метки зон → 24 сцена во весь экран,
   параллельно 25 тексты; затем 26 сверка 28 composition-зон, 27 stateChoice.
4. Вопросы дизайну: зона `money` (нет в zones.json), комплект флагмана
   «Тихая роскошь» под телефонный кроп, 14 пересечений прямоугольников.

## Правила конвейера (не потерять)

Тикет = отдельный субагент с чистым контекстом; субагенты не делают git-команд
и не добавляют зависимостей; коммит по путям после приёмки; параллельно — только
непересекающиеся территории; каждый тикет закрывается Status: done + Comments.
