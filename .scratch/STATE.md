# Состояние работ — снимок, 2026-08-07 (вечер)

**Читать ПЕРВЫМ — `.scratch/HANDOFF.md`**: там на чём остановились, что
отправить дизайнеру и что владелец спросил последним. Здесь — общее состояние.
Реестр расхождений — `docs/design-board-gaps.md`; правила конвейера в конце.

## С чего продолжать

Дерево чистое, всё запушено. **Раскрытия наконец пошли: раунд 14 дал 11 кадров
от НАШИХ баз, шесть принято.**

**Очередь работ:**

| № | что | приоритет |
|---|---|---|
| 81-2 | переразметить `gamer/sneakers` и `lux/jewelry` — они закрывают жест соседям | **P1** |
| 82 | значки пулов вместо буквы у вещи без фото (19 файлов приняты) | P1 |
| 80 | артефакт по нижней кромке зоны при наезде | P1 |
| 79 | кнопка «Отойти»: не видна на светлых комнатах, место не то | P2 |
| 74 | зона списком: сортировка, массовое скрытие, «только свободные» (29b) | P2 |
| 72 | дрейф комнаты в покое (28b) | P2 |
| 83 | подсказка легла на «Списком →» в комнате хозяйки (скриншот 07.08) | **P1** |
| 81-3 | `emerald/anything` — кадра нет, посмотреть зону вырезкой | P2 |

Сделано 07.08 из приёмки: **65** (постоянный бар), **66** (зоны списком),
**67** и **73** (комната списком, турн 29a), **68** (буква вместо дыры),
**70** (стенд сеется сам), **71** (21 предметный кадр), **75** (переразметка
`sneakers`), **76** (бронь называет причину), **77** (гость: надписи развели),
**78** («Хочу» первым), **81 часть 1** (семь раскрытий подключены).

**РАСКРЫТИЙ СТАЛО 17 ИЗ 130.** Раунды 14–15 — первые, снятые от НАШИХ баз
2800×1563. `warm` и `loft` получили кадр впервые: запрет ADR-0005 снят
точечно по `frameRound`, а не с комнаты (`src/config/design.ts`).

**Пакет для дизайна собран** — `C:\Wishlist\ДЛЯ-ДИЗАЙНЕРА-базы-2800`:
письма 11 и 12, восемь баз, вырезки `study`/`loft`, контактные листы раунда 14
и крупный кадр `warm/anything`. Что от него ждём (письма 12 и 13): пересъёмка
`warm/anything` жестом СОДЕРЖИМОГО, а не крышкой — крышка там в базе уже
сдвинута, и двигать её ещё раз бессмысленно (1.59). Больше ничего: значки
приняты, `loft/travel` принят, `study/travel` и `emerald/anything` он
остановил по протоколу и правильно сделал.

**Правило, выведенное раундом 15:** если предмет в базе УЖЕ открыт, жест —
содержимое, а не крышка. Так сделан `lux/anything` — 4.03, лучший в партии.

**Инструменты приёмки кадров, писать заново не надо:**
`scripts/check-drop.mjs` — числа по плоской папке поставки против обоих
ректов; `scripts/drop-contact-sheet.mjs` — контактный лист «база → открыто».

**Три ловушки, в которые уже попадали за этот день** — читать перед тем, как
верить письму дизайна:

1. **Кадры раунда 13 — снова чужой мастер.** 2400×1340 вместо наших
   2800×1563. Свой замер: фон против нашей базы 0.036–0.065 при шуме
   ресэмплинга 0.01–0.02, отношение зона/фон 1.79–2.50 при пороге 3.0.
2. **`rectProposal` из раунда 13 — те же числа, что отклонили в раунде 11.**
   Разбор лежит в `remapNote` самих зон. Чуть не приняли на слово.
3. **«Шрифт вшит в SVG» — не был вшит.** 781 байт, ни `@font-face`, ни
   `data:font`. Вшили сами: `design/round13/logo/grace-lockup-embedded.svg`,
   сборка — `scripts/embed-lockup-font.mjs`.

## Где мы

Продукт называется **Grace**. За 06.08 закрыто **10 тикетов** (54, 57–64),
`npm test` — **914 юнитов**. Всё запушено.

**Выкачено.** Стенд отдаёт вершину `d02f36d`: проверено разметкой —
«Сокровищница» и «Друзья» на месте, прежних «Зал славы»/«Связи» нет.

История на память: 06.08 с 15:22 UTC у GitHub была крупная авария Actions —
прогоны либо падали с «Failed to resolve action download info», либо вовсе
не создавались на новые коммиты. Пять падений подряд были НЕ нашим кодом.
Лечится ожиданием и `gh run rerun <id>`; ходить проверять
`githubstatus.com/api/v2/summary.json`, прежде чем искать баг у себя.
Тогда же в `ci.yml` добавлена группа очереди с отменой устаревших прогонов —
её не убирать: при «пуш после каждого тикета» без неё висело по три прогона
разом.

## Что сделано 06.08 (всё на стенде)

| Тикет | Что |
|---|---|
| 54 | имена участников копилки видят друг друга (ADR-0008), хозяйке — ничего |
| 57 | телефон: комната от верха экрана, шапка на ней, displayName вместо пресета |
| 58 | имя Grace по продукту: вкладка, письма, README, стоп-лист слагов |
| 59 | лист вещей: низ не режется молча, кромка-затухание, «ещё N» на экран зоны |
| 60 | десктоп: потолок листа от ряда плиток (150 → 300), ряд помещается |
| 61 | посев стенда: 55 вещей в 12 зонах, фото в MinIO, `/dev-login?seed=1` |
| 62 | сводка зоны стала нажимаемой; «Связи» → «Друзья», «Зал славы» → «Сокровищница» |
| 63 | ректы `study/travel` и `loft/travel` — на верхний сундук |
| 64 | 120 зон без кадра отвечают светом на приход камеры |
| — | ректы `cream/beauty`, `cottage/music`; сверка замеров дизайна |

## Правило съёмки — отменено 07.08, оставлено как знание

Съёмка раскрытий 06.08 была **сужена** до 15 зон, а 07.08 **остановлена
целиком** (ADR-0009). Само правило остаётся верным описанием того, что модель
умеет, и понадобится при пересмотре: проходит **один цельный предмет с
крышкой** (чемодан 3/3, коробка 2/2, витрина, стойка, поднос), проваливается
группа мелочей (вешалки, флаконы, карточки, книги, винил) — модель их
перерисовывает. Размер зоны ни при чём. Всем зонам без кадра жизнь даёт
свет в коде (тикет 64, в проде).

**Движения денег в проекте нет** — подтверждено владельцем. Экран «оплата
складчины» из турна 19b вычеркнут.

## Цикл с дизайнером — где нить

**Раскрытия закрыты (ADR-0009, 07.08).** Съёмка остановлена целиком,
`BRIEF-lids.md` заморожен, пересъёмок `warm`/`emerald`/`cottage` не ждём,
жест для `lux` не переделываем, зонный фон в подсчёте больше не нужен.
Десять подключённых кадров — окончательное число.

**Ждём от дизайна** (письмо `design/DESIGN-BRIEF-09.md`):
**21 предметный кадр** 400×400 на пять пустых пулов (`grooming`, `gaming`,
`books`, `events`, `travel`) — это теперь главное; экраны списков («зона
целиком списком» из 19b и новый «вся комната списком»); карточки вещи хозяйке
и гостю, пустая зона, затем «что подарили»; исправленный локап логотипа
(сейчас там живой `<text>`, на чужой машине развалится).

Предметные кадры — заодно и проверка исполнителя на задаче, где модель
попадает с первого раза. Пройдёт — есть основание вернуться к раскрытиям
(условие владельца); не пройдёт — возвращаться не к чему.

Письма: `ANSWERS-suitcases.md`, `ANSWERS-cream-batch3.md`,
`ANSWERS-cream-measures.md`, `BRIEF-lids.md`, `BASES-sha256.md`.

## Что сделано за 07.08

Всё на стенде: **65** (постоянный бар, чистый верх, без «Добавить вещь»),
**66** (зоны списком под комнатой вместо горизонтальной полосы), **67** (вся
комната списком — `/room/list` и `/r/{slug}/list`), **68** (буква вместо
чёрной дыры у вещи без фото), **70** (стенд сеется сам + `/dev-login?report=1`),
**71** (21 предметный кадр), плюс правка подсказки «коснись зоны» (переехала
под кадр, стала тише). **69 отменён десятым** — раскрытия возобновлены.

Стенд пересобран: 55 вещей, 31 фото, 0 потерь; имя «Ирина», дата праздника
не задана. Демо-призраков в комнате нет ни в одной зоне.

**Раскрытия ВОЗОБНОВЛЕНЫ** (ADR-0010), но упёрлись в базы. Раунд 13 привёз
четыре чемодана файлами — **все 2400×1340, снова чужой мастер**. Померили
своим замером: фон против НАШЕЙ базы расходится на 0.036–0.065 при шуме
ресэмплинга 0.01–0.02, отношение зона/фон 1.79–2.50 при пороге 3.0.
Подключать нельзя, и дизайн сам это признал. Он просит наши 2800×1563 —
**собраны в `C:\Wishlist\ДЛЯ-ДИЗАЙНЕРА-базы-2800` (8 файлов + SHA256.md),
владельцу остаётся отправить**. До баз коробки не снимаются.

Материалы раунда 13 — `design/round13/`; ответ — `DESIGN-BRIEF-11.md`.
**Локап логотипа по-прежнему с живым `<text>`** — вопреки письму дизайна
шрифт в SVG не вшит (781 байт, ни `@font-face`, ни `data:font`).

Стенд пересобран 07.08: сброс + посев, 55 вещей, 31 фото, 0 потерь. Имя
«Ирина», дата праздника НЕ задана (не выдумывал — ставится в настройках).

## Приёмка 07.08 — шесть замечаний, решения приняты

Разбор и замеры — `.scratch/acceptance-2026-08-07/NOTES.md`, тикеты 65–69.

- **Раскрытия остановлены** (тикет 69). Подключено 10 кадров из 130 зон,
  112 помечены `reshoot`. «Кремовая» закрыта на 4 зоны из 13, оставшиеся
  девять — тот самый неснимаемый тип. Фотографией законченной комнаты не
  выходит; языком продукта назначен отклик светом (тикет 64, уже в проде).
  Возврат — только если пойдут предметные кадры (условие владельца).
- **Дизайн развёрнут** письмом `design/DESIGN-BRIEF-09.md`: стоп раскрытиям,
  21 предметный кадр на пять пустых пулов, два экрана списков.
- Телефон: постоянный нижний бар вместо шторки, без «Добавить вещь», без
  верхнего меню — **тикет 65 сделан и на стенде** (замеры в тикете; шторка,
  планка и жест тяги удалены вместе с `room-tab-drawer.tsx`). Дальше — зоны
  списком под комнатой вместо полосы (66).
- Новая фича: **вся комната списком** — вход для тех, кто не хочет гулять
  по комнате (67).
- Примеры: **80 штук, 54 без фотографии**, пять пулов пусты целиком (68).
  Тикет 61 наполнял стенд настоящими вещами — это другой механизм, призраков
  он не касался.

## Очередь

- **Долг владельцу:** полный разбор доски дизайнера с вопросами и планом
  тикетов (заготовка — `.scratch/design-round-9/BOARD-QUESTIONS-draft.md`,
  22 экрана из 19b разобраны по важности). **Отложено владельцем 07.08** —
  «вернёмся позднее»; напомнить, когда дойдём. Сейчас не начинать.
- Переразметить `sneakers` в `study` и `loft`: зона стоит в основном не на
  обуви, а на панели, кирпиче и на крышке нашей же коробки — из-за неё
  `travel` не получает запаса под откинутую крышку (разбор в тикете 63).
- Выгрузка комнат покрупнее — **решено НЕ делать сейчас** (владелец отдал на
  усмотрение). На ширине показа 1920 отдаваемый 2800 теряет против оригинала
  5504 всего 5–6%; мылит только на плотном экране, который просит до 3840.
  Удвоить самый тяжёлый объект страницы ради меньшинства на десктопе — плохой
  размен, на телефоне это чистая цена. Вернуться, когда будем делать retina
  через `srcset`, чтобы каждый экран получал свой размер.
- Личную комнату владельца посевом НЕ наполнять (его решение). Посев работает
  только на демо-аккаунте.
- Пул `grooming` осиротел: заведён раундом 8, а `rooms.json` у зоны «Уход»
  в мужских комнатах держит `pool: "perfume"` — пять вещей не видны нигде.
- Артефакт базы: в `cottage` открытая книга стоит на диске проигрывателя.
  Перегенерировать нельзя; кандидат в композитинг, если к нему вернёмся.

## Чему НЕ верить

1. Тестам про геометрию зон — только глаза по вырезке.
2. Письмам дизайна: считать самим. Его «база байт-в-байт» дважды оказывалась
   архивной копией; sha в письме сверять со своим файлом.
3. Его `rooms.json` целиком — точечный перенос, иначе стирает поля приёмки.
4. Зелёному пушу как гарантии выката.
5. **`git add -A` при работающих агентах** — 06.08 так была захвачена
   незаконченная правка и main на час покраснел. Коммитить явными путями.

## Инструменты

`scripts/check-batch.mjs` — таблица партии; `check-frames.mjs` — полный пакет;
`rects-preview.mjs` — вырезки с прямоугольниками (`overview`/`crop`/`sheet`).
Посев стенда — `/dev-login?seed=1`. Вырезки приёмок —
`.scratch/design-round-10/`.

## Правила конвейера

Тикет = субагент с чистым контекстом; без git/зависимостей/dev-серверов;
коммит **по путям** после приёмки ведущим; параллельно — только
непересекающиеся территории (`messages/**` общий: точечный Edit с
перечитыванием). Живые проверки — ведущий. Партии дизайна: быстрая таблица
сразу, глаза обязательны, его файлы контракта — только точечно.
**Пушить каждый коммит сразу** — владелец следит по стенду.
