# Передача — 07.08.2026, вечер

Читать ПЕРВЫМ при возобновлении. Состояние проекта — `.scratch/STATE.md`,
этот файл про то, на чём именно остановились и что владелец спросил последним.

## Состояние: всё на проде

Дерево чистое, незапушенного нет, вершина `ffde24f`. Последний прогон CI
зелёный, последний Deploy — success (19:05, 6 мин). **Все закрытые тикеты
выкачены на стенд.**

Один прогон CI в списке помечен `cancelled` — это группа очереди в `ci.yml`
гасит устаревший прогон при быстрых пушах подряд. Так задумано (см. STATE.md),
следующий за ним прогон зелёный.

## Что владелец спросил последним (четыре пункта)

1. **Переразметить `gamer/sneakers` и `lux/jewelry`** — не начато, это первое
   в очереди. Зачем: они закрывают жест соседним зонам, из-за чего кадры
   `gamer/anything` (2.49) и `lux/travel` (0.65) не подключаются. Разбор —
   в тикете 81, Comments части 2. Способ ровно тот же, что сработал в
   тикете 75: вырезка `scripts/rects-preview.mjs`, мерить по предмету,
   следить за соседями и `x ≥ 12`, пересчитать `bloomAR` формулой пакета
   (`w/h × 58`, зажим 30…120), сохранить `rectOld` и `remapNote`. После —
   прогнать `scripts/check-drop.mjs` и, если порог взят, подключить кадры.
2. **Задание дизайнеру** — собрано, лежит в
   `C:\Wishlist\ДЛЯ-ДИЗАЙНЕРА-базы-2800`. Что отправить и что в нём — ниже.
3. **Скриншот** — заведён тикет 83: подсказка «коснись зоны» легла на ссылку
   «Списком →» в комнате хозяйки. Регресс моего же тикета 73.
4. **Про прод** — да, всё запушено и выкачено (см. выше).

## Что отправить дизайнеру

Папка `C:\Wishlist\ДЛЯ-ДИЗАЙНЕРА-базы-2800` — в ней уже всё:

| файл | что это |
|---|---|
| `DESIGN-BRIEF-13.md` | **главное письмо**, приёмка раунда 15 |
| `DESIGN-BRIEF-12.md`, `DESIGN-BRIEF-11.md` | предыдущие, для контекста |
| `sheet-r15.jpg`, `sheet-suitcases.jpg`, `sheet-boxes.jpg` | контактные листы «база → открыто» |
| `warm-anything-zoom.jpg` | крупно: дорисованный бант |
| `rects-study-loft/` | вырезки с сеткой (он писал, что они не доехали) |
| восемь `*.jpg` 2800×1563 + `SHA256.md` | продуктовые базы |

**Задача у него ровно одна: пересъёмка `warm/anything` жестом СОДЕРЖИМОГО, а
не крышкой.** Крышка там в базе уже сдвинута; он выполнил прежнюю команду
дословно и получил 1.59 — движения не видно. Дорогу он уже прошёл сам на
`lux/anything`: сундук был открыт, жест сделали содержимым (появился бордовый
бархат) — 4.03, лучший результат партии.

Всё остальное закрыто: 19 значков приняты, `loft/travel` принят,
`study/travel` и `emerald/anything` он остановил по протоколу и правильно.

**Четыре зоны он ждёт от НАС** — прямоугольники по `lux/travel`,
`bold/anything`, `gamer/anything`, `loft/anything`. Это пункт 1 очереди.

## Очередь

| № | что | приоритет |
|---|---|---|
| 81-2 | переразметить `gamer/sneakers` и `lux/jewelry` | **P1** |
| 83 | подсказка легла на «Списком →» (скриншот) | **P1** |
| 82 | значки пулов вместо буквы (19 файлов в `design/round15/icons`) | P1 |
| 80 | артефакт по нижней кромке зоны при наезде | P1 |
| 79 | кнопка «Отойти»: не видна на светлых комнатах | P2 |
| 74 | зона списком: сортировка, массовое скрытие (турн 29b) | P2 |
| 72 | дрейф комнаты в покое (турн 28b) | P2 |
| 81-3 | `emerald/anything` — кадра нет, посмотреть зону | P2 |

Тикеты — `.scratch/acceptance-2026-08-07/issues/`.

## Чем мерить (писать заново не надо)

- `scripts/check-drop.mjs <папка> [--rect contract|proposal]` — приёмка партии
  кадров: своя зона против фона из ОСТАЛЬНЫХ зон комнаты, порог 3.0;
- `scripts/drop-contact-sheet.mjs <папка> <out.jpg>` — контактный лист
  «база → открыто», без него глазами не примешь;
- `scripts/rects-preview.mjs overview <комната> --grid 20 --rects "имя=x,y,w,h"` —
  прямоугольники на кадре с сеткой;
- `scripts/embed-lockup-font.mjs` — вшить шрифт в SVG-локап.

## Правила, выведенные за день (не потерять)

1. **Письмам дизайна не верить — считать самим.** За день три ложных
   заявления: чужой мастер кадров, повторно присланный отклонённый
   прямоугольник, «вшитый» шрифт, которого в файле не было.
2. **Вещь с крышки может переехать; вещь, которой в базе не было, появляться
   не должна.** Так приняли лейбл у `loft/travel` и отклонили бант у
   `warm/anything`.
3. **Если предмет в базе уже открыт, жест — содержимое, а не крышка.**
4. **Глаза главнее порога в обе стороны.** Тикет 49 отклонял прошедшее порог;
   07.08 владелец принял 2.77 и 2.96, потому что кадры честные.
5. **Страницы проверять сборкой**, а не только `typecheck`: два экспорта
   метаданных на одной странице ловит только `next build`.
6. **Счётчики в тестах обновлять с причиной поимённо** — они и заведены как
   ловушки на молчаливые правки карты.
