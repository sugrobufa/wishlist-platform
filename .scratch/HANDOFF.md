# Передача — 08.08.2026, вечер (перед сбросом контекста)

Читать ПЕРВЫМ при возобновлении. Общее состояние — `.scratch/STATE.md`,
очередь работ — `.scratch/acceptance-2026-08-08/BACKLOG.md`.

## СТОЯЧЕЕ УКАЗАНИЕ ВЛАДЕЛЬЦА

**«Продолжай 89 и далее все тикеты без моего согласования».** Дано 08.08.2026.
Брать из `BACKLOG.md` по порядку, коммитить и пушить каждый тикет сразу,
вопросы к владельцу копить здесь, а не блокировать ими работу.

Порядок: **89 → 92 → 93 → 94 → 95 → 96 → 97 → 98 → 99 → 100**, дальше
косметика доски пачкой.

## Где остановились

Дерево чистое, всё запушено, вершина — `fix: «Списком» … (тикет 101)`.
CI зелёный, стенд `155-212-143-124.sslip.io` выкачен.

**Следующий тикет — 89** (сокровищница: глазок, удаление с предупреждением,
вход напрямую). Файл: `.scratch/acceptance-2026-08-08/issues/89-*.md`, там же
разобрана ловушка: `listHallItems` фильтрует `hiddenFromHall` и обслуживает
страницу ХОЗЯЙКИ — включить глазок «в лоб» значит спрятать вещь и от неё.
Решение владельца по «хочу» в сокровищнице записано там же: не нужно, выбора
не показывать.

## Что сделано за 07–08.08 (всё на стенде)

Тринадцать тикетов: **81-2** (переразметка `gamer/sneakers`, `lux/jewelry`,
`lux/money`, `lux/music` + подключён `lux/travel`), **84**, **85**, **86**,
**82** (значки пулов), **87** (раунд 16), **80** (кроссфейд шёл другим кропом),
**81-3**, **72** (дрейф), **74**, **90** (раунд 17), **91** (карточка вещи
гостю — последний незакрытый P0 доски), **88** (зона строками), **101** (500).

**Раскрытий 22 из 130, и у каждой из десяти комнат есть кадр.**

## Открытые вопросы владельцу (не блокируют)

1. **Пустая комната: призраки или затемнение?** Доска 25c рисует затемнение
   на 42%, у нас демо-призраки. Вопрос вынесен дизайну письмом 16.
2. **Порог шера «хотя бы пять вещей»** (доска 25c) — такого правила у нас
   нет, ждём ответа дизайна.
3. **«Уже дарят · Аня забрала»** — доска показывает имя занявшего ДРУГИМ
   гостям до праздника, у нас `SIGNED` значит «после праздника». Смыслы
   разошлись, нужно решение владельца, а не тикет.

## Дизайну отправлено, ждём ответа

Папка отправки — `C:\Wishlist\ДЛЯ-ДИЗАЙНЕРА-базы-2800` (**класть туда всегда**,
владелец отправляет вручную). Последние письма:

- **BRIEF-15** — вердикт по раунду 17: **сборка принята как метод**, для
  сборок записано своё правило приёмки (own ≥ 0.05 и фон ≤ 0.01, отношение
  бессмысленно); признана наша ошибка по `gamer/anything` раунда 14;
- **BRIEF-16** — новое задание, согласовано владельцем: **раскрытия на паузе**,
  кроме одной зоны. Просим четыре экрана: пустые состояния (25c) →
  услуга-впечатление (8e) → цвет света и время суток (11e) → лента друзей по
  празднику (11e/20a). Плюс три хвоста: нить-позиция 28a, два расхождения,
  которые он сам нашёл в нашей сборке (просим назвать элементы поимённо),
  логотип.

**В работе у дизайна:** `emerald/anything` — компактный нетекучий предмет,
одна попытка; не выйдет — зона остаётся со светом.

## Новая доска — разобрана

Пакет: `C:\Wishlist\Wishlist платформа главный экран доска\handoff-claude-code`
(819 КБ доски против наших 749). Текст выдернут в `.scratch/round17/board-text.txt`.

Свежие турны — те, что уже закрыты (29a/29b, 28b). **Не закрыто три вещи**,
все три в письме 16. Полный разбор доски против кода —
`docs/design-board-recheck.md` (два списка: сделано своим исполнением —
11 штук; не сделано — 21).

## Чем мерить — инструменты, писать заново не надо

- `scripts/check-drop.mjs` — приёмка партии кадров;
- `.scratch/round16/zone-diff.mjs` — расхождение по КАЖДОЙ зоне комнаты:
  **им проверяются сборки** (у честной сборки соседи на уровне 0.003);
- `.scratch/round16/where-diff.mjs` — карта расхождения, где именно жест;
- `.scratch/round16/try-rect.mjs` — примерка прямоугольника до правки контракта;
- `.scratch/round16/fit.mjs`, `pair.mjs` — вырезки для глаз;
- `.scratch/round16/gen-pool-icons.mjs` — пересборка значков из SVG пакета.

## Правила, добытые за эти дни (не потерять)

1. **Порог считает фон по прямоугольникам ОСТАЛЬНЫХ зон** — зона, которую
   раскрытие физически ЗАКРЫВАЕТ, топит собственный кадр. У `lux/travel` так
   и было: 0.93 при честном кадре, 3.31 после переразметки соседей.
2. **Кадр может взять порог и всё равно быть браком** — и наоборот. Смотреть
   надо всю комнату (`zone-diff.mjs`) и **зумом на отдельный предмет**:
   на мелкой вырезке мягкость отрисовки читается как подмена, и мы на этом
   один раз ошиблись, забраковав честный кадр.
3. **«Второй такой же предмет» бывает отражением** (два бокса в Пентхаусе).
4. **Числа дизайна ниже наших системно** — он усредняет фон по всему кадру.
5. **Незаметный вход прячет сломанный экран.** `/room/list` падал с 500 с
   тикета 67 и молчал, пока вход был стрелочной ссылкой. Сделали кнопкой —
   нашли за минуту.
6. **Функции не ходят через границу RSC.** Ловушка — `tests/rsc-boundary.test.ts`.

## Как проверять живьём

Локально: `docker compose up -d` (порты публикуются только так — контейнеры
из старого запуска висели без маппинга), затем `npm run dev`, вход через
`/dev-login` (сеет стенд сам). Прод — тот же путь, но сессия уже есть в
браузерной панели; замеры удобнее скриптом через `javascript_tool`, чем
скриншотом: панель скрыта и кадры не композитятся.
