# Передача — 08.08.2026, ночь

Читать ПЕРВЫМ при возобновлении. Общее состояние — `.scratch/STATE.md`,
очередь работ — `.scratch/acceptance-2026-08-08/BACKLOG.md`.

## СТОЯЧЕЕ УКАЗАНИЕ ВЛАДЕЛЬЦА

**«Продолжай все тикеты без моего согласования».** Дано 08.08.2026, повторено
вечером. Брать из `BACKLOG.md` по порядку, коммитить и пушить каждый тикет
сразу, вопросы к владельцу копить здесь, а не блокировать ими работу.

## Где остановились

Дерево чистое, всё запушено, вершина — `feat: укрепление аккаунта перед первым
шером (тикет 94)`. `npm test` — **990 юнитов**, lint/typecheck/build чисто.

**Следующий тикет — 95** (лента друзей по близости праздника). Числа не надо
выдумывать: они в `task15.json → friendsFeed` (пакет задания 15, см. ниже).
Дальше: 96 → 97 → 98 → 99 → 100 → 102, потом косметика доски пачкой.

## Что сделано в эту сессию (89, 92, 93, 94)

- **89 — сокровищница**: три действия вместо одной непонятной кнопки (глазок
  «скрыть от гостей», «убрать из сокровищницы», «удалить» с предупреждением),
  плюс «Добавить вещь» прямо с витрины (`/room/add?hall=1`, состояние `LOVE`
  молча). Фильтр наблюдателя переехал в `dto/hall.hallItemShownToObservers`.
- **92 — заметка-цитата** на витрине (доска Б22) + дорога к её правке: с
  витрины раньше не вело ничего.
- **93 — сокровищница гостю** (доска А5): `/r/{slug}/hall`, ISR 300, тот же
  тег `room-{id}`. Заодно заплатана утечка: `itemForGuest` отдавал `inHall`,
  не спрашивая `hiddenFromHall`.
- **94 — укрепление аккаунта** (доска Б8): раздел «Вход и доступ» в
  настройках, просьба перед первым шером, новая колонка `User.hardenAskedAt`,
  у `User.secondAuth` наконец есть писатель и читатель.

## Открытые вопросы владельцу (не блокируют, но копятся)

1. **Пустая комната: призраки или темнота?** Дизайн ответил вердиктом
   «темнота, призраки снять» (задание 15). Призраки работают в проде и имеют
   свою настройку — снятие это отмена принятого решения, а не тикет.
   **Тикеты 99 и 100 стоит делать уже под выбранный язык.**
2. **Порог шера «хотя бы пять вещей»** — дизайн подтвердил и расписал
   (`emptyStates.shareSheetUnderFive`): шер не блокируется, лист меняет
   приоритеты. Такого правила у нас нет — заводить ли.
3. **«Уже дарят · Аня забрала»** — доска показывает имя занявшего ДРУГИМ
   гостям до праздника, у нас `SIGNED` значит «после праздника». Смыслы
   разошлись, нужно решение владельца.
4. **Кроп покоя сцены в центр композиции?** (см. разбор задания 15). Для нашей
   геометрии это `scene.phone.image.x = −100` вместо −12. Меняет то, что видно
   первым во всех десяти комнатах, и спорит с приёмкой 05–06.08.
5. **OAuth-провайдер для укрепления** (тикет 94): ни Apple, ни Telegram, ни
   Google не подключены — просьба укрепить аккаунт поэтому не показывается.
   Заводить ли регистрацию приложения (Google Cloud / Telegram bot).
6. **Список устройств** («Где открыта комната») с доски 13b — сессии Auth.js
   не хранят ни устройства, ни города. Нужно ли это в Phase 1.
7. **«В зале с 2025»** (доска Б22): такой даты в модели нет, `receivedAt` —
   про подарок, `createdAt` — про заведение. Нужна ли подпись вообще.

## Пакет дизайна «задание 15» (турн 30) — РАЗОБРАН

Пришёл 08.08 вечером: `C:\Wishlist\Wishlist платформа главный экран 3\design_handoff_task15`.
Полный разбор — `.scratch/acceptance-2026-08-08/INTAKE-task15.md`.

**Числа брать можно, оба «расхождения» — нет.** `task15.json` закрывает
значения для тикетов 95, 96, 97, 99, 100 и даёт новый тикет **102**
(нить-позиция). Оба заявленных расхождения проверку не прошли: первое считает
телефон по десктопным числам (1120 — ширина ДЕСКТОПНОЙ сцены из его же
`rooms.json`), второе описывает нить, которой в сборке нет вовсе.

**Ответ дизайну готов и лежит в папке отправки:**
`C:\Wishlist\ДЛЯ-ДИЗАЙНЕРА-базы-2800\BRIEF-17-otvet-na-zadanie-15.md` —
владельцу остаётся отправить.

**emerald/anything закрыт**: одна согласованная попытка, кадр брак (own 0.0745,
ratio 2.87), зона остаётся со светом. **Раскрытий по-прежнему 22 из 130**, и
это окончательно.

## Чему НЕ верить

1. Тестам про геометрию зон — только глаза по вырезке.
2. **Письмам дизайна: считать самим.** Счёт дошёл до пяти ложных заявлений;
   последние два — в задании 15.
3. Его `rooms.json` целиком — точечный перенос.
4. Зелёному пушу как гарантии выката.
5. `git add -A` при работающих агентах — коммитить явными путями.
6. **Сквозному моку `unstable_cache` в тестах.** Next Data Cache
   СЕРИАЛИЗУЕТ значение: положишь в кэш строку БД — дата вернётся строкой.
   Поймал живой стенд на тикете 93; в `tests/guest-hall.test.ts` мок теперь
   гоняет результат через JSON.
7. **Функции не ходят через границу RSC** (`tests/rsc-boundary.test.ts`).

## Как проверять живьём

Локально: `docker compose up -d`, затем дев-сервер через панель браузера
(`preview_start`, конфигурация `web`), вход через `/dev-login` (сеет стенд сам).
Скриншоты не работают — панель скрыта и кадры не композитятся; мерить надо
скриптом через `javascript_tool`. Клик по координатам тоже ненадёжен — жать
элементы скриптом.

Гостевая сторона стенда — `/r/demo` (владелец комнаты «Мила»).

## Чем мерить — инструменты, писать заново не надо

- `scripts/check-drop.mjs` — приёмка партии кадров;
- `.scratch/round16/zone-diff.mjs` — расхождение по КАЖДОЙ зоне (им
  проверяются сборки);
- `.scratch/round16/where-diff.mjs`, `try-rect.mjs`, `fit.mjs`, `pair.mjs`,
  `gen-pool-icons.mjs`.
