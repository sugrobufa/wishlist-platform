# Передача — 11.08.2026, поздний вечер

Читать ПЕРВЫМ при возобновлении. Тикеты — `.scratch/acceptance-2026-08-10/issues/`,
разборы пакетов — там же `INTAKE-*.md`.

## СОСТОЯНИЕ

**Всё запушено, вершина `7fb11ac`, неотправленных ноль.** Юниты — **1839 из
1840**; единственное падение разобрано ниже и продукта не касается. Типы чисты,
линт — два старых предупреждения в `scripts/`.

## Сделано этим вечером — тикеты 185–200

| | |
|---|---|
| **185** | кадр настроек по пакету 43: настоящий кроссфейд слоёв (они перемонтировались — свет менялся встык), строка состояния + `aria-live`, плашка «Примерка» с отменой, лента в твоём свете, кадр во всю ширину (375×208.9) |
| **186** | плитка вещи ведёт на карточку: у гостя вела, у хозяйки нет |
| **187** | день рождения вместо безымянной «Даты» — миграция `room_birthday`, домен `src/server/birthday.ts`, повтор без галочки |
| **188–191** | пустая комната: автонаполнение выпилено, первое действие «Добавить первую вещь», анкета убрана, полоса получила потолок и свою прокрутку |
| **192** | разведены две строки действий в зоне денег |
| **200** | `viewport-fit=cover` — оба инсета айфона были нулями |

## Заведено, не начато

**193** три пустых места в зоне → одно с плюсом · **194** строка впечатления не
ведёт на карточку · **195** ссылка магазина пропадает у гостя при скрытой цене ·
**196–199** по пакету 44: карточка вещи как экран чтения, `handoff/README.md`
учит модели v1, праздники плашкой, пустая комната весом в кадре. Плюс **184** —
третье слово для одного действия.

## КРАСНОЕ, И ЭТО НЕ ПРОДУКТ

`tests/birthday-migration.test.ts` — **в одиночку 4 из 4 зелёные, в общем
прогоне падает одна проверка**: «настоящая таблица комнат этим прогоном не
тронута». Она считает комнаты в dev-БД, а соседние файлы параллельно их
создают. Тест гоночный по построению; чинить — сделать утверждение независимым
от общего числа строк. Не флейк базы: воспроизводится каждый раз.

## Дизайн

**Пакет 44 разобран** (`INTAKE-round44.md`): оба обещанных поля впервые
действительно на месте, 23 ключа ровно 23. Три расхождения — бар 76 против
нашего замера 86.0 (и против его же `tokens.json`), `measuredFrom` говорит 375
при фотографии 430, «README исправлен пакетом» без файла в пакете.

**Находка дизайна правит нас, и она серьёзная:** `handoff/README.md` двенадцать
дней учил модели v1 («два состояния», «хочу → люблю», «зал славы»). Ущерба нет
— `CLAUDE.md` держит v2, — но карточку вещи мы собрали формой именно по его
устаревшей строке. Тикет 197 чистит и заводит сторож на читаемые файлы пакета.

**В папке отправки — письмо 48, это ОТВЕТ, не задание.** Задания за дизайном
нет: раунд 44 закрыл письмо 47 целиком.

## Открытые хвосты

| что | состояние |
|---|---|
| гоночный `birthday-migration` | см. выше, красит общий прогон |
| e2e на 188 и 190 | не написан: пустой комнаты read-only способом со стенда не достать. План — временный длинный узел через `page.evaluate` |
| `setRoomWants` без вызывающих | поле `wants` в БД сохранено намеренно (тикет 189), функция жива только в тестах |
| десктопная пустая комната | блок выше 116 выступает на сцену; поведение доTикетное, 188 объявлен мобильным |
| коммит `f58ff8f` | мусорный `@` в сообщении, историю на общей ветке не переписывали |
| `min-h-screen` на шести экранах без бара | список готов, если владелец увидит ту же пустоту |

## Чему НЕ верить

1. Тестам про геометрию зон — только глаза по вырезке.
2. Числам как приёмке СВЕТА — ночь дважды сходилась по замерам и проваливалась глазами.
3. Вырезкам из исходника с жёстким `\n` — дерево на Windows держит CRLF, CI — LF.
4. **Письмам дизайна без своего счёта** — но раунды 43 и 44 прошли почти чисто.
5. Зелёному локальному прогону как гарантии CI.
6. **Проверке в Chromium как приёмке инсетов** — `env(safe-area-inset-*)` там ноль и до правки, и после.
7. Спискам «чужих падений» в отчётах агентов — перепроверять поштучно.
8. **Читаемым файлам пакета** (`handoff/README.md` и соседи) — до тикета 197 их не проверял никто.

## Как проверять живьём

`docker compose up -d`; e2e поднимает свой сервер на :3100. Телефон отдельно —
`npx playwright test --project=mobile --no-deps`. Стенд — `https://155-212-143-124.sslip.io`.
Ночной e2e идёт своим расписанием: `gh run list --workflow=e2e.yml`.
