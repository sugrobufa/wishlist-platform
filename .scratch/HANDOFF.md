# Передача — 14.08.2026

Читать ПЕРВЫМ при возобновлении. Тикеты — `.scratch/acceptance-2026-08-13/issues/`
и `.scratch/acceptance-2026-08-14/issues/`; разборы пакетов — `INTAKE-round48…51.md`
в тех же папках.

## СОСТОЯНИЕ

**main зелёный и запушен, вершина `25d9768`.** Дерево чистое, кроме вырезок
работающего агента. Юниты — **2231 из 2231** (132 файла), типы чисты, линт —
два старых предупреждения в `scripts/`.

**В работе один агент: тикет 234** (пять адресов получают прямоугольник). Его
вырезки лежат в `.scratch/acceptance-2026-08-14/234-rects/`.

## ЧТО ДЕЛАТЬ ПЕРВЫМ

1. **Дождаться тикета 234 и свести.** Он заводит полку `bar` в четырёх мужских
   комнатах и размечает пять адресов. **Это закроет шесть открытых расхождений
   счёта**: `zonesNote` уже обещает «134 адреса» и «14 в мужских», а в
   `rooms.json` пока 130 и 13. Новый сторож `tests/package-prose-counts.test.ts`
   их держит и обязан позеленеть сам. Ключи словаря для полки заводит ВЕДУЩИЙ —
   агенту `messages/*` закрыт.
2. **У экрана полок нет входа.** Тикет 239 собрал `/room/list/zones` и
   `/r/{slug}/list/zones`, но тихая строка «Полки · {count}»
   (`ZoneList.entry`, ключ уже в словаре) под кадром не поставлена: она живёт в
   `src/app/room/page.tsx`, `src/app/r/[slug]/page.tsx` и
   `scene/zone-rail.tsx` — чужая территория для того агента. **Без неё дороги к
   семи полкам у человека нет.**
3. **Тикет 241 не заведён:** четыре строки набора уже приняты в словарь (пакет
   52), но онбординг не показывает число комнат на плитках (6 · 4 · 10, турн
   58a), а настройка не переименована в «Комнаты на выбор»
   (`Settings.setOverline` лежит в `PACKAGE_ONLY` и ждёт своего экрана).
4. **Тикет 238** — десять прямоугольников на пустом месте, найдены обходом 233.
   Числа и вырезки готовы (`233-rects/before/comp-*.jpg`), задание написано.
5. **Тикет 231** — «Привязать и поделиться» уводит к провайдеру и возвращает в
   комнату, шер после возврата не открывается. Ни здесь, ни в комнате.

## Что сделано 13–14.08 — двадцать тикетов

**Приёмка 13.08 (пять замечаний):** 214 сокровищница объясняет себя · 215
огоньки называют себя · **216 экран итога перестал спорить с комнатой (баг)** ·
217 кнопка стала действием · 218 подложка знаков.

**Пакеты дизайна 48–52, пять раундов за день:** 219 наполнение четырёх
состояний · 220 две строки витрины · 221 слово шкалы и круглая подложка · 222
счётчик витрины набран не тем приёмом (наша находка) · 223 словарь 49 и сторож
брифов по форме · 224 кадр комнаты в шапке итога · 225 шкала своей строкой ·
232 круглые знаки угла · 233 прямоугольник берёт предмет с опорой · 235 живая
полка без места · 236 ноль забранных · 239 экран полок · 240 сторож прозы.

**Приёмка 14.08 (восемь замечаний):** 226 фото хозяйки в шапке · 227 «Отдать
ссылку» открывает шер на месте · 228+237 кадр в настройках отпускает с ручками ·
229/234 набор полок · 230 «рамки предметов» оказались нашими местами.

## ГЛАВНОЕ, ЧТО ЗНАТЬ

### Отношения с дизайном изменились за день

Пакеты 49–52 — **четыре подряд без единого спора**. Он отменяет свои вердикты
по нашим проверкам («по правилу он кнопка, и правило право»), признаёт
выдуманные доводы («я сослался на свойство, которого не смотрел») и сам назвал
корень своих промахов счётом: «это не внимательность, это отсутствующая
проверка». **Два сторожа прозы пакета (тикет 240) — его предложение.**

Счёт неверных заявлений — 16, все до пакета 49. Проверки, ловящие каждый вид за
минуту, записаны в памяти проекта.

### Правила, забранные в устав за эти дни

- **прямоугольник берёт предмет ВМЕСТЕ С ТЕМ, ЧТО ЕГО ДЕРЖИТ** — раму, полку,
  столешницу. Предмет без опоры читается артефактом (пакет 50);
- **прямоугольник стоит на предмете в БАЗОВОМ кадре**, не в кадре раскрытия;
- **пустое состояние — одно обещание и одно действие**; третий текст обязан
  снять один из первых двух (пакет 50);
- **словарь — плоская карта «ключ → строка»**, требование, а не наблюдение;
- **новое состояние не должно требовать нового знака.** Потребовало — значит
  завели второй сорт, а не закрыли дырку (пакет 51).

### Находка, которая дороже своей правки

Пара `study/events` перемерена против нового прямоугольника: **кольцо упало
0.1295 → 0.0439**. Пара «плыла» не от нечестного раскрытия, а от обрезки — жест
законно выходил наружу, потому что самой рамы в прямоугольнике не было. Кольцо
проходит у всех 22 пар вместо 21. **Стоит проверить тем же способом остальные
плывущие пары** — просьба ушла письмом 57.

### Инвариант №9 переписан целиком

У полки **три состояния**, и 404 только у одного. Живая полка без места на
кадре: страница 200, вещи на месте, вход через список; нет метки и наезда.
404 — за выключенной хозяйкой и за дверью `zonesHiddenByProduct`. Число зон
теперь **считается, а не помнится**.

## Дизайн: письмо 57 ЖДЁТ ОТПРАВКИ

`C:\Wishlist\ДЛЯ-ДИЗАЙНЕРА-базы-2800\BRIEF-57-schitali-15-ne-vykhodit-zoneSet-vybiraet-komnaty.md`

Единственное неотправленное; 51–56 закрыты пакетами и убраны в архив. В нём:
`zoneSet` выбирает комнаты, а не полки (его «15» не выходит: 13, 13, объединение
19) · находка про кольцо · расхождение по числу мест (его 23 против нашего 22) ·
десять прямоугольников на пустом месте.

## Чему НЕ верить

1. **Одиночный зелёный прогон не опровергает падение в общем** — и наоборот:
   14.08 обход состояний падал через раз в полном прогоне и был зелёным
   поштучно. Оказалась драка за Postgres между процессами, а не логика.
2. **`rooms.json` целиком из пакета — НИКОГДА.** Пакет 52 прислал его целиком, и
   его копия наших координат отстала на час: `study/sport` 300,244,68,86,
   `loft/events` 400,165,35,60 — оба переразмечены нами в тот же день. Взято
   одно поле `zonesNote`, диффом одна строка.
3. **ЧИТАЕМЫМ ФАЙЛАМ — ЛЮБЫМ.** За два дня врали: комментарий про `thanksAll`
   (устарел за час), запись о трёх снятых ключах (прожила час), замер в
   `room-studio.module.css` (мерен до полного вылета кадра), живой
   `handoff/README.md` (объявлен исправленным, остался прежним — поймал сторож).
4. **PowerShell для правки файлов с кириллицей** — `Get-Content -Raw` +
   `Set-Content` съедают кодировку и роняют файл целиком. Только Edit/Write или
   node с явным `utf8`.
5. Числам как приёмке СВЕТА — только глаза на телефоне.
6. Тестам про геометрию зон — только глаза по вырезке.

## Глазами на телефоне НЕ ПРОВЕРЕНО НИЧЕГО

Двадцать тикетов за два дня, и ни один не смотрен на живом стенде. Все они
видимые: строки витрины, шкала своей строкой, круглые знаки, четыре состояния
итога, кадр комнаты в шапке, фото хозяйки, экран полок, липкий кадр. **Это
первое, что стоит сделать.**

Стенд — `https://155-212-143-124.sslip.io`, пустая комната — `/dev-login?empty=1`.
`docker compose up -d`; телефон — `npx playwright test --project=mobile --no-deps`.
Ночной e2e — `gh run list --workflow=e2e.yml`.
