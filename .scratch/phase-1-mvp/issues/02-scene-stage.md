# 02 — Сцена: кадр, хотспоты, наезд камеры, кадры «открыто»

**What to build:** Комната хозяйки — живая сцена по контракту дизайн-пакета:
кадр интерьера, зоны-хотспоты из rooms.json, тап по зоне — вычисленный наезд
камеры (motion.json), кроссфейд в кадр «открыто» (если есть), выход обратно.
Телефон и десктоп из одной карты координат. Выключенные зоны не рендерятся.

**Blocked by:** 01.

**Status:** ready-for-agent

- [ ] SceneStage: слои кадра/«открыто»/хотспоты; координаты ТОЛЬКО из rooms.json (относительные проценты), десктоп ×1.7778 из формулы, никаких захардкоженных координат в компонентах
- [ ] Наезд: transform из формулы motion.json → openZone[0] (scale 1.72 phone / 1.45 desktop), длительности/кривая из motion.json; периферия темнеет; закрытие — «быстро внутрь, спокойно наружу»
- [ ] Кадры «открыто» кроссфейдом у зон, где они есть; у остальных — только наезд (честная подпись из zones.json)
- [ ] Ambient-дрейф отдельным слоем под камерой; prefers-reduced-motion по контракту; hover-эффекты за (hover:hover) and (pointer:fine); цели ≥44px
- [ ] Изображения комнат раздаются как есть (без пережатия), lazy для кадров «открыто»
- [ ] Работает для всех 10 пресетов (переключение комнаты в dev-режиме достаточно через БД)
