# 15 — e2e happy path: полный цикл дарения + перф-бюджет

**What to build:** Playwright-сценарий всего цикла на реальном стеке (docker
compose): регистрация → комната → вещь «люблю» вручную → вещь «хочу» по URL
(фикстурный локальный магазин) → шер → гость тихо бронирует → хозяйка видит
только счётчик → «праздник прошёл» → «Дошло» → зал славы + связь. Плюс
Lighthouse-бюджет комнаты гостя.

**Blocked by:** 06, 08, 10.

**Status:** ready-for-agent

- [ ] e2e-тест цикла зелёный локально (magic link перехватывается из консоли/тест-хука); два контекста браузера: хозяйка и гость
- [ ] Ассерты инварианта в e2e: в DOM и network-ответах хозяйки нет имени гостя до «что подарили»
- [ ] Фикстурный «магазин» — локальная страница с JSON-LD для стабильного парсинга в e2e
- [ ] Lighthouse mobile на /r/{slug}: performance ≥90 с плейсхолдер-статикой; первый экран сцены ≤2МБ
- [ ] Прогон в CI отдельным workflow-job с docker-сервисами (postgres/redis/minio) — можно nightly/manual, не блокируя основной CI
