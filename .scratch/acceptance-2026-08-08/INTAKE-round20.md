# Разбор пакета «раунд 20» (турн 32) — 09.08.2026

Пакет: `C:\Wishlist\Wishlist платформа главный экран 16\design_handoff_round20`
(README, `board-turn32.html`, `task16.json`, `task15.json`, `messages-ru.json`,
`rooms.json`, два предметных кадра).

Ответ отправлен письмом `ДЛЯ-ДИЗАЙНЕРА-базы-2800\BRIEF-21-zadanie-17.md`.

## Итог: по сути принят, по файлам — точечно

Впервые закрыты все четыре вопроса задания 16. Два заявления о ФАЙЛАХ
проверку не прошли, оба одного класса — «остальное не менялось».

## 1. `tod` (32b) — БЕРЁМ, точечно десять значений

`cream dusk · warm dusk · lux dusk · emerald night · bold night · cottage day ·
gamer night · sport dusk · study dusk · loft night`

Совпало с его README. Morning нет ни у одной базы.

**Это не косметика.** Наш `grading.ts` считает `day` идентичностью для ВСЕХ
комнат — значит для девяти из десяти ручка света сейчас врёт: в дневном
положении вечерняя `cream` остаётся вечерней, в вечернем получает сумерки
поверх сумерек. Правило пересчёта у него есть
(`lightAndTime.timeOfDay.transitionTable`), но словами и невыполнимыми:
«day = brightness(1.35) saturate(.92) минус её сумеречный оверлей» — вычесть
наложенный градиент в CSS нельзя. Запрошены девять строк числами.

## 2. `rooms.json` целиком — НЕ БЕРЁМ. Это файл эпохи раунда 8

Заявление «остальное в нём не менялось» неверно. Замеры:

| Что | Пакет | Наш |
|---|---|---|
| Зон всего | **128** | **130** (контракт) |
| `accepted` | **0** | 130 |
| `reshootReason` | **0** | 50 |
| `openFrame` | 13, пути `round8/…` | 130 полей, подключено 22 |
| `rectVersion` | 128 | 0 (поля у нас нет) |
| Прямоугольников расходится | **26** | — |

- пропали зоны `sport/gaming` и `loft/gaming`;
- 26 прямоугольников — ДОнаши: его `cream/beauty` = `{281,125,87,52}` лежит у
  нас в поле `rectOld`. Так же `study/travel`, `loft/travel`, `gamer/sneakers`,
  `emerald/beauty`, `sport/watches`, `warm/music`, `lux/music`;
- стёрт слой приёмки: `accepted`, `reshootReason`, `rectOld`, а наверху файла
  `acceptedRule`, `identityCheck`, `round7`;
- примечание про tod вставлено во ВСЕ поля `note`, включая `maskHint.note`.

Правило «его `rooms.json` — точечный перенос» (STATE.md) подтвердилось в
третий раз и теперь с числами.

## 3. `messages-ru.json` — переименование неполное

`Access.strengthenBody` он починил. Три строки продукта всё ещё «зал славы»:
`Occasion.receivedRow`, `Occasion.receivedRowNoName`,
`Connections.originGiftOneHall`. У нас они давно переименованы и записаны в
`PACKAGE_DRIFT` (tests/messages-tone.test.ts) — брать его файл целиком нельзя
и здесь.

## 4. Согласие (32a) против тикета 98 — его модель ЛУЧШЕ, перестраиваем

Мы собрали согласие за день до пакета. Его устройство лучше в четырёх местах:

| Место | Тикет 98 (в проде) | 32a | Решение |
|---|---|---|---|
| Вопрос гостю | после праздника, на «Друзьях» | на подтверждении брони, тихим блоком после действия | берём его |
| Кого видит хозяйка | всех ждущих | только предложивших связь | берём его |
| Отказ | уходит у обоих молча | остаётся односторонней, «знакомы · подарок в истории» | берём его |
| Третье состояние | отдельный блок вне списков | чип янтарной точкой в существующих рядах | берём его |
| Цены «только друзьям» | любая состоявшаяся связь (в т.ч. FOLLOW) | только взаимные | берём его — строже |

**Чего это стоит в коде:** «предложение» рождается в момент БРОНИ, а не
«Дошло», — значит поле на `Booking` (гость предложил связь) и перенос вопроса
из `receiveGift` в `bookItem`. `Connection.consentAskedAt/consentA/consentB`
остаются, меняется момент и вид. Тикет **98b**, не переделка с нуля.

**Два вопроса заданы** (без них не строим): вид блока на подтверждении брони
(и что показывать при ВТОРОЙ броне той же хозяйке) и форма «передумать» в
«Моих бронях».

## 5. Что закрыто без работы

- **32c фильтры** — не снимаем. Но он говорит, что «Друзья» и «Связи» —
  ДВА экрана, а у нас один (лента сверху, фильтры под ней). Вопрос задан;
  пока оставляем как есть.
- **32d свой лист шера** — снят им самим. Мы его и не строили: системное окно
  плюс плашка «от пяти вещей» (тикет 104) — ровно то, что он теперь описывает.

## Что из этого стало работой

1. **тикет 107** — родной `tod` каждой базы (ждёт девять строк пересчёта);
2. **тикет 98b** — согласие по 32a (ждёт ответа на два вопроса про вид блока);
3. три строки «зал славы» — на его стороне, не наша работа.
