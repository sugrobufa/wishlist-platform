# Разбор пакета дизайна — раунд 23 («задание 19», турн 35)

Дата: 09.08.2026. Пакет: `C:\Wishlist\Wishlist платформа главный экран 19\design_handoff_round23\`
(README.md, task19.json, board-turn35.html, messages-ru.json, CHANGES.md).

Маленький раунд: три правила ленты + одно уточнение. **Отвечает на наше письмо
`BRIEF-23-zadanie-19.md`** — по правилу проверки (README называет задание,
на которое отвечает) письмо 23 закрыто.

## Что пришло и что с этим делаем

| Что | Вердикт | Куда пошло |
|---|---|---|
| Замер ночи v2 принят (0.268 против ожидания 0.27) | расхождений нет | ничего делать не надо |
| 35a — память ленты: желания 7 дней, вехи 30, кап 30 строк | берём | тикет 114 ч.2 |
| 35a — склейка: один друг в день = одна строка, приоритет, хвост желаний | берём | тикет 114 ч.2 |
| 35a — у новичка ленты нет вовсе, `Feed.empty` снят | берём | тикет 114 ч.2 |
| 35b — затемнение .56 относится к наезду в комнате, не к экрану списком | берём | В10/35b, тикет сцены |
| `Feed.mergedTail` — новый ключ | берём точечно | тикет 114 ч.2 |

Ничего не отклонено. Раунд пригоден к работе целиком — четвёртый подряд.

## Заявление дизайна проверено — на этот раз ВЕРНО

Он написал: кракозябру в `CHANGES.md` поймал и поправил сам до вердикта
верификатора, `新` в файле больше нет, а истинная форма `mergedTail` — из
доложенного в пакет `messages-ru.json`.

**Посчитали сами** (правило STATE.md «письмам дизайна не верить»):

- `CHANGES.md` — посторонних символов вне кириллицы, латиницы и пунктуации
  нет;
- `messages-ru.json` — поиск по иероглифам (Han/Hiragana/Katakana/Hangul)
  пуст;
- `Feed.mergedTail` (строка 51) цел:
  `" · и {count, plural, one {# новое желание} few {# новых желания} many {# новых желаний} other {# новых желания}}"`;
- `Feed.empty` действительно снят и помечен `empty_WITHDRAWN` с причиной
  (строка 52);
- `Connections.empty` (строка 357) НЕ тронут — и правильно: пустое состояние
  связей остаётся, снята только пустота ЛЕНТЫ.

**Счёт ложных заявлений остаётся восемь** — это девятое и оно верное, первое
проверенное «в его пользу». Отмечаем: он теперь ловит свои ошибки раньше нас.

Файл `messages-ru.json` целиком по-прежнему НЕ берём — только ключ
`mergedTail` точечным переносом (правило не изменилось и от этого раунда не
зависит).

## Что он снял сам

`Feed.empty` — по нашему же доводу: «две пустоты на одном экране». Пустое
состояние связей и пустая лента рядом читались бы как сломанный экран.
Второй раз за три раунда он снимает собственное требование по нашему разбору.

## 35b — недоразумение было наше общее, и оно закрыто

Мы собрали экран «зона списком» и приняли за него правило затемнения .56 из
task15. Дизайн уточнил: .56 — про **наезд на пустую зону В КОМНАТЕ** (панель
поверх кадра, турн 25c), гасится сцена под панелью. Наш экран списком сцены не
имеет, гасить там нечего — он собран верно и не трогается. Слой подключается
вместе со сборкой наезда; правило «не множится на ночь» — к нему.

Признал причину честно: «в task15 место слоя не было названо».

## Чего в пакете НЕТ

**Ответа про видимость сокровищницы** — и не могло быть: он ждал решения
ВЛАДЕЛЬЦА. Решение принято 09.08 (ADR-0011, три положения, дефолт «всем по
ссылке») и уходит ему письмом 24 первым пунктом, как обещано тремя письмами
подряд.
