# 225 — шкала желания встаёт своей строкой: одно число вместо трёх

**Откуда:** пакет 49, `handoff/round49/desire-scale-v2.json` (турн 55b) — ответ
на письмо 53, пункт 2. **Заменяет `round48/desire-scale.json` целиком.**

Спор о числах закрыт, и закрыт в нашу пользу с объяснением причины:

> 5/4 — числа СТРОКИ ЗОНЫ, перенесённые в карточку; та же ваша находка второй
> раз. 6/5 (round41) — попытка уменьшить то, что уменьшать нельзя. Оба снимаю.

**Одно число на карточку: точка 14, цель 44, шаг между целями 10.** Источник
назван их же файлом: `round29` говорит, что форма и карточка ДЕЛЯТ компонент,
значит числа формы и есть числа карточки. У нас точка 14 в цели 44 уже стоит —
проверить шаг (сейчас `gap: 2` в `.steps`) и довести до 10.

## Главное: мета-строка «цена · огоньки · слово» отменяется

Раскладка турна 54c держалась на точке 5 и вместе с ней уходит:

> цель 44 в мета-строку не встаёт; шкала стоит СВОЕЙ строкой, как в форме.

Сейчас у нас шкала внутри `.priceRow` рядом с ценой (это было требование
round39 «оба числа про одно»). **Теперь она отдельной строкой под ценой.**

## Числа строки (contract → `layout`)

- слово — справа, выравнивание по правому краю; при ширине экрана < 400
  переносится под ряд, по левому краю первой цели;
- тон слова: выбрано — `500 12.5` при .6; пусто — `400 12` при .6;
- **пустые точки контурные**: `border 1.5 rgba(255,249,242,.28)`, то же место и
  тот же размер (сейчас у нас заливка .2 — заменить);
- у последней выбранной ступени — `box-shadow: 0 0 14px 2px rgba(231,201,169,.5)`.
  **Внимание:** это НЕ отменяет ореол «мечтаю» (36d: «мечта в этом продукте
  всегда подсвечена») — сверить два правила и, если они спорят, взять свечение
  round49 и записать вопрос в отчёт, а не гасить «мечтаю» молча;
- цели: четыре, каждая 44×44; тап ставит свою ступень, тап по текущей — снимает
  («шкала возвращается в „не сказано"»), диалога нет: действие обратимо тем же
  тапом. **У нас это уже так** — проверить и не сломать.

## Границы

- **Точка 5 остаётся ровно в одном месте — строка зоны** (`zone-row.json →
  wishDot`), «где шкала не ввод, а взгляд: там её не нажимают». Не трогать.
- Гостевой показ (`DesireScale`, 6 px с шагом 5) контракт 49 не упоминает —
  не трогать, записать в отчёт как открытый хвост.
- Слова: пусто — `ItemCard.desireEmpty`, ступени — `AddItem.desire1–4`, вопрос
  формы `AddItem.desireLabel` только в форме. Новых ключей нет.
- Форма добавления: числа те же (она и есть источник), но раскладку формы этот
  тикет не меняет.

## Проверка

- юниты по числам: цель 44, шаг 10, точка 14, контур пустых, свечение выбранной;
- юнит: шкала НЕ внутри строки цены — своим блоком; слово справа;
- прежние юниты шкалы зелёные (тап по текущей снимает степень, пустое пишет
  «не сказано, насколько хочется», у витрины шкалы нет);
- `npm run test`, `npm run lint`, `npm run typecheck`.
