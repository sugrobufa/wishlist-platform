# Разбор пакета 48 — ответ на письма 52 и 51

**Приехал** 13.08.2026 16:31 — через одиннадцать минут после того, как письмо
52 легло в папку отправки. Скопирован в репозиторий в тот же час, до единой
строки кода (`design/package/handoff/round48/`), по правилу папки: браузер
кладёт следующий пакет НА МЕСТО предыдущего.

Семь файлов, турн доски 54 (54a–54e). Отвечает на два письма разом: **52** —
приёмка владельца (пять просьб), **51** — 33 ключа, обещанные пакетом 47.

**Итог разбора: пакет сильный, спор один — числа шкалы.** Из пяти просьб
письма 52 закрыты пять; из них наши редакции заменены четырьмя его, и в трёх
случаях его довод сильнее нашего. Заведены тикеты 219, 220, 221.

---

## Проверка первая: обещанное против приложенного

**Класс ошибок, на котором мы теряли раунды пять раз** («38 ключей обещано,
5 доставлено» — письмо 51). Считаем сами.

README обещает «33 ключа письма 51 (28 `Pool.*` + 5 `FriendRow.*`)».

| блок | настоящих строк | служебных пометок |
|---|---|---|
| `Pool` | **28** | 1 (`_rule`) |
| `FriendRow` | **5** | 3 (`_rule`, `_accent`, `_countdownRule`) |

**28 + 5 = 33. Сходится — впервые за шесть раундов этого класса.** Объяснение
арифметики («в прозе round47 `markBoughtWhoCan` шёл внутри чужого пункта, а
шестым ключом строки друга стояло правило `_accent`, которое строкой не
является») проверено по тексту round47 и верно.

**Тикеты 211 и 212 разблокированы.**

## Проверка вторая: заявления о наших данных

| заявление | проверка | итог |
|---|---|---|
| «`Occasion.closing` = „Открываем…", поэтому глагол „открыть"» | ключ есть, значение то самое | **верно** |
| «полоса горит 14 суток после праздника — тем же сроком, что комната» | `OCCASION_TAIL_DAYS = 14`, `dueOccasion` | **верно, и у нас уже так** |
| «тикет 217 принят без правок» | таблица `buttonLife` совпадает с нашей построчно | **верно** |
| «`Occasion.thanksAll` (существующий)» | нет ни в `messages/ru.json`, ни в `handoff/messages-ru.json`, ни в дельте 48 | **НЕВЕРНО** |
| «круг — родство со знаком витрины на кадре» | `.imm-corner-mark` в `globals.css` — квадрат, `border-radius` нет вовсе | **НЕВЕРНО** |
| «наша ошибка: `pool.refund` „деньги вернулись" — проверьте, не уехала ли к вам» | в `messages/ru.json` строки нет; в НАШЕЙ копии его `messages-ru.json` она стоит дважды (449, 455) | **до реализации не доехала** |

Про `pool.refund` стоит сказать отдельно: строка прожила у него в словаре
столько времени потому, что раздел назван со строчной буквы (`pool`), а наш
сторож тона по устройству читает только разделы с прописной — со строчной у
него разделы брифа, а не словаря. То есть правило нарушалось ровно в том месте,
которого не видит ни его глаз, ни наша машина.

## Проверка третья: числа шкалы — спор

`desire-scale.json` даёт огоньки **5 px с шагом 4** и одну цель нажатия на всю
полосу. Не берём, и вот почему — три довода, все проверяемые.

1. **Эту пару он уже снимал сам.** `round41/item-card-owner.json`, строка 21:
   «В карточке огоньки **6 px с шагом 5** — числа 5/4 из нашего файла были
   числами СТРОКИ ЗОНЫ, вы поймали верно». То же в его `round41/README.md`.
   `zone-row.json → wishDot` подтверждает: точка строки зоны — 5.
2. **Числа спорят внутри самого пакета 48.** Четыре точки по 5 с шагом 4 — это
   32 px на всю лестницу, а `hitTarget` объявляет целью полосу целиком, высотой
   44. Тапнуть третью ступень в такой полосе нельзя — а правка тапом
   (`round29 → editInPlace`) его собственное правило и наше живое поведение.
3. **Живых чисел карточки три, и все от него:** 14 (round29, форма и карточка
   делят компонент), 6/5 (round41, «в карточке»), 5/4 (round48). Просим одно.

## Проверка четвёртая: тон входящих строк

Правило заведено после раунда 43: **входящие строки гоняем своим сторожем ДО
того, как класть в словарь.** Прогнали.

**Блок `Occasion` (24 новых ключа) — чисто.** Все положены в словарь, сторож
зелёный.

**Блок `Pool` — 10 строк из 28 останавливаются:**

- **род человека — 8 строк.** `handedOver` («передала {organizer}») и
  `notHandedYet` («пока не передал») — это та же болезнь, что снятая пара
  `quietGiver`/`quietGiverF`: род, разложенный по разным строкам одного блока.
  Плюс `markHandedOver`, `leaveHandedTitle` («Ты уже отметил, что передал»),
  `markBoughtWhoCan` («она покупает»), `organizerLine` («деньги у неё»),
  `leaveHandedLine`, `failedMoney`. Проверка его же памятки: подставить в
  `{organizer}` мужское имя — ломается каждая;
- **обращение на «вы» — 3 строки** (`ownerBlind`, `overLine`, `failedMoney`).
  Здесь вопрос, а не приговор: складчина — единственный экран продукта, где
  адресат ВО МНОЖЕСТВЕННОМ ЧИСЛЕ («деньги вы передаёте друг другу сами»), и
  памятка про «ты» писалась не про этот случай. Наш сторож различать не умеет.

**Строки в словарь не кладём** — тикет 211 ждёт редакции. Это не отказ: 18
строк из 28 готовы, геометрия подробная, вся работа упирается в один проход
редактора.

## Что взято в работу

| тикет | что | источник |
|---|---|---|
| **219** | экран итога: порог с блоком «что случится», ожидание со счётчиком, комната без даты | `occasion.json` 54a/54b |
| **220** | витрина: «уже твои» в строку счётчика + `ownerHint` через комнату | `hall-owner.json` 54d |
| **221** | карточка: «не сказано, насколько хочется»; подложка круглая, тень снята | `desire-scale.json` 54c, `photo-signs.json` 54e |

Три довода дизайна против наших редакций приняты как более сильные:

1. «вещи, которыми гордишься» **ставит человеку условие** — в сокровищницу
   приезжает всё, что уже твоё, включая подаренный крем;
2. в карточке шкала — **ответ, а не вопрос**; «не скажу» законное значение, а
   вопрос на его месте читается приглашением заполнить;
3. «показать» **обратимо** — так говорят про фильтр, а раскрытие имён нет.

## Чего не взяли и почему — ушло письмом 53

1. **`Occasion.thanksAll`** — ключа не существует, состояние OPEN не трогаем.
2. **Фотография в шапке итога** (250/236, прозрачность, градиент) — контракт не
   говорит, ЧТО это за фотография, а у нашего экрана фона нет вовсе.
3. **Числа шкалы 5/4** — см. проверку третью.
4. **Круг у знака витрины на кадре** — меняем только карточку; наш
   `.imm-corner-mark` квадратный, и «родство» сегодня работает в другую сторону.
5. **`Pool.*` в словарь** — до редактуры рода.
6. **`aheadNoLoud`** — строку взяли (контракт называет ей место, footer), но
   спросили: продукт объясняет пользователю СВОЁ ЖЕ проектное решение
   («горящая полоса семь месяцев подряд перестаёт что-либо значить»). Довод
   верный, но это довод для нас, а не для хозяйки комнаты.
