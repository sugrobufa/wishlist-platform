# CHANGES · round53

## Новое

- **Пул `bar`** в `seeds.json` — 6 вещей, фотографий нет. `stats.pools` 19 → 20,
  `stats.seeds` 95 → 101.
- **Правило ярлыка** в `zones.json → _badgeRule`: ярлык = счётчик − 5, прочерк
  при нуле.
- **Правило «отражение — не предмет»** (`lux-music.json`).
- **Правило «прямоугольники не вкладываются»** (`study-overlap.json`) — пара к
  «прямоугольник берёт опору».
- **Проверка дампа на скопированные координаты** (`rect-audit.json`).
- **Правило «замеренное число носит с собой снимок»**.

## Изменено

- `zones.json → keys.bar[4]`: «+3» → «—»;
- `Onboarding.setDescriptionM`: дописан бар;
- `rooms.json → zonesNote`: 7 → 8 живых полок, карта 134 / 126 / 8 / 0;
- `design_handoff_round41/places-v3.3.json → drawIfWholeMeasured`: 23 → 21,
  с указанием дампов (правка на месте — это замер, а не вариант).

## Принято без правок

- Четыре прямоугольника разметки (`warm/music`, `sport/watches`,
  `study/tech`, `study/bar`) — опора вошла в каждый;
- `lux/music` в живых полках;
- числа карты 134 / 126 / 8 / 0.
