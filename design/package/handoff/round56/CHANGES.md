# CHANGES · round56

## Снято

- **`order.label`** из `desire-guest.json` — строки «сначала то, чего хочется
  сильнее» не будет; `newStrings: 0` становится правдой.
- **Витрина из гостевого бара** — её там не должно было быть; знак живёт в углу
  кадра (36d).
- **Требование притяжательного во втором пункте бара** — пункт другой.

## Добавлено

- `GuestRoom.tabRoom` = «Комната {name}» — единственный новый ключ.
- `tag-thread.svg` — готовая нить бирки, ×1.

## Правки турнов на месте

- **61d** — бар: «Её витрина» → «Мои подарки», с пометкой о правке;
- **56b** — уточнение уже стояло там же (правило 36d), не трогалось.

## Исправлено у нас

- `guest-gaps.json`: `AddItem.kindMine` → `Hall.captionMine`.

## Без изменений

Порядок `desire 4 → 1` (ваш тикет 124), точка 5 в сетке, точка 14 в карточке,
отсутствие фильтра и переключателя порядка.
