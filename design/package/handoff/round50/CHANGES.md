# CHANGES · round50

## Отменено из наших прежних пакетов

- **Вердикт «знак витрины не круглить»** (round49/signs-shape.json) — опирался
  на моё незнание кода. Знак витрины, «Друзья», «Настройки» — круги 44.
- **Подложка метки 36** → 44: у кнопки подложка равна цели.
- **Пилюля «Свет включится, когда появятся вещи»** (турн 51d) — снята.
- **Полоса прогресса и «11 мест» при нуле вещей** (турн 42) — появляются с
  первой вещи.
- **«Комната твоя. Пока в ней темно»** (турн 41a) — пересказ экрана.
- **«Первая — самая заметная»** (пакет 47) — снято как неправда.
- **`Settings.tryOnLineAlt`** — снят, живёт редакция разработки.
- **Строка «почты ни у кого нет — писать некуда»** — не завожу.

## Добавлено в словарь

- `Occasion.thanksSubject` = «Спасибо за подарок» (тема mailto, тело пустое);
- `Occasion.emptyBackToRoom` = «В комнату — вещи на месте»;
- `Occasion._zero` — правило нуля;
- `Settings.tryOnLine` — редакция разработки, слово «кадр» снято.

## Выпрямлено

- `ZoneCounterGuest`: `cases.*` → плоские ключи, `ownerVariant.text` → `owner`;
- `PoolInline`: `participants/progress/join/refund` → `participantsOne`,
  `progressCollecting`, `joinCta`, `refundLine` и т.д.; `examples` →
  `_examples` строкой.

## Поправлено при сборке (наши же числа)

- **Бар 86**, не 120: число из `round41/tokens.json`. В первой редакции
  `guest-frame.json` стояло 120 — геометрия паспарту считалась бы от неверной
  высоты.
- **Витрина убрана из бара** в обоих режимах — решение турна 36d: знак в углу
  ближе к метафоре. Третий таб — «Мои подарки».

## Ждёт слова владельца

- `objectAbsent` как **живая полка без места на кадре** (сейчас 404 + вещи
  уезжают в «Что угодно»);
- предел сообщений пустого состояния: одно обещание + одно действие.
