# Пакет round51 — ответ на письмо 56

Турн доски: **57** (57a, 57b). Числа состава в этом пакете посчитаны скриптом
по файлам, не глазами — см. errata в round50/README.

## Решение владельца о живой полке сделало один экран обязательным

До сих пор список полок был удобством. Теперь это **единственная дорога к семи
полкам из 134** — и его нет ни в турнах, ни в списке P0 (19b обещал «зону
целиком списком», это другой экран). Нарисован: **57a**, контракт
`zone-list.json`.

**Главное решение экрана — он ничего не помечает.** Соблазн поставить у семи
полок тихую подпись «на кадре её нет» отклонён по тому же доводу, которым мы
снимали «писать некуда»: это сообщение об отсутствии. Человек не знает, что у
полки бывает место на кадре, и узнавать ему незачем — полка открывается, вещи
внутри, всё работает.

Следствие, которое стоит записать: **новое состояние не потребовало ни одного
нового знака**. Если бы потребовало — значит согласились не «закрыть дырку», а
завести второй сорт полок.

## Новые числа карты

| | было | стало |
|---|---|---|
| адресов в словаре | 130 | **134** |
| с местом на кадре (наезд работает) | 122 | **127** |
| живут списком (наезда нет) | — | **7** |
| скрыто адресно (404) | 8 | **0** |

134 = 13 × 10 комнат + «Бар и табак» в четырёх мужских.
7 списком = `emerald/beauty` · `sport/gaming` · `study/gaming` ·
`loft/gaming` · `gamer/bar` · `loft/bar` · `sport/bar`.

**Инвариант №9 переписан целиком** — дословный новый текст в
`zones-map-v2.json`. В старом неверны все четыре числа и оба следствия, причём
одно следствие отменяется полностью: **«вещи уезжают в „Что угодно"» больше не
про это** — уезжать некуда и незачем, полка жива. Правило остаётся только для
переезда между интерьерами, где полки действительно не совпадают.

## Мужской набор стал 14, женский остался 13

Первое место, где наборы разошлись длиной, а не составом. Владелец назвал «Бар и
табак» мужской полкой прямо, спорить не с чем — но **положение «Всё вместе» в
настройках теперь даёт 15**, а не 14, и подпись `Onboarding.setDescriptionALL`
(«Все полки сразу — разберёшься по ходу») верна только если бар в него входит.
Проверьте у себя.

## «Часы» → «Часы и аксессуары»

Подпись в `handoff/zones.json` заменена. **Глагол раскрытия не менялся** —
«крышка поднимается» про бокс с часами, а бокс в кадре есть. Аксессуары
приезжают в ту же полку, кадр менять не нужно.

Новая полка `bar`: подпись «Бар и табак», глагол «графин ловит свет», сценарий
съёмки «графин ловит свет → тележка выкатывается в кадр», глагол добавления
«Поставить на тележку».

## Приложены целиком

- `handoff/zones.json` — с `bar` и новой подписью часов;
- `handoff/messages-ru.json` — с разделом `ZoneList` (7 живых строк).
