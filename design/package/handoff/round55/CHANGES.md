# CHANGES · round55

## Кадры

- `loft` — перерисовать участок билетов: рама с паспарту вместо веера и серой
  пластины. Границу шва вести по белой стене, растушёвка 40 px.
- `study`, `gamer` — **не трогать**, там уже рамы.
- `sport` — ждём вырезку.

## tokens.json

- `button.gift`: 218×66 → **124×44**, одно слово; отверстие 11 с центром
  16.5/22; нить — три дуги, продета в проём; тень 0 4px 7px; ось поворота
  16.5px 22px.

## Словарь: добавлено

- `Settings.setSwitchHint` — цена смены набора, число считается;
- `Hall.guestTitle` = «Сокровищница {name}»;
- `Booking.inviteMore` = «Позвать ещё дарителя»;
- `GuestRoom.shareRoom` = «Позвать дарителя».

## Словарь: изменено

- `Onboarding.setLabelF/M`, `Settings.setLabelF/M` — единственное число на
  множественное: «Женские», «Мужские»;
- `Onboarding.zoneSetHint` — «Потом можно поменять всё» → «Сейчас поменять можно
  без потерь — вещей ещё нет»;
- `Hall.ownerHint` — редакция письма 60.

## Словарь: снято

- `Onboarding.setLabelALL`, `Onboarding.setDescriptionALL`,
  `Settings.setLabelALL` — положения больше нет;
- подстановка `AddItem.kindMine` в гостевом виде витрины — подписи нет вовсе;
- `Booking.tagFor` с бирки (ключ остаётся в диалоге брони).

## Не заводится

- Строка «уже дома» под вещами чужой витрины — третий повтор `guestSubtitle`.
- Фильтр по степени желания.
- Переключатель порядка в полке.
- Отдельный диалог смены набора комнат.
