# Что изменилось — round44

## Новые ключи словаря (23)

Карточка вещи — в существующем разделе `AddItem`:

| Ключ | Строка |
|---|---|
| `cardPriceVisALL` · `FRIENDS` · `ME` | цену видят все · цену видят друзья · цену видишь только ты |
| `cardShelfLine` | Стоит {where} |
| `cardAddedOn` | Стоит в комнате с {date} |
| `cardStores` · `cardStoresFrom` | Где купить · {count} магазина · от {price} |
| `cardNoteOverline` | Заметка |
| `cardAddPhoto` | Добавить фотографию |
| `cardPoolNote` | Дорого одному — скинуться с другими |
| `cardQuietHint` | никто не узнает до праздника |

Занятая вещь у гостя — `ZoneGrid.guestTakenTitle` = «уже дарят»,
`guestTakenHint` = «в комнате есть свободные».

Праздники — `Occasion.birthdayLabel`, `birthdayRepeat`, `nearestLine`, `offerTitle`,
`offerLine`, `offerShow`, `offerSkip`, `addOwn`, `ownHint`.

Примерка — `Settings.tryOnLineAlt` (альтернатива вашей редакции, обе годятся).

## Наш `handoff/README.md` — три устаревших места, исправлены

1. **Блок «Главная идея» учил модели v1** — «у вещи два состояния», «переход хочу → люблю»,
   «отправляет вещь в зал славы». Модель отменена письмом 25 двенадцать дней назад;
   `items.json` знал об этом, README — нет. Перезаписан на модель v2.
2. **Строка про карточку вещи** — та самая, из-за которой собрали форму вместо экрана
   чтения. Заменена перечнем турнов 47a · 51a · 51b · 51c с прямой пометкой, что прежняя
   строка врала до 11.08.
3. **«витрина зала славы»** в описании `refs/` → «витрина сокровищницы».

## `tone.md`

Новый раздел: прогон **`toneCheck`** из четырёх правил вместо `genderCheck` из одного —
род · падеж токена · технические слова · точка в конце короткой строки. Причина записана
прямо там: правил у нас было больше, чем прогонов.
