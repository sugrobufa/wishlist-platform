# CHANGES · round52

## Изменено в словаре

- `Onboarding.setLabelALL`: «Всё вместе» → «Все десять»;
- `Onboarding.setDescriptionALL`: «Все полки сразу — разберёшься по ходу» →
  «От кремовой спальни до кабинета»;
- `Onboarding.zoneSetHint`: «Наборы отличаются полками» → «Наборы отличаются
  комнатами, а комнаты — полками»;
- `Settings.setLabelALL`: «Всё вместе» → «Все десять».

## Добавлено

- `Settings.setOverline` = «Комнаты на выбор» — имя настройки в профиле;
- `Onboarding._zoneSet`, `Onboarding._setDescriptions`, `Settings._setOverline`
  — правила рядом со строками.

## Не менялось намеренно

- `setDescriptionF`, `setDescriptionM` — у этих двух положений различие правда
  в полках;
- `handoff/tone.md` — запрет слова «набор зон» был прав.

## Исправлено в документации

- `handoff/README.md`: настройка выбирает комнаты (6/4/10), называется «Комнаты
  на выбор»; строка индекса раунда 10 больше не говорит «свой набор зон»;
- `handoff/rooms.json → zonesNote`: 13/14 полок, 134 адреса, новая редакция
  инварианта 9, зона без прямоугольника — живая полка.

## Правка на месте

Турн **57b** — примечание про «15» переписано там же, а не новым турном: это
неверное число в контракте, а не вариант. Пометка о правке стоит в примечании.
