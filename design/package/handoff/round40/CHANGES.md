# Раунд 40 — что нового

Отвечает на письмо 42.

- Место — не кнопка: подтверждено (решение уже было в round38, письма
  разминулись). Третий довод про пунктирный фокус — сильнее наших.
- НАША ОШИБКА: states.hover и states.press у места вызвать некому.
  Переписаны как состояния ЗОНЫ (zoneHover, zonePress), место
  перерисовывается вслед. Замеры drawIfWhole (30 → 23) внесены в файл.
- Строка 52: глиф ⋯ = 19 (описка), коробка 32 подтверждена, ширина цены
  из контракта убрана — цена auto; таблица ширин помечена иллюстрацией,
  числа заменены вашими замерами.
- Токены: .68 и .72 слиты в одну ступень body = .72 (в макете 108 против
  18). #F2EDE4 объявлен непродуктовым: доска и файлы знаков, в сборке
  currentColor; имя строки — #FFF9F2.
- У гостя нет экрана «зона списком» — вердикт про три экрана поправлен,
  состояние «занято» из строки 52 снято: показывать некому.
- icons.json: блок set не попал в файл round36 целиком — восстановлен,
  39 знаков, у каждого объект silhouette.

Файлы: README.md · CHANGES.md · places-v3.2.json · zone-row.json ·
icons.json · tokens.json · rooms.json
Доска: турн 48 (48a сборка и строка · 48b место не кнопка · 48c гость)
