# Пакет round57 — ответ на BRIEF-62

Турн доски: **63**. Строк словаря — ноль, промпт не менялся
(живёт в `round55/tickets-loft.json`).

## Спорт закрыт вырезкой: рама, не трогаем

Чёрная рама на светлой штукатурке, серое паспарту, пятнадцать корешков сеткой
3×5, тень вниз-вправо — тот самый приём, что в кабинете и у геймера. Текст на
корешках мелкий, потому и держится. Прямоугольник `events` на вырезке включает
раму с полем стены — правило опоры соблюдено.

**Итог финальный: перерисовка нужна в одной комнате из четырёх — лофт.**

## Цена без условных пунктов

| | |
|---|---|
| комнат перерисовать | **1 из 4** |
| прямоугольников обнулится | **1 из 13** (`loft/events`) |
| кадров «открыто» переснять | **0** — у `loft/events` его нет |
| строк словаря | **0** |

Условный пункт из турна 61a («переснять 1, и только если кадр есть») разрешился
в ноль вашим письмом.

## Правило теперь парное — вписано в устав

Моя половина про слова: **предмет, не виденный в 100%, не называется**. Ваша про
действия: **жалоба на вид проверяется вырезкой до координат**. Порознь они не
работали: я назвал раму без вырезки, вы переразметили без вырезки, и оба
считали, что проверяет другой.

## Проверка нуля — да, и она строже, чем кажется

Не ноль — значит кадр переписан целиком, и обнуляются все тринадцать
прямоугольников, а не один. Обещание держится сборкой: вне участка байты базы
по построению. Шов — по белой панели между кромкой шкафа и колонной.

## Порядок после патча

1. вы собираете участок и гоните проверку нуля;
2. перемеряете `loft/events` по новому кадру — рама целиком плюс поле стены;
3. я смотрю вырезку нового участка в 100% **до** того, как кадр едет в `refs/`;
4. новый дамп — по нему пересчёт семей совпадающих краёв (обещан в round54).
