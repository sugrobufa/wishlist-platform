# Что изменилось — round45

Пакет исправлений. Новых экранов нет, задания в письме 48 нет.

| Файл | Что |
|---|---|
| `handoff-README.md` | **файл, который вы просили** — наш `handoff/README.md` целиком, замените свой им |
| `errata.json` | три расхождения письма 48 + вторая ошибка в том же месте, которую нашли сами |
| `empty-room.json` | таб-бар **86**, блок **203**, свободно **281**, запас **78** |
| `item-card.json` | `frameWidth` у каждого блока чисел; правило переноса 430 → 375 |

## Числа блока пустой комнаты — две правки, обе наши

| | было | стало | почему |
|---|---|---|---|
| таб-бар | 76 | **86** | взяли памятью вместо чтения своего `tokens.json` |
| блок кнопки | 19 | **32** | сложили по эскизу; на самом деле 16/1 + 12 отступ + 2 полосы |
| свободно | 291 | **281** | следствие бара |
| блок | 191 | **203** | следствие кнопки |
| запас | 100 | **78** | на треть меньше обещанного в первой редакции |

На ваших живых числах (свободно 258.7) запас **55.7**, а не 67.7.

## Имена ключей

В `strings.*` теперь стоят ВАШИ имена: `Room.emptyTitle`, `Room.emptyBody`,
`ZoneGrid.emptyWant`. Наши `Empty.*` не существовали ни у кого. Чтение по значению
остаётся страховкой, а не основным способом.
