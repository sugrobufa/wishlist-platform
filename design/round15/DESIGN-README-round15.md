# Задание 12 — значки, два кадра, вердикт по study

## 1. 19 значков пулов — в `icons/`
`pool-{ключ}.svg` по всем 19 ключам из письма. Сетка 24, контур 1.7, концы и углы
скруглены, без заливок — как иконки интерфейса. Проверены на 24 px (строка списка),
40 px и плитке 72: после первого прогона перерисовал пять мутных — fashionM стал
галстуком (пиджак и платье на 24 px сливались в арку), помада шире, бритва крупнее
головой, гантель с зазором, тюльпан с одним вырезом. Цвет в файлах #F2EDE4 —
в сборке подменяйте stroke на currentColor/нужную ступень.
Money в списке не было — конверт уже решён отдельно; если нужен файлом, скажите.

## 2. warm/anything — пересъёмка, команда исполнена дословно
Движение ТОЛЬКО у той крышки, что в базе: она поднялась с пола у пуфа и легла
поперёк коробки, полуоткрыто. Без банта, лента развязана и свисает как в базе,
бумага не тронута. 1 коробка, 1 крышка до и после.
Числа моим методом (фон = кадр): контрактный 0.0408 / 2.03, дизайнерский 0.0423 / 2.13 —
ниже вашей планки «принятых по глазам» (2.77). Жест физически мал: крышка сменила
место, диф размазан по двум небольшим пятнам. Кадр в пакете, п1 (крышка уехала на пол,
глазами слабее) — в masters. Решение по вашим прямоугольникам-соседям — ваше.

## 3. study/travel и loft/travel — по числам задания 11
Вырезки `rects-study-loft/` к письму не приложились (в папке их не было) — снимал
по принятым числам {0,235,88,62} и {0,238,80,70}, сверить глазами по вырезкам не мог.

**loft — в пакете**: own 0.1273 / фон 0.0218 / **5.84** по принятому ректу. Коробка
лежит на месте, четыре клапана раскрыты вверх, бумага видна; чёрная коробка, кофр
и сумка не тронуты. Одна оговорка честности: транспортный лейбл переехал с верхней
грани на переднюю и укрупнён — предмет тот же и один, но деталь перерисована.
Если по вашей приёмке это брак — пересниму с фиксацией лейбла.
П1 — брак, отвергнут мной: модель поставила коробку вертикально и не открыла (в masters).

**study — кадра нет: 3 попытки, 3 разных провала** (все вещдоки в masters):
1. Крышка открыта, но камера ИСЧЕЗЛА из кадра, крышка крупнее сундука (метрика 5.94 —
   обманула бы, глаза нет).
2. Верхний сундук ЛЕВИТИРУЕТ над нижним, полка перерисована: две пары туфель стали
   четырьмя (4.67 — тоже обманула бы).
3. Консервативная «крышка на щель» — no-op 0.0191.
Диагноз тот же, что emerald/anything: стек у обрезанного края кадра, модель достраивает
невидимое. Останавливаюсь по протоколу; предлагаю оставить «наезд есть, кадра нет» —
или ваш разбор, как по emerald.

## 4. Остальное по заданию
- lux/travel, bold/anything, gamer/anything, loft/anything — **не трогал**, жду ваши
  прямоугольники вырезками.
- emerald/anything — не трогал.
- Пересечения моих rectProposal с соседними зонами понял: мерил без карты чужих зон.
  Если пришлёте полные карты зон комнат (или добавите их в rooms.json поставки),
  следующие обмеры отдам уже с проверкой на пересечения и на x≥12.
