# Задание — раунд 7

Дата: 2026-08-06. Ваши ответы раунда 6 приняты все четыре, реализуем мы.
Ниже — что снимать, что рисовать, и что мы должны вам сами.

---

## 0. Сначала признание нашей ошибки — она снимает старую претензию

В брифе 04 мы писали, что комплекты «Тихой роскоши» и «Лофта» «сняты новой
генерацией, а не правкой кадра» — расхождение композиции 0.0727 и 0.0685 при
пороге 0.05. **Это была ошибка нашего измерения, не вашей съёмки.** Наш скрипт
неверно опознавал базовый кадр (у warm за базу принимал «открыто» для
«Цветов») и мерил против него. После починки опознания обе комнаты дают
0.047 и проходят порог штатно.

Оба комплекта подключены. Все десять комнат теперь на кадрах 2800 px,
владелец подтвердил разницу на своей комнате. Прямоугольники на новых кадрах
сверены глазами — стоят на предметах.

Из этого один практический вывод для вас: **порог у вас не «капризный», ему
можно верить.** Оба ложных срабатывания за всю историю были нашими ошибками
измерения, не порогом.

---

## 1. Пересъёмка: 87 зон можно снимать ПРЯМО СЕЙЧАС

Всего без кадра — **97** зон. Десять ждут наших прямоугольников (§2),
**остальные 87 снимайте, не дожидаясь нас**: их прямоугольники вы проверили
глазами в раунде 5, мы сверились — верны. С учётом трёх общих кадров
(`sharedObjectWith`) это 85 съёмок.

Рецепт ваш же (`reshoot-recipe.md`): материалы и свет не описывать, вместо
описания — запрет менять.

Порог приёмки прежний, проверяем мы: своя зона ≥ 0.05, отношение к фону ≥ 3.
Три общих кадра (`sharedObjectWith`) — как вы предложили, один кадр на пару.

Формат поставки прежний: `handoff/` + ссылки на оригиналы 5504 в
`masters.json`. `accepted` выводится из версий — это ваше решение, оно
работает, спасибо.

### Ваш последний пакет мы ещё не приняли — и это осознанно

В коде сейчас предыдущая версия контракта (`accepted: 39`, `objectAbsent: 8`).
Последний пакет с `placeBased`, `rectVersion` и выведенным `accepted: 33`
примем **вместе с нашими прямоугольниками** для десяти зон из §2 — иначе
восемь place-based зон включатся с координатами, которые, как мы проверили,
в пяти случаях попадают в букет, в зеркало и в штору.

То есть ваш пакет не отвергнут: он ждёт нашей части работы, а не вашей.

## 2. Десять зон ждут наших прямоугольников — пришлём отдельным файлом

Как договорились: место вы называете словами, числа ставим мы. За нами:

- **восемь place-based зон** (ваши словесные описания у нас есть, из восьми
  геометрических попаданий было два — но это уже наша забота);
- **`cream`/Впечатления** — нашли при живой проверке: прямоугольник совпадает
  с «Цветами» до координаты (похоже, скопированная строка) и стоит на вазе,
  а не на доске с билетами. Единственная пара с одинаковым началом на все 130;
- **`warm`/Книги** — на новом кадре шкаф закрыла разросшаяся ваза с пионами.
  Разметим на видимую часть корешков либо переведём в place-based.

Пришлём файлом `rects-fix.json` (та же форма, что ваши записи в `rooms.json`,
с поднятой `rectVersion`). Эти десять зон снимайте **последними**, после
нашего файла.

## 3. Иконки — в работе у вас, ждём

Сетка 24, контур 1.7, скруглённые концы (турн 12a). Таб-бар по вашему
ответу: над комнатой, по тяге снизу — значит нужны и его состояния
(свёрнут / вытянут / в списках, где он постоянный).

## 4. Экраны — список в силе из прошлого письма

1. **Карточка вещи глазами гостя**: где купить, размер и цвет, кнопка брони.
2. **Карточка вещи глазами хозяйки**: изменить, перенести на другую полку,
   скрыть, удалить. Мы это уже построили в коде — нарисуйте, сверим и
   поправим вёрстку по вашему макету.
3. **Пустая зона** (пустую комнату вы рисовали, пустую зону внутри — нет).
4. **Пустая комната на главном** — человек в первый день видит ноль вещей,
   на всех макетах 142.

## 5. По вашим ответам раунда 6 — что нужно от вас словами

Все четыре решения приняты и уходят в код без вас. Но два из них требуют
строк, а слова — ваши:

1. **Зонный счётчик гостя**: «занято и свободно» по зоне. Дайте формулировки
   для `messages-ru.json` (единственное/множественное, ноль занятых, всё
   занято). Наши черновые: «3 свободно · 1 занято» — если у вас лучше,
   давайте ваши.
2. **Складчина, имя между гостями**: подпись участника складчины видна другим
   участникам. Формулировка строки «кто уже в складчине» — за вами.

Телефон из макетов уберите при следующей правке доски — поле решено убрать
целиком, не оставлять пустым.

## 6. Что уже работает на стенде (не рисовать заново)

Комната на кадрах 2800 во всех десяти · координаты в системе кадра, зоны
стоят на предметах · указатель зон 01…13 · метки светом · походка камеры ·
выбор «люблю/хочу» кропами комнаты · ссылка на магазин у гостя · карточка
вещи хозяйки (правка, перенос) · тихая бронь закрыта и для хозяйки,
открывшей собственную ссылку.

Комната во весь экран на десктопе — в работе прямо сейчас, вопрос владельца
закрыт решением: кадр заполняет вьюпорт кропом по центру композиции, как вы
и писали в турне 23 («кроп — свойство показа, карта координат не меняется»).

---

## Итого ваша очередь

- [ ] ~79 пересъёмок — сразу, не ждя нас
- [ ] 10 зон — после нашего `rects-fix.json`
- [ ] Иконки + состояния таб-бара
- [ ] Карточка вещи ×2, пустая зона, пустая комната
- [ ] Строки: зонный счётчик гостя, складчина
- [ ] Убрать телефон из макетов
