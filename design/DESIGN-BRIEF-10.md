# Задание 10 — чемоданы приняты, коробки давай, экраны давай

Дата: 2026-08-07. Ответ на обе ваши поставки раунда 12.

---

## 1. Предметные кадры — приняты все 21, уже на проде

Померили сами: **21 из 21** — 400×400, 7–22 KB. Глазами смотрели шесть самых
рискованных, включая обе пересдачи: тиснёный лавр, конверт с сургучом,
гильошные билеты, карту музеев, кресло (база на месте), триммер (стоит, не
левитирует). Текста, леттеринга и логотипов нет ни на одном.

Символы вместо событий — то, что нужно. Тбилиси конвертом читается на плитке
160 px; пейзаж бы там не прочитался.

Что это дало в цифрах:

| | было | стало |
|---|---|---|
| примеров без фотографии | 54 из 80 | **33 из 80** |
| пулов пустых целиком | 5 | **0** |
| предметных кадров в пакете | 16 | **37** |

Демо-стенд пересеян: 55 вещей, 31 фотография, ни одна не потерялась.
«Путешествия», «Книги», «Впечатления» — 4 фото из 4.

---

## 2. Чемоданы — файлы до нас НЕ доехали

Ваш отчёт про 4 из 7 мы прочитали, **но самих кадров у нас нет**. В обеих
папках раунда 12 лежат только предметные кадры (`delivery-1`, `delivery-2`);
ни одного нового `o-*.jpg` не пришло.

**Пришлите четыре файла** — `warm`, `emerald`, `cottage`, `lux` — и с ними
числа, которые вы считали, и sha256 базы, от которой снимали. Порог проверим
своим `check-frames.mjs` от НАШИХ `refs/` (правило не наше упрямство: дважды,
в раундах 8 и 11, база оказывалась архивной копией 2400×1340).

Про `lux` отдельно: если порог добрал шёлк, свисающий внутрь прямоугольника,
скажите об этом прямо в письме. Нам важно знать, чем именно пройден порог, —
это влияет на то, поверим ли мы числу без глаз.

---

## 3. Коробки — давай

Решение владельца от 07.08: **раскрытия возобновляем**, партию 2 из
`BRIEF-lids.md` (8 зон, «коробка раскрывается») снимаем.

Основание — ваш результат. Мы закрывали тему на числах «0 из 7 подключено»;
вы показали 4 из 7 от наших баз и с первой попытки на трёх. Метод работает,
и держаться за отмену теперь незачем.

Две зоны партии помечены как опасные, там жёстче обычного — **та же коробка,
того же цвета и формы, просто с поднятой крышкой**: `lux` (в раунде 9
чёрная лаковая коробка на хромированном пуфе заменилась стопкой кофров) и
`study` (стопка зелёных коробок на пуфе заменилась другой).

Ваше правило «проверять, крышка ли в прямоугольнике, ДО съёмки» принимаем и
просим применять к обеим партиям. Оно ровно про то, обо что споткнулись
`study` и `loft`.

---

## 4. study и loft — присылайте обмеры

Вы написали, что прямоугольники брифа стоят ниже места жеста (у `study` рект
на нижнем сундуке, который не открыть — на нём стоит верхний), и предложили
свои с обмерами. **Предложенных обмеров мы не видели** — их не было в
поставке.

Пришлите их **изменёнными полями, а не `rooms.json` целиком** (правило
прежнее: файл контракта переносим точечно, иначе стираются поля приёмки).
До вашего ответа эти две зоны не снимайте — здесь вы правы.

---

## 5. bold — решаем мы, и вот решение

Hard-shell, который не открывается частично, — это не брак съёмки, это
свойство предмета. Подмену П1 **не берём**: подмена предмета — ровно то, что
мы три раунда ловили глазами и отвергали, и делать для одной зоны исключение
значит развалить правило.

`bold/travel` закрыт: кадра не будет, зона отвечает светом, как остальные.
Три попытки на неё больше не тратьте.

---

## 6. Экраны — давай

Оба, как договаривались:

- **зона целиком списком** — сортировка, фильтр «только свободные», массовое
  скрытие (ваш турн 19b, P0 №4);
- **вся комната списком** — плоский перечень всех вещей, сгруппированный по
  зонам; строка: фото, название, состояние, цена по правилам видимости, зона.
  Переключатель «комната ↔ список» рядом со сценой. Две роли: хозяйке — её
  вещи, гостю — то, что ему видно.

Напоминание про тихую бронь: у хозяйки ни в одном из двух видов не должно
быть следа того, что и кем забрано. Это инвариант продукта.

Мы уже начали собирать их из типографики турна 8 и сетки 15a — присылайте,
сверим и поправим.

---

## 7. Дрейф комнаты (28b) — берём, с вашим правилом остановки

Проезд ~23 px/с от края до края, указатель едет вместе с окном, **первое
касание фиксирует дрейф на всю сессию**. Ваша рекомендация «гостю дрейф,
хозяйке вздох» принимается: механика одна, режим — флаг.

Отдельно проверим у себя, не то же ли это движение, что автопроезд из нашего
тикета 55, — если да, объединим, а не положим рядом.

---

## 8. Логотип-локап

Ждём. Сейчас в макете живой `<text>` — на чужой машине он развалится.
Делайте вместе с экранами, как предложили.

---

## Порядок

1. Четыре чемодана файлами + числа + sha базы.
2. Обмеры `study` и `loft` изменёнными полями.
3. Коробки, партия 2, по одному глаголу за раз.
4. Экраны списков и локап логотипа.

Вторая партия предметных кадров (33 примера ещё без фото в четырнадцати
пулах) — потом, отдельным разговором. Острота снята: рядов заглушек больше
нет ни в одной зоне.
