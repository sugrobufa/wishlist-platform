# Что изменилось в строках — round42

## Прехедеры вёрсток (нарушали правило рода)

| Файл | Было | Стало |
|---|---|---|
| `reminder.html` | Ты обещал {item} — напоминаем, чтобы не забылось | Подарок за тобой: «{item}» |
| `item-gone.html` | {name} убрала {item} из комнаты. Бронь снята, ты ничего не должен | Бронь снята: «{item}» больше не в комнате — делать ничего не нужно |

Те же две строки исправлены в `letters.json` (`templates.*.preheader`). Прехедер
`after-party.html` был назывным и не тронут.

## Ключи словаря

| Ключ | Было | Стало |
|---|---|---|
| `Occasion.hint` | Отметь, что дошло — вещь переедет в сокровищницу, а даритель станет виден | Имена уже открыты — это случилось один раз, при первом заходе сюда. «Дошло» — про вещь: она уедет в сокровищницу, имя останется при ней |
| `Consent.quietGiver` | подарил тихо — предложения связи нет | тихий подарок — предложения связи нет |
| `Consent.quietGiverF` | подарила тихо — предложения связи нет | **КЛЮЧ УДАЛЁН** |
| `TreasuryAccess.optMutualHint` | тем, с кем ты осталась в связях после подарков | тем, с кем связь осталась после подарков |

`quietGiverF` удалён, а не переименован: если ваш сторож на него ссылается, снимите ссылку.

## Строка вне словаря

Подпись подборки «начни с готового»: «Подборка по интерьеру, который ты выбрала» →
**«Подборка под твой интерьер»**. Живёт в `handoff/task15.json`, `handoff/task17.json`
и на доске (турны 30, 33, 41) — во всех трёх местах исправлена. В словаре её нет;
переносить ли ключом `Empty.starterCaption` — вопрос к вам.

## Строка письма о раскрытии имён

`after-party.html`, тихая строка внизу: «Отметишь «дошло» — вещь уедет в сокровищницу,
и тогда же станет видно, от кого она» → **«Имена дарителей появятся на странице, когда
её откроешь, — один раз и насовсем. «Дошло» — про другое: вещь уезжает в сокровищницу,
имя остаётся при ней»**.

## Памятка тона

`tone.md`: строка 57 — «зал славы» → «сокровищница»; правило рода расширено на все поля
письма; добавлены абзацы про пару ключей по роду и про падежную ловушку токена.
